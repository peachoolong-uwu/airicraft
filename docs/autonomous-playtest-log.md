# Autonomous capability playtest

Started 2026-09-11 (Asia/Taipei). User-authorized goal: continue the playtest/fix loop autonomously for general survival capabilities; treat inefficiency and missing interfaces as valid findings, and revert changes that fail their intended purpose. Related historical evidence: [survival playthrough](codex-driver-survival-playthrough.md).

## Working agreement

- Drive the active client through exposed agent tools. Pause suspected failures and export the flight recorder before changes. Hot-load fixes when supported; resume playtesting automatically.
- Keep changes focused on observed requirements. Separate live evidence, automated checks and unresolved hypotheses. Commit logical changes; preserve unrelated work.
- Use structured observations first and screenshots for visual ambiguity. Track repeated queries and avoid building an observation or memory subsystem from speculation.
- Keep decisions and outcomes here. Raw local evidence lives under `run/playtest/2026-09-11/` (ignored); never commit bridge/dashboard credentials or full unsanitized client logs.
- World memory is game data. Existing named places are the initial capability; expand it only when repeated gameplay exposes a concrete missing fact, ownership rule or retrieval need.

## Acceptance checkpoints

1. Resource supply: repeat useful wood, stone, fuel and iron acquisition from multiple positions; craft and replenish the required tools.
2. Food: obtain an immediate food source and establish a renewable crop source; harvest, replant and produce edible output without manually specifying every routine action.
3. Shelter: choose a usable site, build an enclosed roofed shelter with an entrance, lighting and workstations; return to it using world-persistent memory.
4. Efficiency: use compact structured queries to identify terrain, resource and building/farming opportunities; record and address repeated observation or action overhead that materially slows the loop.
5. Survival: once the peaceful checkpoints are demonstrated, switch to easy and test at least two full day/night cycles while maintaining food, tools and a return route. Escalate difficulty only after the preceding level is stable.

These checkpoints guide testing; they are not claims of completion.

## Verified checkpoint after the autonomous run

- All five acceptance checkpoints above were exercised live. Easy ran from world time 118,078 to 167,363: 49,285 simulation ticks, about 41 minutes and more than two full day/night cycles, with no time commands or sleep skips. No death was observed; the latest saved statistics also have no death entry and equal play-time/time-since-death values.
- Player is paused inside `shore shelter` at `(257,63,480)`, health 20, hunger 20, full iron armor. Inventory retains three cooked porkchops, two bread, an apple, two wheat, fifteen wheat seeds, two iron picks, an iron sword, shears, three coal and five torches. One storage slot remains free.
- Shelter door is verified closed; bed foot `(256,63,480)` faces west toward head `(255,63,480)`, with spawn point set. The east doorway column stays clear. Farm has fourteen wheat and fourteen farmland cells. World bookmarks preserve shelter/farm bounds and updated station/resource notes.
- Live-proven changes include buried resource acquisition and tool-loss replanning, crop collection/replanting, neutral-mob avoidance, usable navigation coordinates, preserved construction areas, filtered entity queries, combat weapon retrieval, drowned weapon classification, and nearby-workbench reuse. See the decision entries for the evidence boundary of each change.
- Full build passed after the final source changes (`post-survival-fixes-build.log`). All task changes are committed; the pre-existing dirty `vendor/action-plan-advisor` submodule was preserved.
- Remaining limits: some recorder exports flag dropped observations; depleted acquisition scopes can repeat eight futile replans; storage/transfer will be the next capacity requirement. Earlier farmland damage during elevated leaf harvesting remains causally unconfirmed. Normal/Hard difficulty and a clean long run without interventions have not been validated.
- Final live pause: debug session `e5ac8a7b-f80e-439e-9315-6f822e55d914`, epoch 1, client tick 28,356. Re-read the handle before resuming. Export `completed-checkpoint.jsonl` has 1,491 observations with frames and a truncated flag; retain the earlier focused, non-truncated incident exports as well.

## Initial checkpoint

- Code at start: `e166303a`, live via HotSwap. World: `another world`, peaceful survival, driver mode active.
- Player `(209,92,447)`, health 20. Inventory: iron/stone/wooden pickaxes, nine coal, three raw iron, eight torches, four dark-oak logs, 57 cobblestone, 16 dirt, one plank.
- World place `iron workshop`: `(207,89,460)`, furnace `(206,90,459)`. Exact return failed twice; downward-digging override did not resolve it and was reverted. Other approaches are untested.
- Pause at start: session `ffff6172-e072-4759-8ae1-bcb6058d881c`, epoch 1, server tick 25509. Event cursor 154. These handles must be re-read after resuming/reloading.
- Known recorder size defect still shortens retained history under task load. Preserve immediate exports and bounded event logs until repaired.

## Decisions and experiments

### D001 — Establish the surface loop before extending architecture

Use existing structured feature/block queries to locate a suitable surface camp with soil and water, then exercise food and shelter. The existing mining proof already covers buried coal and iron. Do not require returning to the old underground furnace before making progress: its location is recorded, and a new furnace is cheap from the carried cobblestone. Revisit route failure if it blocks useful travel or can be minimized during the run.

Next: inspect available feature-query contracts, find surface opportunities, and select a camp using explicit coordinates and named-place memory.

### D002 — First food loop and attack-hand ownership

- The forest feature's explicit stand coordinate `(203,98,446)` was reachable. A high-level inventory goal crafted a stone axe, including ingredients and workstation use, in one request (`action-graph-2dd5d0f0-0747-4450-8fd9-618a77f7d9da`). It used two logs; optimization of recipe yield/replanning is not yet justified by this single run.
- Equipped the axe, then killed pig `a1643344` through `attack_entity`. The recorder shows `minecraft:cobblestone` held during repeated `attack_cooldown` snapshots and the landed hit at tick 13010: chase navigation selected a building block and the attack executor never restored the chosen hand. This is a confirmed execution inefficiency, not a lethal mob-reflex result.
- Narrow fix: capture the selected hotbar slot when an attack begins and restore it before cooldown checks and hits. Existing focused entity-interaction tests pass; live second-hunt validation pending. HotSwap applied without restarting the client.
- Pork pickup required a separate explicit navigation to the observed drop at `(218,104,453)`; inventory confirmed three porkchops. This matches the attack tool's kill contract, but repeated hunt/pickup overhead may justify a food-acquisition primitive after more evidence.
- Started a high-level cooked-porkchop inventory goal. Raw evidence: `run/playtest/2026-09-11/{axe-graph,hunt-pig,pork-pickup}-status.txt`, `hunt-weapon.jsonl`, `events-first-food.txt`.

D002 outcome: the cooked-porkchop goal succeeded and inventory confirmed one cooked porkchop (two raw retained). The second hunt (`b9933ea1`) succeeded after HotSwap; recorder snapshots at ticks 13756/13761 show the stone axe during attack cooldown, and the target died at 13788 with the axe selected. This validates hand restoration live, not a controlled speed benchmark across identical terrain. Evidence: `hunt-hand-fixed.jsonl`. No production logging or new test-only abstractions were added; existing entity-interaction regressions passed.

### D003 — Surface-filtered feature queries

The current water feature search has no surface constraint. Its nearest results from the initial position offered water around Y=42–62 while our useful terrain was near Y=98–110, requiring extra inspection to distinguish caves from river/lake shores. Add optional `surfaceOnly` to the existing query, filtering target and standing positions while preserving the connected-water evidence. Use the same ground classification as acquisition (ignore logs/leaves as roofs). This is a focused observation enhancement driven by camp/farm selection, not a general map or memory rewrite.

D003 outcome: two regressions first failed (underground pool taking the result limit; underground standing position offered for surface water), then passed. The live `surfaceOnly=true` query is available through the normal provider after HotSwap. In the same-position comparison, an unfiltered result offered target `(266,62,457)` / stand `(266,62,456)` on a connected water component; the surface query selected `(272,62,461)` / `(272,63,462)` instead and reported two qualifying surface sources. The first larger lake retained its shore target, while its returned center moved from Y=58 to Y=62. This is filtered coordinate evidence, not proof of route reachability. `water-any.txt` and `water-surface.txt` preserve the comparison. Full build passed. Surface classification was extracted unchanged from acquisition and shared with feature search.

### D004 — Exact breaking must remove plants

The bucket inventory goal succeeded (`action-graph-150bd65e-9d14-4faa-8707-2764d7ddb178`), including smelting three carried raw iron. Inventory confirmed one bucket. Evidence: `bucket-graph-2-status.txt`.

Attempting to get wheat seeds reproduced false success: `break_blocks` on observed short grass `(215,107,459)` returned `brokenTargets=0 skippedTargets=1`; the following world read still found short grass. Paused and exported `grass-skipped.jsonl` before rebuilding. The completion predicate classified every replaceable block, and every waterlogged block, as already removed.

Narrow fix: only air and standalone fluid blocks count as cleared. After HotSwap and approaching the same target, the repeated request returned `brokenTargets=1 skippedTargets=0`, and the next nearby scan no longer found grass there. No seeds dropped from this single random trial. Focused block-break tests passed; live proof is in `grass-break-fixed-result.txt`. Continue testing seed acquisition without assuming one broken grass guarantees a drop.

### D005 — Reuse the generic inventory planner for seeds and tools

The high-level goal for four wheat seeds resolved a short-grass mining route with probabilistic yield (`estimatedBreakCount=32`) and completed with four seeds. No seed-specific acquisition tool was necessary. The route also picked up the earlier pork drop in transit: inventory now holds five raw porkchops. Surface shore navigation to `(260,63,478)` completed, with the player settling in adjacent shallow water `(260,62,479)`. A bucket interaction filled the bucket, and a high-level stone-hoe goal completed.

Planted four wheat crops using two batch `use_block` calls: soil `(259,62,479..481)` plus `(260,62,478)`, crops one block above. Structured reads confirmed wheat age 0 and hydration beginning (moisture 7 on the first tile). Evidence: `seed-goal-2-status.txt`, `fill-bucket-result.txt`, `hoe-goal-status.txt`, `till-farm-result.txt`, `plant-farm-result.txt`, `shelter-ground.txt`.

Choose a small excavated shelter in the hillside immediately west of the farm. Initial room bounds `(255..257,63..64,479..481)`, entrance `(258,63,480)`, floor Y=62 and roof Y=65. Preserve the farm and add roof where the hillside is open. Existing exact-break batches suffice for the first room; no speculative building DSL. Seven entry/first-row blocks were successfully excavated through normal tools.

Recorder diagnosis: a captured runtime snapshot repeated about 1 MB of mission evidence twice, mostly the entire known recipe catalog (`knownCrafts` ~958 KB, `knownSmelts` ~40 KB). Static recipe knowledge must be retained once with explicit references, while current decision/world evidence remains independently queryable. This is measured from `grass-skipped.jsonl`, not just an assumed byte-budget problem. Implementation pending.

### D006 — Area inspection must include air cells

Roof placement was blocked as `target_not_inspected` for `(258,65,480)`. The automatic inspection and an explicit one-cell `inspect_area` both returned zero records. Paused/exported `roof-uninspected.jsonl`. Source filtered out air unless a nonreplaceable block sat directly below, making ceilings and the upper half of doors impossible to inspect through this path.

Removed that hidden filter: `inspect_area` reports the requested cells, including air, subject to its existing result cap. `find_blocks` remains the selective search. Only returned cells grant mutation freshness; limits remain unchanged. Focused world-query tests passed. After HotSwap, the same two-cell roof query returned both air cells and both cobblestone roof placements completed (`roof-query-fixed.txt`, `roof-fixed-result.txt`). No client restart.

### D007 — Shelter furnished; relocate crops away from foot traffic

Door `(258,63,480)` is closed and faces east. Interior furnace `(255,63,479)`, crafting table `(255,63,481)`, wall torch `(255,64,480)` and outside farm torch `(259,64,479)` were placed successfully. `shelter-interior.png` visually confirms the lit room and workstations. Saved `shore shelter` and `shore wheat farm` in world memory with exact geometry and crop positions.

A later check found `(259,62,479)` reverted to grass and the crop above missing; three other wheat plants remain. The old recorder window cannot identify the moment. Trampling during entry/navigation is plausible, not proven. The original farm overlaps the door approach, so move/expand cultivation away from this traffic instead of treating every loss as an executor bug. Preserve mature crops until harvest where practical. Evidence: `shelter-farm-check.txt`; paused boundary and export `recorder-before-catalog.jsonl`.

### D008 — Deduplicate recorder context, retain current state at one-second intervals

Initial live compaction reduced a runtime payload from about 2 MB to 151,536 JSON bytes, but 120,427 bytes were repeated action-graph details and 18,205 were dialogue history. The final projection stores recipe catalogs, dialogue history, and each action-graph execution as reusable versioned context; a snapshot keeps exact sequence references plus current evidence/summaries. Unchanged context extends its validity; budget eviction considers the end of validity so recently reused context is not discarded just because it was first inserted long ago. Detailed execution payloads retain the existing bounded trace contract.

Runtime snapshots now sample every 20 completed server ticks; per-client-boundary task/reflex decisions and five-tick LLM polling remain. Duplicate mission/task/reflex copies inside `agent` are removed. CLI interval export includes overlapping context; live and file seek load the referenced versions. Full build and context/version/retention/seek tests passed. HotSwap required no restart. Live snapshots measured 15,627 JSON bytes; the first sample held nine distinct execution contexts rather than copying all nine into every snapshot. This proves payload reduction, not yet a full ten-minute workload retention run. Earlier loss counters remain historical and are not reset to hide drops.

Browser validation loaded both structured and framed exports, decoded a 640x360 incident frame, and exercised saved playback without console errors. Evidence: `recorder-context-first.jsonl`, `seed-farm-conflict.jsonl`, `recorder-build.log`.

### D009 — Repeated pause must preserve both tick gates

The watcher raced an explicit incident pause. A second pause reused the client capture, but the server controller rejected the same session; the runtime's error handler reset the client gate. Confirmed mismatch: client ticks advanced from 40,402 to 41,627 while server tick stayed 38,494 and server phase remained PAUSED. This is a debugger bug, not game progression behavior.

Make server pause idempotent for the already-paused session; another session is still rejected. Controller regression passed. HotSwap and recovery through the existing server pause handle restored both gates. Repeated normal CLI pause then returned the same session `c606c35d-20e4-4a3c-ab20-201601bc7319`, epoch 1, client tick 42,786 and server tick 38,514. A later read still held those ticks. Evidence: `duplicate-pause-state.txt`, `pause-restored.txt`, `pause-repeat-fixed.txt`, `pause-repeat-stable.txt`, `duplicate-pause-tests.log`.

The eight-seed goal selected immature wheat from the planted farm, collected one seed and ultimately failed for lack of reachable sources. It was no longer active when cancellation was requested. Remaining crop `(259,63,481)` reached age 1, disproving a complete growth stall; another crop at `(260,63,478)` remains age 0. Normal saved-world randomTickSpeed is 3 and measured light was 12. Crop/source lifecycle handling is the next confirmed capability issue; do not resume this destructive seed goal unchanged.

### D010 — Generic acquisition preserves immature crops

Use Minecraft's `CropBlock.isMature` for generic resource acquisition. Filter both candidate selection and the final target-presence check, and supply the resolver with a separately cached count of harvestable blocks. Keep ordinary nearby-world evidence unchanged: a growing crop still exists and remains observable. Exact `break_blocks` remains available for deliberate terrain/crop edits. This covers CropBlock crops; other plant families and automatic replanting are not claimed.

After HotSwap, forcing acquisition from the two immature wheat plants failed with `itemCount=1 rejectedTargets=0`, collected count 0. A subsequent world query retained both plants at ages 0 and 1 (`immature-crops-preserved.txt`). Repeating the eight-seed inventory goal selected `minecraft:short_grass:hand` instead of wheat, and began approaching grass beyond the shore. This validates planner ranking and executor preservation; it does not yet prove an eight-seed collection here. Full build passed (`crop-maturity-build.log`).

### D011 — Preserve the intent attached to built places

Confirmed damage beyond crop harvesting: the earlier seed acquisition undermined the shelter entrance. The paused exact read found air at `(258,62..64,480)` where the door support and both door halves had been. Door inventory increased from two to three. The recorder first shows three doors at server tick 37,472, with the player at `(258.4,62,480.7)` and acquisition approaching wheat `(259,63,481)` via excavated work position `(258,62,481)`. Evidence: `seed-farm-conflict.jsonl`, `shelter-door-after-acquisition.txt`.

Decision: extend named world places with an explicit optional preserved region. It must constrain automatic resource target selection and Baritone route excavation; walking through a preserved place and deliberate coordinate-based construction remain possible. Keep this world/dimension scoped and persistent. This is ownership information learned from actual play, not an invented general-purpose memory database. Repair the entrance, then retry a constrained resource request and a return trip against the protected site. A Baritone calculation hook may require one client restart to install; save/export first and prefer HotSwap for subsequent changes.

The improved seed run did select grass, collected one additional seed (two total), but sparse steep terrain consumed time and seven cobblestone. Cancelled it to address site preservation. Current player `(288,91,463)`; six cooked pork, six coal, 28 cobblestone, 36 dirt, two wheat seeds, iron/stone/wooden picks, axe, hoe, six torches and a filled water bucket. Two wheat plants remain near the shore. Current code `578a7d92`; game paused, no seed goal should be resumed. Re-read tick handles before acting.

### D012 — Preserved world regions validated live

`remember_place` now accepts optional inclusive `preserveArea` bounds in the place's dimension; recall/list returns them. Replacing a place without bounds removes preservation. The world file remains compatible with old bookmarks. An immutable client-world policy feeds automatic target selection, harvestable source counts, and Baritone's existing protected-block cost check. A final input guard also stops Baritone breaking a newly preserved block on a previously calculated path. Exact coordinate edits remain available. Recorder snapshots include active preservation bounds and load availability.

Focused persistence, scope, malformed-input and publication tests and the full build passed. One client restart installed the new Baritone Mixin; Minecraft's normal shutdown hook saved the world, and the same inventory and bookmarks loaded. Subsequent policy-loop cleanup used HotSwap. Live return from `(288,91,463)` to `(256,63,480)` completed with the protected areas active. Repaired entrance support, adjacent floor/wall and door through ordinary exact tools. A one-block cobblestone acquisition confined to the protected shell failed with zero collected and left the shell intact. Navigation aimed inside its solid wall failed without excavating it. An explicit `break_blocks` then broke that same wall block and `place_block` restored it, confirming deliberate edits still work.

Evidence: `protected-return-1-*`, `protected-door-repair.txt`, `protected-acquisition-*`, `protected-solid-navigation-*`, `protected-navigation-after.txt`, `protected-explicit-{break,replace}.txt`, `place-preservation-full-build.log`. This preserves terrain from gathering/path work; it does not prevent crop trampling or claim protection from mobs. Farm layout still needs to avoid foot traffic. The pre-restart recorder export is `pre-preservation-restart.jsonl` (marked truncated due to earlier budget losses); the restart begins a new retention measurement.


### D013 — Bound the remaining visual recorder cost

After the preservation restart, server tick 18,765 retained 594 runtime snapshots across the 12,000-tick window, with no structured-record budget drops. Total storage was 50,584,522 bytes. Animated water kept every sampled RGB frame distinct: 497 retained frames reached the separate 32 MiB visual cap, with 438 historical visual drops. Evidence: `recorder-after-ten-minutes.txt`.

Change the default RGB interval from 20 to 40 server ticks (0.5 fps); explicit configuration can still request 1 fps. The snapshot/decision rates remain the same. This follows the measured visual cost and the user's preference for sparse agent-readable history. Verify the fresh client for a complete ten-minute window after installing crop tending. Existing exports keep their loss flags; `pre-tending-restart.jsonl` includes the available frames and is marked truncated.

### D014 — One-pass crop tending instead of coordinate micromanagement

Bread request `action-graph-d6ccb873-6b56-4e33-9e15-545c783ee090` failed `no_route` while four wheat plants were immature. Paused and exported `bread-no-route.jsonl`. The current Java providers do not implement a usable growing-crop route; historical bread-bootstrap docs and leftover runtime YAML are not evidence of an active capability. Two new crops at `(261,63,484)` and `(263,63,484)` are planted away from the entrance, with a torch at `(262,63,484)` and all four crops still present (`crop-growth-during-implementation.txt`).

Implement `tend_crops`: a single bounded pass over an existing flat plot, identifying mature crops, approaching, harvesting, collecting drops, and replanting; plant empty farmland from available seeds and report growing/missing-seed counts. It uses the normal job/executor ownership and cancellation path. The planting item selects the CropBlock type; no hardcoded wheat-only maturity rule. Immature crops and unrelated blocks stay intact. Plot tending is a deliberate crop operation inside preserved farm bounds. It does not till soil or wait for growth. Keep site preparation separate until further play requires a reusable higher-level operation.

State-machine and job-lifecycle tests passed, including delayed planting completion, crop changes during approach, no-seed reporting, stalled paths, and session/reflex pauses. Full build passed. Installing the new task enum/executor requires a restart; later implementation fixes can use HotSwap. Live harvest/replant proof is pending, so do not count this as a completed farming checkpoint yet.

Growth context: the new crop had light 14, and other wheat advanced to age 1/2, so a complete growth stall is disproven. Minecraft's current `CropBlock.getAvailableMoisture` rewards surrounding hydrated farmland and penalizes crowded same-crop layouts. Expand tilled soil around spaced rows while waiting instead of modifying game growth rules.

Initial live tending pass completed with `harvested=0 planted=0 growing=4 missingSeedPlots=12`; exact world reads retained all four plants. This also exposed loss of primitive completion detail in semantic TASK UPDATE messages. Tending now uses the normal bounded block-operation result path, and semantic terminal updates include the matching primitive's detail. Focused runtime/executor/job tests passed; HotSwap applied the feedback fix without restarting. Evidence: `tend-growing-feedback.txt`. The expanded plot has 12 additional tilled cells around the spaced crop row; fetch seeds from the remembered forest and validate automatic planting next. The remembered protected bounds survived the crop-tending restart (`preserved-places-after-restart.txt`).

### D015 — Resource supply and farmland coordinate consistency

The forest seed goal `action-graph-7334b9c1-2866-43b0-ae4d-fa795c87b660` succeeded: zero to eight seeds through 28 short-grass breaks. A surface-constrained wood request then gathered eight pale-oak logs with the stone axe and no rejected targets. Returned from the forest to the farm. A row-only tending pass planted two empty cells and preserved two growing crops: `harvested=0 planted=2 growing=2 missingSeedPlots=0`, seeds eight to six. Evidence: `forest-seeds-3-*`, `forest-wood-eight-*`, `seed-supply-return-1-*`, `tend-row-first-plant.txt`.

A current-position bookmark captured on farmland recorded `(261,62,483)`, although Baritone walks at navigation Y=63 over the 15/16-height soil. Returning from `(264,62,483)` failed. Separately, requesting the correct `(264,63,483)` physically arrived with estimated ticks zero but reported CANCELLED because Airicraft compared raw block Y=62. Both incidents were paused and exported before edits (`farmland-bookmark-failed.jsonl`, `farmland-navigation-cancelled.jsonl`; neither truncated).

Current-position memory and navigation completion now use Baritone's own `playerFeet()` coordinate convention. Explicit user coordinates remain exact. Focused facade, task and memory tests passed; HotSwap applied four classes. Live current capture returned `(264,63,483)`, then moving to `(261,63,483)` and returning to the new bookmark both completed. Evidence: `farmland-fixed-{away,return}-*`. Updated the local watcher to pause unexpected CANCELLED outcomes as well as failures.

The row crop at `(263,63,484)` reached age 5; live mature harvest/replant is still pending. A fresh query found five crops total, not six: the old entrance-side `(259,63,481)` plant is absent. Keep productive rows away from routine access and do not count remembered crop totals as current evidence.

### D016 — Aim inside short crop outlines

The generic crop tending code aimed at the full block center. A deterministic voxel raycast reproduced a miss when that endpoint lies on the top boundary of the half-height mature beetroot outline; aiming at the outline's center hits. Use the actual crop outline center for both visibility and camera aim. The geometry regression and focused navigation/memory/task tests passed; HotSwap applied the environment change. This is geometry proof, not a live beetroot harvest claim. Live wheat harvest remains the next farming checkpoint.

### D017 — Full recorder window and shelter/supply recheck

At server tick 12,551, the new half-frame-per-second default retained the full 12,000-tick window: 597 runtime snapshots, 300 visual frames, 49,453,744 bytes total and 20,784,272 visual bytes. No dropped record types were reported; expired entries belong to normal rolling retention. Evidence: `recorder-half-fps-window.txt`. This is a complete live window under the current workload, not a guarantee for every scene or event rate.

Returned to the shelter and inspected both halves of its shell. All 50 required floor, roof and wall/door cells were observed with no air gaps; furnace, table and wall torch remain present. The high-level 21-torch goal succeeded, stock five to 21, consuming four coal and four sticks. Picked up the displaced entrance crop's seed on the return trip (seven total). Evidence: `shelter-{current,west}-shell.txt`, `shelter-return-after-farm-*`, `replenish-torches-*`. Full build passed after the navigation and crop-aim fixes (`post-navigation-build.log`).

### D018 — Detours are navigation progress

Local eight-coal acquisition selected `(249,63,489)` from the shelter. The first approach was rejected after 81 ticks, although recorded positions moved from `(256,63,480)` through the eastern entrance to `(260,63,483)` and `(260,62,485)`. The progress metric only accepted a new shortest straight-line distance to the target, so leaving the room initially in the opposite direction consumed the entire no-progress allowance. Paused both gates and exported `shore-coal-stall.jsonl` before editing (674 observations, not truncated). The next side had already excavated 22 stone blocks; that productive continuation was not evidence of a new stall.

A regression using the recorded detour failed first, then passed when the no-progress check tracked movement between positions. Stationary approaches still time out, and the 2,400-active-tick attempt budget bounds looping routes. HotSwap applied the change and resumed the same coal job. Live completion is pending.

The same coal job completed after HotSwap, inventory two to ten coal, with no replacement acquisition request (`shore-coal-eight-2-*`). This proves continuation and resource completion; the original detour had already been abandoned, so its prevention is currently covered by the recorded-position regression. Several short drop approaches emitted CANCELED and were reselected while pickup still progressed; log this as a bypassable inefficiency, not a new progression blocker.

### D019 — Renewable crop harvest and parallel smelting demonstrated

The eight-iron acquisition completed from ore at Y=68 down to Y=54, with no detour stall and eight raw iron collected. Returned to the remembered shelter. Enabled existing automatic spawn-proof lighting (`maxLightLevel=1`, spacing six); four torches were consumed during excavation. Started smelting all eight raw iron using one coal at the shelter furnace, then tended the farm while smelting continued.

The first mature row pass completed at server tick 18,637: `harvested=1 planted=1 growing=3 missingSeedPlots=0`. Inventory confirmed one wheat and ten seeds (seven before the pass); exact read confirmed the mature `(263,63,484)` plant was replanted at age 0 and the other three row crops remained. This proves real harvest, pickup and replant through one tool call. A bread output still requires three wheat, so the full edible-crop checkpoint remains open. Evidence: `shore-iron-eight-{1,2}-*`, `iron-return-home-*`, `tend-first-mature.jsonl` (93 records, not truncated), `tend-first-mature-*`.

### D020 — Increase farm throughput as seed supply improves

Collected the eight smelted ingots; the chestplate inventory goal succeeded and `equip_item` accepted it for the chest slot. The crafting run also converted one log to four planks that remained afterward; record this as a small possible prerequisite inefficiency, not a proven crafting blocker. Its exact route history has not been diagnosed.

With ten seeds available, planted eight additional cells in the already-prepared soil via one bounded tending pass, preserving two seeds. The main plot now has twelve crops. This revises the earlier sparse-row choice: adjacent wheat slows individual growth, but more planted cells should improve total harvest from existing soil without another excavation project. Measure output rather than assuming the improvement. Updated the world-persistent farm note with the new layout and successful tending workflow. Evidence: `expanded-farm-crops.txt`, `expanded-farm-inventory.txt`, `chestplate-inventory.txt`.

### D021 — Full leggings supply chain and two open return-path findings

The iron-leggings goal `action-graph-e94ba171-e887-4788-a1f3-f6aabf34ac89` succeeded end to end: craft a portable furnace, acquire iron, start and await smelting, collect output, and craft leggings. Equipped the leggings. Extra ore exposed by the route yielded surplus; two ingots and one raw iron remain. It used a new underground furnace at `(255,52,474)` and table at `(255,52,476)` rather than reusing the surface stations.

Recheck found the newly planted `(260,63,483)` crop missing and its farmland converted to dirt/grass; current light is 12. The return from the expanded farm crossed that area; the later leggings mining route went north, so do not attribute the loss to that mining route. Paused and exported `farm-traffic-loss.jsonl` (1,085 records, not truncated). Trampling is suspected but no per-tick landing reproduction has yet been captured. Repair and a short player/block trace remain pending.

Returning from the underground crafting pocket to valid open `(259,63,481)` repeatedly failed `CALC_FAILED`. Selecting the iron pickaxe and moving one block sideways did not resolve it. An exact one-block upward navigation did succeed, proving local excavation/placement works. The next whole return advanced partially to `(258,52,480)` then failed again. Increasing path-search time from 2 to 10 seconds did not resolve it, so restored the original 500 ms primary / 2,000 ms failure budgets. Do not keep that ineffective override. Evidence: `leggings-return-failed.jsonl`, `leggings-return-pick-selected-*`, `pocket-{side-step,up-one}-*`, `return-after-pillar.jsonl`, `return-extended-search-*`. Next test uses the actual recorded mine entrance near `(263,65,471)` as an intermediate waypoint; home remains the final intended destination.

### D022 — Access construction and placement input restoration

The recorded north entrance was reached, but its final leg to home still failed. This disproved a purely underground-climbing diagnosis. Exact shore inspection found floor gaps at `(259,62,478)`, `(259,62,480)` and `(259,62,482)`, all inside the preserved farm region. The narrow shore access alternated solid islands and gaps; automatic navigation could not bridge protected cells. Deliberately placed a three-block cobblestone walkway through exact construction tools.

The third placement stalled at `waiting_for_sneak_for_placement`, player standing with the actual sneak input cleared. Paused and exported `walkway-sneak-wait.jsonl` (110 records, not truncated). `PlacementSneakController` treated its ownership flag as evidence that the key was still pressed, so it waited indefinitely after input was cleared. A regression failed first, then passed after restoring owned input when the actual key is up, including a stale player-sneaking flag. Focused placement tests passed; HotSwap applied two classes. Resuming the same job completed the third block within 33 client ticks, with no replacement request. Exact inspection confirms a continuous floor along x259, y62, z478..483. Evidence: `walkway-sneak-fixed-*`, `doorstep-after-build.txt`.

The repeated long return failures are not yet classified as an A* defect: access construction was incomplete. Retry home after the walkway, then repeat from the mine entrance to establish whether that construction resolves them. The farm trampling trace remains pending.

### D023 — Passive-mob avoidance caused the repeated return failures

The continuous walkway fixed short access, but the return from the north entrance still failed. A temporary, read-only local path-cost probe showed finite connected movement costs into the shelter, disproving a fully blocked local route. Inspecting Baritone's actual avoidance implementation then found its filter accepts every `MobEntity`. The live probe found 87 avoidance centers including fish, bats, farm animals, a wandering trader and its llamas. The three trader-party entities multiplied the home destination's cost by 64. More heavily overlapping fish also explain why digging away from that area incurred extreme estimates; this latter connection is an inference, not a separate controlled test.

Controlled live test: from the unchanged `(263,65,471)` position with the same home goal `(256,63,480)`, the default coefficient 4 failed repeatedly. Setting only `mobAvoidanceCoefficient=1` made the identical return complete in 105 client ticks, without terrain preparation between those two attempts. Evidence: `home-avoidance-factors.txt`, `home-path-costs.txt`, `home-without-passive-avoidance-*`. This supersedes the earlier incomplete explanation based on pocket geometry and shore floor gaps.

Fix the integration by filtering Baritone's entity stream to Minecraft's `Monster` marker, which covers slimes and ghasts as well as HostileEntity subclasses. Keep its subsequent enderman, zombified-piglin and daylight-spider exceptions and retain spawner avoidance. Remove the temporary path-cost logging. A new Mixin requires one client restart; afterward restore the normal coefficient 4 and repeat the route. Live validation of the durable fix is pending.

D023 live follow-up: after the Mixin restart, the original mob avoidance coefficient remained 4 (radius 16). Shelter to north entrance and the previously failing north entrance to shelter both completed. Evidence: `monster-fix-{north,home}-*`. Corrected the world-persistent mine note to remove the obsolete mandatory-waypoint workaround, and moved the farm bookmark onto the solid access walkway rather than a planted cell. Eleven crops still present and growing. The durable route fix is now live-verified; hostile avoidance will be checked after the peaceful food checkpoint.

### D024 — Crop tending must collect actual scattered drops

The isolated mature wheat at `(260,63,478)` reported `harvested=1 planted=1`, but inventory remained one wheat and spent one seed. Paused and exported `old-crop-drops.jsonl` with frames (103 observations, not truncated). Nearby-item inspection and the rendered capture confirmed wheat and seeds still lying 1.6–2 blocks away. The collection phase accepted squared integer-coordinate distance <=2.25 and waited 20 ticks; proximity to the old crop was not proof of pickup.

Changed collection to observe matching crop-drop entities, navigate to their positions, retarget remaining piles, and replant only after those drops disappear. The existing 240-tick phase limit bounds unreachable/full-inventory cases. A regression failed before the change; scattered-pile, retargeting and timeout tests now pass with the crop suite. Applied five classes via HotSwap. Manually recovered this already-completed task's abandoned harvest by walking onto the crop: wheat one to two, seeds one to four. Next mature harvest remains the live proof for the new automatic collection behavior.

Crafted shears and a composter through ordinary recipes/goals. Placed composter `(260,63,480)` on the spare shore table, clear of the x259 access path. Preparing a leaf-compost loop to improve farm output without altering random-tick rules. The composter recipe goal consumed four logs and left ten planks/five slabs; extra plank conversion is a bypassable planning inefficiency to inspect later.

### D025 — Surface acquisition on fractional ground and giant mushrooms

The 32-leaf request failed immediately on farmland `(260,62,478)` with `acquisition_scope_left`. Acquisition still used raw player block Y, unlike the already-fixed navigation/bookmark boundaries. Changed its player position to Baritone `playerFeet()`. Focused acquisition/excavation tests passed; HotSwap applied one class. The identical request then broke 13 leaves and collected 12, proving the initial farmland rejection fixed.

It subsequently stopped at `(263,64,487)`. Paused/exported `leaves-scope-second.jsonl` (165 observations with frames, not truncated). Exact column inspection found grass at Y63, leaves at Y67/68, and a giant red mushroom cap at Y69..71. SurfaceTerrain ignores logs/leaves but classified the mushroom canopy as terrain, so ordinary ground beneath it became underground. This is a separate false surface-boundary rejection, not evidence that the feet fix failed. Extend the existing vegetation exclusion to giant mushroom blocks and retry.

D025 follow-up: ignoring `MushroomBlock` canopy/stems in the existing surface-ground scan was compiled and HotSwapped. The remaining 20-leaf request completed from the stopped position, in the same anchored surface scope, with no rejected targets. Evidence: `leaves-mushroom-fixed-1-*`. Nearby leaf gathering also converted `(260,62,484)` and `(262,62,485)` farmland to dirt, destroying their crops. Paused/exported `leaves-farm-loss.jsonl` (354 observations with frames, not truncated). Diagnose player landings before resuming farm-adjacent resource work.

### D026 — Narrow the farm-loss claim and continue useful work

A traced shelter-to-ground-level tree edge round trip recorded no farmland changes. The return's only one-block descent landed on ordinary grass before crossing the plot. Evidence: `farm-roundtrip-trace.jsonl`, `farm-descent-trace.jsonl`. These disprove the claim that ordinary walking on the plot inherently destroys crops. The earlier leaf job's elevated work/drop approaches remain suspect, but the canopy was removed during that job and the same elevated path can no longer be replayed without reconstructing terrain. Two additional trace attempts performed no harvesting: one surface-only request started inside the shelter and correctly failed; after moving outside, the cleared local scope contained no reachable matching leaves. Do not label these as new bugs.

Keep the precise cause of the two lost cells unconfirmed. The important observed fact is that farmland became dirt during nearby harvesting. Repair the soil, keep routine harvesting away from the planted plot, and capture another real occurrence if it arises. No speculative navigation patch. Collected leaf stock is now 33; use it for composting. Full build passed after the crop pickup and surface fixes (`post-crop-surface-build.log`).

### D027 — Wait at an already-reached crop pickup goal

The first pass with drop-aware collection collected/replanted one crop, then failed while collecting the next. Paused/exported `crop-pickup-path-failed.jsonl` (143 observations with frames, not truncated). A read-only JDI inspection of the retained executor showed its failed work goal `(261,63,483)` was the player's current navigation cell. The implementation unnecessarily started a zero-length Baritone path while waiting for newly spawned item pickup. The trace also confirms no farmland conversions during this tending pass.

Skip path submission when Baritone already reports the pickup goal reached; keep observing the drops until collected or the phase budget expires. Added work position to decision snapshots. The regression failed before the change and passed with the crop/acquisition suites. HotSwapped one class. The next full tending pass completed; inventory reached five wheat and six seeds, including remaining nearby drops and the newly harvested crop. Evidence: `crop-pickup-current-goal-fixed-*`, `crop-pickup-fixed-pass.jsonl`, `farm-after-pickup-fix.txt`. Automatic harvest/collect/replant is now live-proven with the new logic. Proceed to bread and Easy-mode preparation.

### D028 — Peaceful food checkpoint and difficulty control

The first bread inventory goal succeeded using wheat from the maintained farm (`first-farm-bread-*`); fourteen crop cells were verified replanted, with two wheat, six seeds and one bread retained. The iron-sword supply goal also succeeded, supplementing the existing iron chestplate/leggings and six cooked porkchops. Peaceful resource gathering, hunting/cooking, crop harvest/replant, composting and basic enclosed shelter have all been exercised live.

The host desktop is locked, so the normal settings UI is unavailable. Added `world difficulty [--set peaceful|easy|normal|hard]` to the authenticated wrapper/bridge for the local integrated world. It runs on the server thread, rejects locked/hardcore changes and paused-server requests, and reports actual difficulty and time of day. Wrapper routing/validation/error tests and compilation passed. HotSwap loaded the bridge/service; a live read confirmed peaceful, unlocked, non-hardcore. No difficulty change has been made yet.

The sword-goal return then failed because a newly auto-placed crafting table at `(259,63,480)` obstructed the preserved doorway approach. Paused/exported `sword-return-failed.jsonl` (204 observations with frames, not truncated). This is a separate automatic workstation placement violation of preserved-place intent; fix it and restore entry before switching to Easy.

### D029 — Automatic workstations must respect preserved spaces

Portable crafting-table placement ranked the protected air cell `(259,63,480)` as feasible, then used an explicit-placement child task, bypassing the Baritone-only preservation guard. Added preserved-state filtering to its candidate policy and a live recheck before each child attempt. Applied the same preserved-cell check at both furnace discovery and execution placement boundaries; existing protected workstations can still be reused, and explicit coordinate-based edits remain allowed.

The recorded doorway regression and focused crafting/smelting tests passed. HotSwap applied the affected classes. Deliberately removed the misplaced table and the identical home return completed (`clear-doorway-table-*`, `doorway-cleared-home-*`). This proves the obstruction and restoration live; automatic selection of a new unprotected workstation site is still pending a suitable crafting run. The difficulty control also correctly rejected a request while ticks were paused (`difficulty-paused-rejected.txt`).

### D030 — Begin Easy survival

Cleared and closed the shelter door, equipped the iron sword, and enabled normal spawn-proof lighting. `world difficulty --set easy` returned changed=true at world time 118,078; a separate read returned easy with changed=false. No commands changed time, weather, health, inventory or growth rules. Start state: healthy, iron chestplate/leggings, six cooked porkchops, one bread, one apple, fourteen torches, a maintained wheat plot and the remembered enclosed shelter. Capture `easy-start-ticks.txt` anchors the server clock. Aim for at least two full day/night cycles with actual supply work, not idle shelter time alone. Pause/export unexpected behavior and resume after fixes.

### D031 — Tool breakage must invalidate the active acquisition route

The Easy-mode replacement-pick goal acquired one raw iron, then repeatedly stalled after its old iron pickaxe broke. At pause, the player held a wooden pickaxe against iron ore; a stone pickaxe remained in inventory, while the route required `minecraft:iron_pickaxe`. Export: `easy-pick-acquisition-stall.jsonl` (389 observations with frames, not truncated). Acquisition only checked tool suitability at dispatch/break time and treated later failure as another target rejection.

Added a per-tick presence check for the route's required tool, after checking completed inventory quantity. Tool loss fails with `MISSING_ITEM` and releases movement instead of cycling excavation sides. The regression failed before the change; acquisition/crop tests passed afterward. HotSwapped seven classes and resumed the same action goal. It replanned automatically to the existing stone pickaxe, gathered the remaining ore, and entered smelting (`tool-loss-replan-*`). The goal now needs two fresh pickaxes because the old one broke; full crafting completion is pending.

### D032 — Compact inventory readiness and completed Easy supply recovery

The same replacement-pick action goal completed with two fresh iron pickaxes after tool-loss replanning. Its portable table was placed at `(254,63,486)`, outside both preserved areas, and the final home return completed. This supplies live automatic-workstation placement evidence for D029. Export `easy-pick-completed.jsonl` contains 583 observations, not truncated.

Extended the existing `inspect_inventory` response with free storage slots, equipped armor/offhand, per-stack durability (inventory slot, item ID, remaining/max uses), and health/hunger/saturation. This requirement arose directly from needing a large debug trace just to identify a worn pickaxe before a supply run. Counts and prior output fields remain available; no new query tool or memory schema. Compilation and HotSwap passed. Live response confirmed both iron picks at 250/250, stone pick 71/131, the two worn armor slots, and seven free storage slots (`inventory-readiness-{live,final}.txt`). Starting an iron-boots supply goal while continuing the Easy survival clock.

### D033 — Distinguish slow resource recovery from a new blocker

The boots goal collected three raw iron while rejecting several deep approaches. Paused/exported `easy-boots-ore-stall.jsonl` (605 observations with frames, not truncated); healthy pick durability ruled out D031. Fresh inspection found only one remaining higher ore in the old scope and a dropped raw iron in the cave. The planner had already replanned to that drop. Resumed the bounded attempt: it descended from surface to `(259,54,473)`, recovered the required supply and entered smelting. Keep the expensive search/recovery as an efficiency observation; no new pathfinding patch was justified. Full build passed (`easy-supply-build.log`). Next survival capability is a bed/spawn point, using sheep/wool if available.

D033 evidence correction: the same cave raw-iron entity was still observed after smelting began. Required inventory was obtained, but its exact source may have been fallback ore rather than that drop; this is not proof of underwater item pickup. The boots goal completed and they were equipped. Started the remembered forest trip for sheep/wool with full health.

### D034 — Filtered entity search and first Easy combat

The forest stop had no sheep in the fixed 32-block observation. Extended `inspect_nearby_entities` with optional exact `entityTypeIds`, radius 1–128 (default 32), and maxResults 1–64 (default 32). It searches loaded client entities only and filters before nearest-first limiting; distant results require navigation into the existing interaction radius. This follows an actual wool/bed requirement and avoids repeated screenshots. Query-bound tests, filter-before-limit coverage, and existing orchestrator/inventory tests passed. The static tool catalog requires a normal save/restart to expose the new schema; live validation follows.

During implementation the survival reflex fought in the forest. Paused at world time 139,123, server tick 47,429. The recent recorder shows an aggro skeleton at 12.25 blocks triggering DEFEND, subsequent close-quarter attacks, arrow damage to 18.38 health, an attacking spider joining, string pickup, and threats_clear resolution. Health recovered to 20; saturation reached zero; armor and axe wear increased. No death or stuck reflex observed. `easy-first-night-recent.jsonl` contains 448 observations with frames; `easy-first-night.jsonl` contains 1,796. Both flag truncated because this session's droppedByType counters are nonempty; do not present them as lossless. These establish combat activity, not yet two full Easy cycles.

D034 live validation: after the normal restart, Easy persisted at world time 139,313. A radius-128 sheep-only query returned three sheep at 28.8, 75.7 and 125.6 blocks with exact selectors and positions (`sheep-query-live.txt`). Shearing the nearest completed, and navigation to its observed drop collected one white wool. Travel hunger reached 17; `eat_food` consumed the farm bread and subsequent inspection confirmed hunger 20 and saturation 6 (`second-sheep-travel-inventory.txt`). This is live crop-to-food-to-hunger recovery, not just recipe completion.

### D035 — Drowned ranged capability depends on its weapon

The wool return paused for DEFEND and diverted toward drowned detected at 33.93 blocks, followed by several others at 30–34 blocks. This was not an empty threat set: an initial filtered query omitted drowned. Paused at world time 144,051 and exported `home-return-drowned.jsonl` (3,205 observations with frames, not truncated). The last decision targeted a drowned below the water surface while the original home navigation remained held. Debug entity inspection found both remaining drowned with empty equipment (`drowned-equipment.txt`).

Root cause: `isRangedThreat` treated every `RangedAttackMob` as ranged, but Minecraft's DrownedEntity implements that interface regardless of equipment. Its actual TridentAttackGoal.canStart requires a main-hand trident. Added the same condition to threat classification, preserving the existing six-block melee threshold and ranged classification for trident carriers. Regression failed before the fix; reflex tests passed afterward. HotSwapped one class and resumed the same interrupted home route; live recovery pending.

D035 live result: the first tick after HotSwap resolved the distant drowned at server tick 4,910. Subsequent drowned were admitted at 5.98–6.00 blocks, confirming the melee boundary live. Close encounters and air recovery continued; do not claim the home route completed. Export `drowned-after-classification.jsonl` contains 1,223 observations, not truncated.

### D036 — Combat must retrieve an available weapon

The same encounter exposed another inventory limitation: `equipBestCombatHotbarItem` examined only slots 0–8. Shearing and other work had moved the iron sword to inventory slot 27; the reflex fought with an iron pickaxe despite carrying a fresh sword and stone axe. Pause/inspection (`combat-inventory-weapon*`) confirmed pick durability 129/250, untouched sword 250/250, health 17.025 and hunger 16. The ranged fix is separate from this equipment weakness.

Search all 36 carried inventory slots using the existing weapon ranking; prefer equal-ranked hotbar items, otherwise swap the selected weapon into the current hotbar slot. Resolve the screen slot by its backing inventory/index so an interrupted container task cannot cause an unrelated container slot swap. Added weaponItemId to attack events. The exact slot-27 regression failed before expanding the search; reflex tests passed afterward. HotSwapped one class. Eating pork restored full health; an air recovery hold then explicitly awaited the planner, and Codex resumed that exact hold. Weapon use in a subsequent live encounter remains pending.

### D037 — Bed, spawn point and repeat farming

The resumed wool return completed at client tick 7,681. The white-bed inventory goal succeeded using three wool from two sheep; it unnecessarily converted a log first despite having six planks for a three-plank recipe (`bed-plan-state.txt`, `bed-craft-inventory.txt`). This is confirmed excess crafting, not a missing-material claim; defer deeper recipe planning work until the bed and crop maintenance checkpoint is complete.

Normal coordinate placement from a deliberate stance placed the bed at foot `(257,63,480)`, head `(257,63,479)`, facing north. No new orientation API was needed. Using it in daylight produced the game's `Respawn point set` message (`bed-use-result.txt`, client log 04:38:16). Closed the shelter door and updated its world bookmark while retaining the protected bounds. The farm still had fourteen farmland and fourteen wheat cells, with four mature crops and five at age six (`farm-after-wool-trip.txt`). Starting another harvest/collect/replant pass in Easy.

### D038 — Revert a bed layout that blocks the door

The first repeat farm call failed before harvesting. A read-only inspection of its retained executor identified work goal `(259,63,481)`; an explicit navigation to that exact cell also failed. Removing the bed from `(257,63,480)` made the same route complete (`bed-exit-reproduction-*`, `bed-removed-exit-*`). This was a Codex construction mistake: the bed occupied the inside door approach under a low roof, not evidence for a new pathfinding patch.

A relocation attempt from an edge stance was rejected with player_hitbox_overlaps_target; moving to `(257,63,481)` allowed placement at foot `(256,63,480)`, head `(255,63,480)`, facing west. The revised bed's exit and return to `(257,63,480)` both completed, and using it in daylight reset the spawn point. Updated the world bookmark accordingly, preserving bounds and recording why the east column must stay clear. The second farming pass then completed six harvests/replants: wheat 2→8, seeds 6→15, all fourteen crop/farmland cells intact (`easy-farm-repeat-*`, `repeat-farm-{inventory,plants}.txt`).

### D039 — Reuse an observed workbench in recipe planning

The bed's excess log conversion came from `effectiveRecipeInputCounts`: it reserved four table planks whenever no table was in inventory, ignoring the placed table the executor would use. It also multiplied those setup planks by the number of recipe runs. Preserved `bed-excess-planks.jsonl` (280 observations; truncated flag set, so not lossless) plus the earlier route/inventory artifacts.

Expose the executor's existing usable-workbench search to observation. While resolving, publish a current-tick world-site fact for that table and actor; the planner can omit portable setup only while that observation is fresh. Scale recipe inputs by batch size, then add portable setup once if still required. Nearby-table and batch regressions failed before the fix; a stale-table test retained the setup requirement. Provider, crafting and action-runtime tests passed. HotSwapped three classes and started a two-bread batch beside the shelter table for live verification.

D039 live result: the two-bread goal succeeded with one `wheat_x3_to_bread` route step and no plank conversion (`nearby-table-bread-complete.txt`). Logs stayed at one and planks at seven. An immediate inventory observation briefly showed the earlier ingredient count; a settled read confirmed wheat 8→2 and bread 0→2 (`after-bread-settled-inventory.txt`). Avoid inferring duplication from an intermediate client inventory update.

The helmet goal exhausted the local ore scope with zero targets and zero broken blocks, repeating eight replans before terminal failure. Recorded as a bounded but wasteful depleted-scope retry, not a new excavation failure. A structured query of the remembered old workshop found four iron ore at `(205,95..96,457..459)` (`old-workshop-iron-query.txt`). Starting a trip there to search a fresh acquisition scope. Current recorder exports have a truncated flag from session drop counters; keep that limitation explicit.

D036 live verification: the next workshop trip triggered skeleton combat without any manual sword equip. Attack events at client ticks 19,354, 19,383, 19,395 and 19,415 explicitly recorded `weaponItemId=minecraft:iron_sword`; the threat resolved. Further encounters followed. At the next resolved hold, inventory showed the sword in hotbar slot 5 at 239/250 durability, health 20 and air 300 (`new-combat-events.txt`, `sword-combat-{state,inventory}.txt`). This proves inventory retrieval and actual attacks in the live world. Resumed the exact held workshop navigation.

### D040 — Complete the supply return and Easy checkpoint

The old-workshop scope contained eleven observed ore, and constrained acquisition collected five raw iron with full health (`fresh-iron-inventory.txt`). The supplied helmet goal succeeded, and equip verification showed all four iron armor slots. Its return encountered another hostile, recovered to full health, resumed the held route and arrived at the revised shelter bookmark. The tighter local monitor reads the current hold, waits for food use, and resumes only a resolved reflex; earlier stale-hold/item-use rejections were expected concurrency guards, not successful resumes.

Final inspections confirmed the closed door, intact bed, fourteen planted/hydrated farmland cells, full health/hunger and retained food/tools. Paused at world time 167,363 after 49,285 Easy simulation ticks. This completes the stated two-cycle capability checkpoint across the playtest/fix loop; it is not a claim of uninterrupted autonomous reliability or validation on harder difficulties. Keep Easy for the next session, address inventory capacity before extended gathering, and investigate remaining wasteful scope retries from their recorded evidence.

### D041 — Shield preparation and chest/logbook follow-up

The next requested capability is shield use against skeletons, followed by cave exploration rather than strip mining. Added carried-shield retrieval to offhand, facing/blocking drawn bows and incoming projectile trajectories, release between shots and on reflex cleanup, plus an explicit shield preparation preference in the planner prompt. Focused reflex/item-use/prompt tests passed and two runtime classes were HotSwapped without restarting. Live shield blocking is still pending; do not count shield-use events as proof of blocked damage.

The shield item goal at home failed in the previously depleted ore scope. Travel to the remembered iron workshop completed. Its supply route then left two raw-iron drops within 0.6 blocks while all storage slots were full. Paused and exported `run/playtest/2026-09-13/full-inventory.jsonl`; the resumed route later picked up ore and entered smelting without manual disposal. Drop requests were rejected by the active graph guard; nothing was discarded. A subsequent supply replan was paused/exported as `supply-replan.jsonl`. Both exports report truncation from recorder loss counters. The player remains paused while adding the user's requested chest inspection/transfer tools and an automatic world-persistent interaction logbook. These are prerequisites for the longer cave trip, not a change to the shield objective.

### D042 — Chest storage and automatic interaction history

Added `inspect_container`, `transfer_container` and `close_container` for ordinary open chests/barrels. Transfers preflight the full exact quantity, preserve component variants, fill partial stacks first, refuse stale screen IDs/nonempty cursors/insufficient space, and use normal screen clicks. Transfer submission is explicitly not completion; inspect settled counts. Mutation ownership remains behind the normal active-task, graph and survival gates.

Added server observations for container opening/clicks, crafting output and real item drops. Significant changes coalesce within a completed server tick, then append off-thread to `airicraft/interactions.jsonl` inside the world save. `read_logbook` is read-only, filtered by item, event and remembered place, and returns bounded recent history. Stock snapshots remain dated observations. Item queries retain later empty stock snapshots so old positive stock does not hide an observed depletion. Read/write ordering and world isolation have tests; normal server shutdown drains pending writes.

Full build passed. New mixin observation hooks required a normal saved-client restart. In the same survival world, the chest graph crafted one chest from the existing mixed plank supplies; the server logbook recorded exactly one crafted chest at the workbench. Placed home chest at `(256,63,481)`, leaving the door column clear. Deposited 96 cobblestone and withdrew 7: settled chest count 89 matched the persisted server snapshot. Seventeen further surplus deposits restored 20 free storage slots. Stale syncId and insufficient-source requests were rejected without changing stock. All 18 item counts in the live chest inspection exactly matched the final persisted snapshot. `chest-roundtrip.jsonl` contains 1,284 observations with frames and is not truncated. Close-tool loading, restart persistence, furnace/drop recording and shield combat remain to validate before cave exploration.

### D043 — Confirm persistence and repair open-furnace collection

A saved-client restart retained the chest and logbook. `read_logbook` returned the previous home stock; reopening the chest confirmed it, and `close_container` worked. A one-dirt drop produced one server-recorded `dropped` entry at home. Querying an item retains later container observations that no longer contain it, so historical positive stock is not the only result after depletion.

At the workshop, opening furnace `(202,92,469)` revealed three iron ingots and one coal. Confirmed untracked collection failed with `station_unavailable`: the executor converted an `#open_screen` station key to a world position, unlike smelting's existing open-screen path. Paused/exported `furnace-collect-failure.jsonl` (1,363 observations with frames, not truncated). The logbook correctly showed no withdrawal for the failed request.

Added exact dimension/screen-ID matching for collection from a confirmed open furnace, with stale-window rejection. Collection also refuses a nonempty cursor and reports `inventory_full` if quick-transfer leaves output behind, preserving the tracked process instead of falsely completing. The open-screen regression failed before the fix; smelting executor/planner/process tests passed afterward. HotSwapped one class. Retrying the same furnace recovered all three ingots, and `furnace-recovered-logbook.txt` shows a three-ingot withdrawal followed by a coal-only snapshot. The return navigation reported CALC_FAILED near the requested stance, but ordinary furnace use succeeded within range; record that as a bypassable waypoint issue, not another blocking pathfinding defect.

### D044 — Shield blocks live arrows and allows counterattacks

The shield inventory goal succeeded, and the automatic logbook recorded one crafted shield. Against a naturally encountered skeleton, the reflex raised the offhand shield for drawn bows/incoming arrows, then released it and closed for iron-sword attacks. Minecraft awarded `Not Today, Thank You` at client tick 8,461. Health stayed at 20 through the recorded cycles and final inspection; shield durability changed from 336 to 332. Attack events followed at ticks 8,543, 8,587, 8,600 and 8,612 before `threats_clear` at 8,613. This validates one live skeleton encounter, not perfect blocking under every timing or mixed-mob scenario.

Paused and exported `shield-combat.jsonl`: 3,074 observations with frames, not truncated. Baseline/final inventory and events are alongside it in `run/playtest/2026-09-13/`. The held navigation was reset by the later configuration reload after its incident had been saved.

### D045 — Compact visible cave survey and bounded return

Added read-only `survey_cave`: sparse first-hit rays return visible standing candidates, exposed ore and hazards, with light and explicit route uncertainty. The planner procedure uses remembered entrances/junctions, short movements, lighting and resupply; passage navigation disables Baritone breaking/placement. No hidden ore scan selects targets. The new provider loaded through factory HotSwap and normal configuration reload without restarting the client. Focused tests and the full build passed.

Surface observations returned water hazards and standing candidates. With breaking/placement disabled, navigation entered the existing underground workstation room at `(257,52,476)` but failed its final exact goal `(255,52,475)` with CALC_FAILED. Paused and exported `cave-endpoint.jsonl` (1,103 observations with frames, not truncated); screenshot shows the cramped table/furnace room. Surveys at radius 12 and 24 offered the same visible floor but no additional passage or exposed ore. A visible candidate is not a proven route: do not retry it blindly or label the reachable room a progression blocker.

The return to remembered `(263,65,471)` completed with excavation and placement still disabled. This validates a bounded passage visit and return, not natural-cave discovery or ore acquisition. Deeper natural-cave exploration remains the next live acceptance boundary.

Final checkpoint: return to shore shelter `(257,63,480)` also completed. Restored `allowBreak=true` and `allowPlace=true`, closed and inspected the door, and confirmed health/food 20, shield 332/336, five torches and fifteen free storage slots. `read_logbook` still returned the dated home chest stock (89 cobblestone and the other stored supplies). Paused under debug session `4bb2a732-8469-40f2-a162-1b7b2dbb5ad4`, epoch 1. `final-home.jsonl` contains 1,479 observations with frames, not truncated. The live client remains paused for the next session.

### D046 — Furnace insertion and cave resupply

Resumed the continuing playtest goal from the paused home checkpoint. Started charcoal process `smelt-process-fff382e7-e515-4b9b-ae82-3839e179ef9b` in the home furnace `(255,63,479)`, using two carried oak logs and two dark-oak planks as fuel. Server history recorded both exact insertions and the stock snapshot (`charcoal-insertion-logbook.txt`). Collection completed with two charcoal in inventory; `charcoal-collected-logbook.txt` records the withdrawal and empty furnace observation. This closes the prior furnace-insertion live evidence gap.

Crafted two charcoal-and-stick recipe runs, increasing torches from 5 to 13. Kept digging/placement disabled and began searching beyond the already visited workstation passage for a natural cave. The shoreline ledge only exposed water and steep dirt; continue from the remembered forest station instead of repeating its unhelpful survey points.

### D047 — Honor horizontal navigation instead of digging for Y

The forest search requested `(215,103,416)` with `exactY=false`. It arrived at `(215,97,416)` but reported CALC_FAILED. Paused immediately and exported `horizontal-arrival.jsonl` (2,841 observations with frames, not truncated), plus status/inventory/events. This freshly reproduces the older `exactY:false` contract defect: `startNavigate` always constructed `GoalBlock`, and completion only relaxed height by one block. Disabling excavation prevented this trial from digging toward the irrelevant Y.

Added an injected-facade regression using the observed coordinates. It failed against the original code. Horizontal goals now use `GoalXZ` and X/Z-only completion; exact goals retain `GoalBlock` and exact completion. Workbench standing positions and exact surface-return refinement explicitly retain their required Y. Near-interaction routes still use their existing three-dimensional `GoalNear`. Clarified both public tool descriptions. Focused navigation/crafting/surface tests and the full build passed.

HotSwapped three runtime classes. Retrying the original request completed at the same arrival point. A fresh 24-block northward request `(215,103,392,false)` then completed at `(215,93,392)` with health 20 and excavation/placement disabled (`horizontal-new-route-{status,inventory}.txt`). This verifies actual travel across elevation changes, not just completion reconciliation. Continuing the cave search along the hill edge.

### D048 — Natural cave descent needs a return plan

The following horizontal goal `(155,86,392,false)` reached a lush cave at `(155,24,392)` with health 20. Paused and exported `deep-descent.jsonl` (3,131 observations with frames, not truncated). Position samples and selected frames show a roughly 60-block planned fall from the snowy ledge near `(168,82,407)` into an existing water pool. No bucket placement or terrain excavation was required. This is a limitation of choosing an unrestricted horizontal destination for surface exploration, not a failure to honor that goal. Use observed exact-height stances around openings; do not infer a reversible route from a safe landing.

Remembered `lush cave arrival` with the descent warning and surface coordinates, then placed a torch at `(154,24,391)`. Visible surveys returned exposed coal/copper and water; no ore was collected yet. Began testing an exact return to `(185,86,392)` with digging/placement disabled. A zombie and skeleton were defeated; a later creeper exploded, with health recovering after food. Return remains unverified and navigation estimates grew substantially after combat interruption.

### D049 — Face the shooter and keep the shield raised through release

The elevated cave skeleton landed two arrows while the reflex was attempting to block. `elevated-skeleton.jsonl` preserves 3,684 observations with frames, not truncated. The first cycle lowered at tick 14,963, raised again for an incoming arrow at 14,981, and took damage at 14,986; another brief release/re-raise preceded damage at 15,104. Shield damage stayed at four. The trace establishes late re-raising; it does not by itself prove the arrow-facing aim caused either hit.

The user explicitly requested facing the shooting skeleton instead of snapping aim to arrows. Shield aiming now uses the shooter's horizontal direction at player eye level. If the shooter is unavailable, it uses the incoming velocity's general horizontal direction, never the projectile position. A bounded 20-tick commitment bridges bow release/temporary visibility gaps and renews while an incoming shot is observed; normal cleanup/cooldown/reflex resolution still releases it. Recorder decisions expose this commitment.

The recorded release-gap regression failed with the prior behavior and passed after the change, alongside direction tests. Full build passed. HotSwapped the runtime without restarting and resumed the current cave return. New live arrow-blocking acceptance is pending; do not reuse D044's earlier encounter as proof of this revision.

D049 live acceptance: a subsequent mixed encounter at about `(220,63,398)` cleared multiple zombies and skeleton `ea6f70f8-9c66-425e-8e60-38f3cdb4613b`. Shield events referenced the skeleton UUID, not projectile positions; damage increased from 4 to 8 at tick 18,555 and to 12 at 18,972. The only recorded damage in the encounter was one zombie hit (20→18.875), with health back to 20 before `threats_clear` at 18,985. Sword counterattacks continued between blocks. The resumed exact surface route completed at `(185,86,392)`, tick 19,840. D050 later found that combat had reset the requested no-break/no-place settings; this return included excavation.

The first export while running reported truncation during pagination despite no loss at its initial manifest. Paused and exported again: `shooter-facing-success-paused.jsonl` has 6,276 observations with frames, `truncated:false`. Use that artifact for replay; `shooter-facing-success-inventory.txt` and the event/status artifacts provide compact evidence. Remembered `lush cave surface return` and `lush cave lower exit` in the world, with the verified route and warning against the steep drop. Current pause session `1dcd33f3-0377-4806-bfa1-6a28ab215d21`, epoch 1. The continuing goal remains active: exposed-ore collection, lighting along the cave route, and food/resupply efficiency are still to playtest.

### D050 — Combat must preserve configured path restrictions

The next inventory check contradicted the claimed non-excavating return: cobblestone rose from 39 to 98 and the active iron pick lost 61 durability. Tool discovery showed `allowBreak=true` and `allowPlace=true`, but a later check found those descriptions were cached at initialization and cannot establish live values. Inventory/durability changes and the reproduced combat reset establish the excavation defect independently. The player was still paused at the surface checkpoint. Root cause: each combat pursuit/replan called `baritone.applySettings()`, resetting all runtime choices to startup defaults. The cave return succeeded, but it was not a verified all-natural route. Corrected both world bookmarks and the earlier acceptance text.

Extracted combat route submission at its existing facade boundary and added a regression for initial pursuit and target replan. It failed because both reset the settings. Removed that reset; startup initialization remains unchanged. Focused reflex/profile tests and the full build passed, and the runtime was HotSwapped without restart. Reapplied no-break/no-place explicitly. A new skeleton encounter cleared with cobblestone still 98 and both iron picks unchanged at 162/250 and 96/250. Export `combat-settings-after.jsonl` retained 6,235 observations with frames, not truncated. Direct settings readback after a new encounter remains to verify; the already excavated route is available to reuse.

### D051 — Read actual path settings instead of cached descriptions

`configure_pathfind` schemas embedded `Current:` values at static catalog initialization. Re-listing them after configuration returned the old values, making those descriptions unsuitable as runtime evidence. Added read-only `inspect_pathfind`: seven useful defaults or up to 32 explicit setting names, returning current/default serialized values. New schemas omit the misleading cached `Current:` text. Unknown/Java-only names are rejected without mutation.

Full build passed. HotSwapped the factory/settings classes and used normal configuration reload to register the provider without restarting. Reload reset the held task, so explicitly reapplied no-break/no-place and resubmitted the intended cave destination. `settings-readback-initial.txt` proves direct live values false/false. Existing cached schemas in this already-running classloader may still contain old descriptions; the new read tool is authoritative. Continuing toward the previously visible coal at `(154,30,400)`.

D050/D051 live readback: after the next creeper encounter and target pursuit, `inspect_pathfind` returned false for both `allowBreak` and `allowPlace` (`combat-settings-live-readback.txt`). Further combat and ore travel retained both values. This directly verifies the runtime setting retention; cached schema text is no longer used as evidence.

### D052 — Collect exposed cave ore with exact actions

Reached `(154,31,400)` through the available route, then surveyed visible faces. Placed a torch at `(152,32,402)` and broke coal `(154,30,401)`. A zombie displaced the player before pickup; a nearby item query located the coal at `(154,30,401)`, and exact navigation collected it. Coal rose from one to two. A subsequent exact batch broke the newly surveyed coal `(154,30,400)` and copper `(153,30,400..401)`. Its initial 40-tick wrapper wait expired, but the same job subsequently completed normally; inventory settled at coal three and raw copper seven. This is net two coal plus seven raw copper acquired using visible survey results, not hidden ore scans.

Water entered the mined pocket, and further combat interrupted collection of another observed coal. Stop this vein and return with the acquired supplies; avoid treating a completed break as completed pickup. The repeated manual approach/survey/break/pickup loop and dark return route remain efficiency targets. Automatic lighting currently covers mining, not general cave traversal.

### D053 — Block nearby priming creepers

Two close creeper blasts in succession reduced health to 11.940621. Paused and exported `creeper-close-blast.jsonl` (1,473 observations with frames, not truncated), with exact damage events. Eating bread began recovery. The shield reflex only handled projectile threats; vanilla source confirms its general frontal damage reduction and five-tick activation delay.

Extended shield selection to a visible creeper within its blast radius once its fuse is advancing past 30 percent. This takes priority over a bow threat, faces the creeper horizontally, records fuse progress when raising, and reuses bounded shield commitment/cleanup. Early/cooling or distant creepers retain normal combat behavior. The new policy regression failed before enabling the rule; reflex tests and full build passed afterward. HotSwapped without restart. A later skeleton encounter still blocked arrows and resolved, while creeper-blast acceptance for this revision remains pending. Returning toward the lower exit with acquired ore.

### D054 — Resupply at home; crop drops need their own pickup height

Returned through the existing cave route with net two coal and seven raw copper. Deposited both into the home chest along with surplus terrain materials. Settled chest inspection and the automatic copper logbook observation confirmed storage. Withdrew two wheat and five seeds for food production.

The farm task harvested its first crop, then failed `crop_pickup_approach_failed` before replanting. Paused and exported `farm-resupply-failure.jsonl` with frames (4,352 observations, not truncated). Remaining seeds were visibly queried at `(261.1,62.9,478.7)` in irrigation water, but the executor forced the crop walking height, targeting `(261,63,478)`. Exact navigation to `(261,62,478)` completed and collected all four seeds (five to nine), confirming the height mismatch. Pickup goals now use the actual item block height, adding one only when the drop occupies shortened farmland. Crop tests and full build passed; hot reloaded just the environment class.

The first retry from water failed immediately with `crop_has_no_reachable_work_position`. Its work-position search excludes farmland and requires full solid support. Paused and exported `farm-work-position-failure.jsonl` (4,024 observations, no frames, not truncated). Navigating to shore `(259,63,482)` allowed the same plot task to start and harvest/replant normally. Treat this separate start-position limitation as bypassable; do not broaden the height fix to redesign work-position selection.

The patched task harvested and replanted nine crops before encountering the same work-position limitation on the outer farm cells. Saved `farm-nine-replanted.jsonl` (3,916 observations, not truncated). This limitation can also occur mid-pass, so a shore starting position alone is insufficient for the whole plot. The nine successful replants are partial live acceptance, not a completed farm pass.

### D055 — Allow walking work positions on planted farmland

The second crop failure was an overly strict stance filter: it required air at feet and a full solid support, explicitly excluding farmland. Allow non-colliding, dry feet/head space over either full support or farmland. This permits standing among crops without adding a jump or terrain-edit action. Existing crop tests and full build passed; hot reloaded the same environment class.

Live retry `job-713e8daf-f9e7-48c0-84e0-9732838a033e` completed the full plot from the previously failing position. Exact inspection confirms 15 wheat plants (correcting the earlier estimate of 14), inventory 15 wheat and 31 seeds. Returned home and crafted five bread; the read-only logbook recorded five actual crafting events at the home table `(255,63,481)`. Closed the shelter door and paused. `farm-complete.jsonl` contains 3,892 observations with frames, not truncated; settled crop state, terminal status and bread logbook are alongside it. Both crop fixes are committed separately. Further automatic cave lighting and live creeper-blast shield acceptance remain open.

### D056 — Light cave travel without displacing the shield

Enabled darkness lighting at threshold seven, underground only, spacing six. Traveled from home through the remembered cave surface return toward `(220,63,398)`, with `allowBreak=false` and `allowPlace=false` confirmed by direct readback. Survey at `(204,83,411)` returned light zero while all eleven torches remained carried and the shield occupied offhand. Paused and exported `navigation-dark.jsonl` (4,620 observations with frames, not truncated). Source matched the live gap: the lighting tick only accepted mining and refused an occupied non-torch offhand.

Extended supported work to navigation and mining. Use an existing offhand torch when present; otherwise temporarily select/swap a carried torch into main hand and restore the previous slot/stack in a finally block. Skip active item use, block breaking and open/cursor-bearing containers; survival hold already owns the outer tick. Candidates must have a dry target and reachable, visible support face. Placement events report actual activity and remaining total torches; semantic aggregation retains the latest count. Focused lighting/projector tests and full build passed; HotSwapped without restart or configuration reload.

After resolving two zombie encounters, resumed the same navigation job. Seven automatic placements consumed eleven torches down to four. Recorder snapshots throughout ticks 17197–17379 show the iron sword restored in slot zero after every observed placement and health twenty; settled inventory confirms shield still offhand. Exact world query confirms wall torch `(216,67,401)` facing east. The next mixed creeper/skeleton/zombie encounter paused navigation normally. Paused and exported `navigation-lit.jsonl` (4,992 observations with frames, not truncated). Main-hand restoration and live navigation lighting are verified; the route destination was still interrupted by combat at export, not completed.

The new creeper guard was selected at fuse progress 0.3214 during that encounter. This proves threat selection/raising, but no explicit explosion evidence yet proves blast blocking. Later arrows/melee caused some damage; do not claim perfect shield coverage. Continue combat and resupply before extending the cave route.

### D057 — Rule out a permanent shield/melee stall

A nearby skeleton still had twenty health after several shield cycles, so paused and exported `close-skeleton-stall.jsonl` (5,164 observations with frames, not truncated). The history changes the diagnosis: for most of the encounter it remained roughly five to thirteen blocks away, moving around terrain while the player blocked shots. At melee distance the player attacked at ticks 18571, 18583, 18632 and 18644, then `threats_clear` resolved at 18646 with health unchanged at 16.37. The close snapshot was not proof of permanent attack starvation. No speculative combat edit. Log the drawn-out approach as an efficiency concern; keeping ranged protection while closing terrain-heavy distances still needs broader acceptance.

Ate bread during a resolved hold, then a second bread for recovery. Fresh path setting readback remains false/false after further combat. Encounters can change hold IDs between a status read and the next action; retry only from current state, never reuse a stale hold or force normal work through an active reflex.

### D058 — Complete the lighting trip and restock

The original lower-exit navigation eventually completed after multiple resolved survival holds. The exact ascent to `(185,86,392)` then completed without further interruption, followed by the exact home goal `(257,63,480)`. Direct path settings stayed false/false. Saved the world-persistent `lit cave descent` bookmark with the existing passage, torch placement and lower-cave encounter warning. All eleven starting torches were consumed across navigation segments, including the route back from combat displacement; this was not eleven torches spent on one short staircase.

At home, withdrew the two stored coal and combined them with one carried coal. Crafted four sticks, then twelve torches. Deposited twenty-one surplus seeds, three string, ten rotten flesh and one spider eye; settled chest inspection confirms seeds31/string3/rotten_flesh10/spider_eye2 and no coal remaining. The single chest now has all 27 slots occupied. Kept ten seeds, two bread, twelve torches, both iron picks and the shield (173 durability remaining at the end of the cave segment). Closed the door and paused at home: health20, food19. `lighting-trip-home.jsonl` has 7,180 observations with frames and no truncation; final inventory, settled chest and coal logbook artifacts are alongside it.

Next practical preparation: expand storage and replenish food before another long cave outing. The new temporary-main-hand lighting path has live evidence for inventory-to-hotbar swapping/restoration; subsequent torches are now in a hotbar slot, so the selection-only branch should be observed on a future trip. Creeper fuse guard activation is live-observed, but confirmed blast blocking remains open.

### D059 — Find craftable outputs hidden by the recipe limit; add storage

The home chest was full. Planned a barrel recessed at `(256,64,482)` so the existing chest lid and doorway stay clear. Converted the carried pale oak log into four planks and withdrew two stored pale oak slabs; combined with two dark oak planks this satisfies vanilla's six-plank/two-slab barrel recipe. `check_craftables` omitted the barrel, even on repeated reads. Paused and exported `mixed-plank-barrel.jsonl` (7,160 observations, no frames, not truncated).

The mixed-material hypothesis was rejected: the local vanilla recipe accepts plank/slab tags, and a bounded read-only Arthas watch showed eight correct grid placements and multiple valid mixed layouts. The actual issue was the response's silent per-category limit of 24, consumed in part by duplicate permutations with the same recipe ID. Deduplicate IDs before limiting, add optional exact `outputItemId` filtering, and return matched/returned counts plus `truncated`. Regression tests cover duplicate crowding, filtering after more than 24 other outputs, explicit truncation and no matches. Inventory/orchestrator/catalog tests and full build passed.

The existing client caches static tool schemas, so this change required one client restart. Used the JVM's integrated-server shutdown hook, rejoined the same world, verified the exact material inventory persisted, and reapplied no-break/no-place plus lighting policy. New query `{"outputItemId":"minecraft:barrel"}` returned exactly one recipe; the actual craft completed using two dark oak planks, four pale oak planks and two slabs. No recipe-matching change was needed.

Removed the one observed stone wall block and placed the barrel with solid backing intact. Exact reads verified it and clear air above the chest. Opened its 27 slots, deposited a spare furnace, two arrows, three cobblestone and one bread, then verified settled contents and closed it. Bread logbook entries identify the barrel at `(256,64,482)`. Home now has 54 storage slots. Artifacts: `barrel-filtered-craftables.txt`, `barrel-craft-result.txt`, `barrel-deposit-settled.txt`, and `barrel-bread-logbook.txt`.

### D060 — Bypass a container reopened during navigation

The farm retry timed out in HARVEST without changing the first mature crop. Paused/exported `farm-harvest-stall.jsonl` (425 observations with frames, not truncated). Initial position-loss speculation was wrong: recorder snapshots show the player stationary. Instead the screen changed from none to `GenericContainerScreen` during the outbound NAVIGATE_TO job at tick2897, after the barrel had already been closed at tick2177. `inspect_container` confirmed the same barrel contents under a new sync ID3 with an empty cursor. The crop environment rejects harvesting with a container open.

Closed the empty-cursor container and retried from the same location. The full pass completed immediately: harvested1/planted1/growing14/missingSeedPlots0. This is a verified bypassable navigation/UI interaction problem; the exact input ordering that reopened the barrel is not yet proven. No speculative crop state-machine edit. Only one wheat was mature, so use nearby pigs for immediate food while the remaining crops grow.

### D061 — Hunt, cook with spare wood, and preserve a food reserve

Two observed pigs were killed through `attack_entity`; both jobs completed with `target_died`. The first porkchop required a separate short approach to its queried drop. The second hunt's three porkchops were picked up after the kill completed; the earlier one-pork inventory was timing, not lost loot. Returned home with four raw porkchops and ate the last carried bread. The stored bread remained in the barrel.

With no coal left at home, withdrew two spare dark oak doors and three pale oak slabs. `check_smeltables` showed each fuel type could cook two, but no single type could cook all four. The current tool selects one fuel type per batch. Used two batches of two; both collected successfully, producing four cooked porkchops. This is a bypassable batch-planning limitation, not a progression blocker. No smelting code change.

Deposited one cooked porkchop into the overflow barrel and confirmed settled counts: one bread plus one porkchop stored, three porkchops carried. `read_logbook` reports both furnace collection and the barrel deposit/observed contents at `(256,64,482)`. Health20/food20, shield equipped, twelve torches and both iron picks retained. Evidence: `cooked-pork-inventory.txt`, `barrel-pork-settled.txt`, and `pork-home-logbook.txt` under `run/playtest/2026-09-13/`.

### D062 — Hotbar lighting, shielded blasts, and changed cave footing

Returned to the cave surface entrance with sword slot0, torches slot2 and shield offhand. Automatic placement at `(187,86,393)` consumed twelve to eleven torches at tick15071; snapshots immediately before/after retain sword slot0. A second placement on a short lower-cave navigation consumed eleven to ten. The hotbar selection-only lighting branch now has live evidence, complementing D056's inventory-swap branch.

Descent job `job-5753dd19-bf18-4d2b-924c-2d7c50f82cd5` failed `CALC_FAILED` at15888 after reaching `(214,76,403)`. Paused and exported `cave-return-calc-failed.jsonl` (3,568 observations with frames, not truncated). Movement correctly stopped at exactly the same position through16362. A later approaching creeper activated defense at16370 and caused the subsequent movement; no post-failure movement ownership bug is demonstrated. Cause of this initial path failure remains unresolved.

Several creepers and ordinary melee mobs then engaged. The second creeper guard raised at17516 with fuse0.3214 and shield damage174, lowered at17536 with damage185 and unchanged health14.928749, and resolved `threats_clear`. Subsequent isolated creeper guards increased shield damage185→195 and195→204 without health loss. This is live evidence of blast absorption, beyond merely selecting the fuse guard. The first encounter's unchanged shield durability alone was inconclusive. Ate one porkchop between encounters and recovered health20. `creeper-shield-bounded.jsonl` retains the first two encounters (543 observations with frames, not truncated); the earlier full-window `creeper-shield-live.jsonl` reported truncation and must not be treated as complete.

A survey-derived coal approach to `(230,62,409)` failed after another blast. Paused/exported `coal-approach-failed.jsonl` (94 observations, not truncated); exact inspection found air at its required support `(230,61,409)`. Fresh supported waypoints `(230,62,406)` and `(231,62,409)` both completed without terrain edits. A new visible survey exposed coal `(231,63,410)`; exact breaking collected one coal. Thus this later failure was a stale standing candidate after a world change, not evidence of broken navigation. The lower-exit return `(220,63,398)` completed as `job-8addfb31-7802-4db9-8c05-912602363a50`.

Combat and blast-altered footing made this a poor resource trip: only one coal acquired, four cobblestone from a blast plus another later pickup, and one dropped iron shovel. Shield/armor remain usable; ten torches and two porkchops carried at the last inventory check. No speculative pathfinding or combat patch. Return home before extending exploration.

### D063 — Stop a large navigation detour and retrace with deliberate footholds

The surface goal `(185,86,392)` from the lower cave took a large descending detour. At tick22088 the same job was still RUNNING at `(320,-6,296)`, estimated remaining cost had risen from roughly280 to1926 ticks, and all remaining torches were used. Recorder snapshots show reflex IDLE throughout this leg. Paused/exported `ascent-deep-detour.jsonl` (4,516 observations with frames, not truncated) and canceled the goal. This proves an unsuitable route for the intended return, not an A* correctness defect. The driver also skipped the short return waypoints prescribed in the cave procedure; do not make System 1 infer contextual "surface" intent from coordinates.

Used grounded positions retained in the recorder to retrace: `(317,-5,313)`, `(318,-5,328)` and `(324,-2,342)` all completed. The climb toward `(322,13,351)` failed under no-placement travel. Paused/exported `retrace-climb-failed.jsonl` (225 observations). Enabling placement alone also failed (`retrace-place-only-failed.jsonl`, 114 observations): Baritone inventory access was false and dirt/cobblestone were outside the hotbar. Enabling inventory access for this climb completed it with two dirt consumed, no excavation. Restored both settings. A second short climb to `(320,21,346)` completed with two more dirt after temporarily allowing placement/inventory again. Restored both after combat cleared. These are deliberate capability changes for the local terrain, not source patches or proof of a fundamental blocker.

Converted the one coal plus one stick into four torches during recovery; those were consumed while retracing. Saved the visible deep iron discovery as world bookmark `deep detour exposed iron`, explicitly noting the unvalidated return route; no iron was mined. A fresh skeleton encounter ended at health20 while shield damage increased by four. `skeleton-shooter-heading.jsonl` (535 observations with frames, not truncated) preserves it. `retrace-footholds.jsonl` (1,334 observations with frames, not truncated) preserves the successful climbs. At the high checkpoint: health20/food20, one porkchop, shield112 durability, sword62, dirt12, cobblestone37, no torches. Return remained in progress; subsequent waypoints `(306,18,355)`, `(293,12,360)` and `(276,14,371)` completed normally.

The remaining return succeeded by following the recorded route, allowing placement/inventory for the upward sections. One canceled waypoint entered the water landing at `(198,36,392)`; paused/exported `retrace-cancel.jsonl` (274 observations). A freshly surveyed dry stance `(196,37,393)` completed, followed by `(199,59,391)` and `(200,61,390)`. Returned to the lower exit, then explicitly followed `(216,67,401)` → `(214,77,405)` → `(204,83,411)` → `(185,86,392)`. The final surface job `job-a3bd4d3a-8326-4e61-ad56-0ab7088755d9` completed at33577. Restored and read back allowBreak/allowPlace/allowInventory all false. Updated the world-persistent `lit cave descent` note with these waypoints and the observed foothold requirement. The earlier generic no-placement ascent should not be repeated; no production navigation patch was justified by these recoverable failures.

Home navigation `job-78b647f8-c8c4-4bc4-9476-fa286fa38d95` completed at34492. Deposited seven rotten flesh and the dropped iron shovel into the barrel; settled slots and the shovel logbook confirm it. Closed the door, equipped the sword and paused at home tick35817. Final health20/food20/saturation12.8, one porkchop carried plus one porkchop/one bread stored, one torch, sword49/shield106 durability, dirt8/cobblestone37. Eight dirt were used across the whole recovery; no cobblestone was consumed. `detour-home.jsonl` has 5,153 observations with frames, not truncated. Next preparation: replenish torches and food and replace the worn sword before another outing. The broad playtest goal remains ongoing.

### D064 — Surface acquisition must admit the top water layer

Crafted a fresh iron sword from the last two carried ingots and stick. The next farm pass completed, harvesting six wheat; crafted two bread. Departing the shelter reopened the chest, repeating D060 (132-observation `chest-departure-recurrence.jsonl`, not truncated). Used the established close-container workaround after withdrawing four oak saplings and two bones for renewable wood preparation. Camera source inspection ruled out a persistent `lookAtNow` motion lock; exact cause of the unwanted open remains unproven.

At the shore oak tree near `(279,63,478)`, a bounded eight-log surface-only job broke three logs, then failed `acquisition_scope_left` while swimming at `(278,62,478)`. Paused/exported `wood-surface-scope.jsonl` (262 observations with frames, not truncated). Exact column inspection shows water at Y61..62 and air above. `SurfaceTerrain.groundY` correctly returns the top water block, but the acquisition check added one for feet/drop positions, excluding ordinary surface swimming. A one-log retry from that same water position failed immediately with itemCount2 before any action.

Keep the existing ground scan and coordinate bounds. Admit feet/floating drops at the top water layer; solid ground still requires the block above, and deeper water remains excluded. The focused regression failed before the change; surface-boundary/acquisition tests and full build passed afterward. Hot reloaded only `SurfaceTerrain` and `MinecraftAcquisitionEnvironment`, without restart/reset. The identical one-log request from the unchanged water position completed as `job-11ecd2a1-57bb-443b-b29d-da021e7fec93` at39704; inventory rose two to three oak logs. Evidence: `surface-water-before.txt`, `surface-water-after.txt`, `surface-water-after-inventory.txt`, `surface-water-fixed.jsonl` (113 observations with frames, not truncated), and red/green/build logs. This proves the water-boundary fix, not completion of the original eight-log batch.

### D065 — Wood collection, replanting and charcoal resupply

With the water fix live, a five-log follow-up in the same bounded surface scope completed (`job-51e5ea54-cda5-4d2a-b528-71048728342c`), raising oak logs three to eight. Placement/inventory access were temporarily allowed for tree access, then restored false; digging remained false. Cleared the final observed overhead log with an exact break once within range and collected it, for nine logs total. Replanted oak at `(279,63,478)`. Six bone meal advanced the sapling to stage1, but it still had not grown at the final check. Saved `shore replanted oak` in world memory with that limitation. Replanting is verified; a repeatable grown-tree harvest cycle is not yet verified.

At home, converted three logs into twelve planks. Smelted the remaining six logs with four explicitly selected planks. Process `smelt-process-293e0049-6c02-4c82-b235-735ac8e7e469` yielded all six charcoal; collection completed at47578. Combined the existing stick with four newly crafted sticks and five charcoal to make twenty torches. Stored the remaining charcoal in the barrel as next-batch fuel. Settled inventory and the charcoal logbook confirm the chain.

The smelting preview overcounted available logs: with six logs and twelve planks, it offered maxInputQuantity6 and autoFuelForMaxInput oak_logx4. This needs ten logs, while only six exist. Source inspection shows preview selection does not reserve inputs; executor selection also precedes input transfer. Automatic execution with overlapping input/fuel was not attempted here, so only the preview inconsistency is live-confirmed. Explicit plank fuel bypassed it. Preserve `charcoal-options-before.txt`; reproduce automatic execution before claiming its failure or patching that path.

Deposited the worn sword, charcoal1, oak_leaves1, oak_sapling3 and wheat_seeds11 into the barrel. Settled inventory proves the remaining carried sword is the fresh 250-durability one, then equipped it. Closed the shelter door and paused at50003: health20/food20, bread2/porkchop1, torches20, planks6, dirt8/cobblestone37, shield106, both iron picks retained. The barrel additionally keeps bread1/porkchop1, spare tools and the new charcoal reserve. Evidence: `charcoal-barrel-settled.txt`, `charcoal-logbook.txt`, `charcoal-home-inventory.txt`, and `charcoal-home.jsonl` (2,209 observations with frames, not truncated). Continue the broad playtest with fuel-overlap diagnosis and more productive cave exploration; tree growth remains pending.

### D066 — Reserve smelting inputs before spending fuel

Collected three dark oak logs in the bounded shore forest (`job-e5c67b08-000f-4414-bd6d-e82f4140180d`, completed53100) and returned home. Stored two logs, leaving one log and six planks carried. The empty-furnace preview recommended that sole log as fuel. Automatic execution `job-3832ead4-610f-428d-8390-96a619cc0eb2` moved it into the furnace and failed `insufficient_input` at54288. Paused at54301, server54026. The server logbook confirms the one-log furnace deposit; inventory has no log. `fuel-overlap.jsonl` preserves 46 observations with frames, not truncated. This confirms the execution defect previously left open in D065.

Reserve the input still needed from inventory before fuel selection, accounting for matching inputs already in the furnace. Apply the same budget to automatic selection, existing-fuel top-ups, manual requests and the preview. Check input availability before moving fuel. Keep existing fuel ranking and single-fuel batches. Three regressions failed under the old budget, then all smelting tests and the full build passed. HotSwap updated six classes in the existing client; no restart or world reset.

Recovered the misplaced fuel by withdrawing one stored log and cooking it with the already inserted log. Collected one charcoal and stored it, bringing the barrel reserve to two. Withdrew the final stored log and waited for the furnace to stop burning. A manual request using the sole log as both input and fuel now failed `insufficient_fuel` before moving anything (`job-8ea5474f-818e-45e5-8fef-bfaafb9f7b30`); settled inventory retained the log and all six planks. The updated preview recommended one plank. The identical automatic one-log request completed (`job-5d24af69-8850-40c0-9c3e-8747b1c90431`,55672), and collection completed (`job-8a6ff1db-2eb6-44a6-8990-1fd596a12366`,55874). Inventory has one charcoal, no log and five planks. The server logbook independently records the plank/input deposits and charcoal withdrawal. This proves empty-furnace automatic selection and manual overlap rejection live; existing-input/top-up accounting has source/test coverage only.

Evidence under `run/playtest/2026-09-13/`: `fuel-overlap-status.txt`, `fuel-reservation-preview-empty.txt`, `fuel-reservation-manual-{status,inventory}.txt`, `fuel-reservation-{auto-status,collected,inventory,logbook}.txt`, red/green/build logs, and `fuel-reservation-fixed.jsonl` (300 observations with frames, not truncated). Ate one bread, equipped the fresh sword, closed the door and paused at56503: health20/food20/saturation6; bread1/porkchop1, charcoal1, torches18, planks5, shield106 remain carried. Barrel keeps charcoal2 plus bread1/porkchop1. Continue with cave exploration and resource gathering; the broader goal remains active.

### D067 — Survey-led coal trip and stale return coordinates

Returned to the established cave with eighteen torches, full health and a shield. Used `survey_cave` first-hit observations, exact block breaks, and fresh surveys of exposed faces to mine thirteen coal ore blocks: three in the upper shaft and ten near the lower landing. Eleven coal were collected; two drops were left uncollected after failed exact pickup approaches. No hidden-ore search was used to select mining targets. Local terrain reads were used to diagnose footing/headroom, and not to extend the target list. The lower encounter resolved at full health and yielded two arrows/two bones. No iron was found on this leg.

Upper roof access required a short foothold with four dirt; exact breaks then succeeded. The lower vein supported batches of four, two and three blocks, but drop collection needed an explicit pit approach. One goal at `(217,61,395)` collected eight coal at once, raising inventory three to eleven. This exposes an efficiency gap rather than an incorrect break result: the planner coordinates survey, approach, exact break, resurvey and pickup manually. General acquisition already owns approach/pickup, but its loaded-block search includes buried targets. A follow-up should offer observed/visible target acquisition through that existing executor, keeping the buried-resource capability as an explicit alternative. Avoid a second cave-only mining executor.

The return goal `(216,67,401)` failed even with placement and inventory access allowed. Paused at68467/server68193 and exported `coal-survey-trip.jsonl` (1,851 observations with frames, not truncated). Exact inspection found that the remembered feet coordinate is solid dirt, with clear air above. The adjacent stone step `(215,67,400)` and then `(216,68,401)` both completed, followed by `(214,77,405)`, `(204,83,411)` and surface `(185,86,392)` at69008. This verifies a stale waypoint and its bypass, not an A* bug. Updated the world-persistent `lit cave descent` and new `lower landing coal vein` notes with the successful geometry and remaining surveyed faces. Restored allowPlace/allowInventory false; digging stayed false.

Home navigation completed at70185. The trip spent eight dirt total (four for roof access, four during return) and ten torches; cobblestone stayed37. This is a resource-gathering success, not an efficiency acceptance: the full departure-to-final-home checkpoint took about12.6 minutes of simulation time for eleven collected coal. The returned shield has94 durability, sword242, and picks78/161. Ate the last carried bread; health20/food20/saturation6. No food or health failure occurred.

Crafted four sticks, then sixteen torches, finishing with24 torches. Although the chosen recipe ID was `coal_and_stick_to_torch`, vanilla's alternative ingredient selection consumed three coal plus the carried charcoal; settled inventory proves coal11→8 and charcoal1→0. Record actual consumption rather than infer it from the recipe label; exact ingredient-choice guarantees are not established. Deposited coal8, arrows2 and bones2 into the barrel. Settled storage and the automatic logbook confirm coal8/charcoal2/arrows4/bones2 plus the existing bread1/porkchop1. One cooked porkchop remains carried. Closed the door, equipped the sword and paused at71623. Evidence: `coal-home-{barrel,inventory,logbook,pause}.txt` and `coal-return-fixed.jsonl` (1,459 observations with frames, not truncated). Next work: reduce observed-ore acquisition overhead, then seek iron for shield replacement; farming/food resupply and filler replenishment remain needed before a long outing.

### D068 — Let System 1 acquire visible resources

D067 spent roughly 12.6 simulation minutes collecting eleven coal, with many planner-controlled approaches and pickup moves. Added optional `constraints.visibleOnly` to `collect_resource`, `mine_blocks` and `ensure_blocks_in_inventory`. The default remains false, preserving loaded-block discovery for explicit buried-resource tasks. When true, the existing acquisition executor selects block sources using the same 495 first-hit rays as `survey_cave`, within 24 blocks of the current eye, then applies its fixed bounds. It re-samples at selection, including after mining/pickup movement. Matching drops remain eligible in scope. Scope, rejection budgets, approach, break, pickup and interruption ownership remain in the existing executor. No second cave-mining runtime.

Shared the ray sampler with cave surveys. Tests cover an occluder hiding a rear block, that block appearing after the occluder is removed, ray count/distance, flag validation on all three tool surfaces, and preserving the flag when anchoring constraints. The new flag rejects non-boolean JSON instead of silently falling back to unrestricted discovery. Focused tests passed. Full build initially hit an unrelated dashboard HTTP connection failure; the isolated dashboard test and subsequent full build passed. One client restart was necessary for the startup-cached tool schemas. Rejoined the same world, verified persisted inventory and new tool schemas, then restored no-break/no-place travel and lighting settings.

Withdrew sixteen dirt for footholds, then followed the corrected cave route to `(218,62,394)`. Fresh survey showed coal at `(215,61,396)` and `(215,62,397)`. Allowed local path excavation for acquisition only, then submitted one three-coal request centered `(217,63,392)`, radius8/verticalRadius6, visibleOnly=true. Job `job-e08fff7f-f7e0-4da1-94f7-d2c21243bc71` selected the first surveyed face, approached its drop, mined the second face, then mined newly exposed `(215,61,397)`. It collected all three coal with zero rejected targets and no manual movement calls during acquisition. Runtime ran from tick 3586 to 3738, about 7.6 simulation seconds. Pick durability decreased by three; dirt16 and cobblestone37 remained unchanged. This case did not need additional headroom excavation, so that branch is not newly live-proven here.

Restored allowBreak=false and paused at3979/server3646, health20/food18. `visible-acquisition-live.jsonl` contains 233 observations with frames, not truncated, including selected targets and pickup phases. Other evidence: `visible-vein-before.txt`, `visible-acquisition-{result,inventory,pause}.txt`, focused test log, initial build failure, dashboard retry and successful full-build retry. The result verifies one visible coal-vein acquisition; it does not prove arbitrary cave exploration, every resource type, or long-run survival efficiency. Next: return/replenish food, then seek iron for shield replacement using the same bounded visible discovery.


### D069 — Protect held item use from Baritone inventory housekeeping

After D068, `eat_food cooked_porkchop` was accepted at3995 with hunger18. The player remained stationary: recorder snapshots show pork in selected slot0 at4000, then the iron pickaxe in that same slot at4019/4040/4061. `food.eat_failed consume_timeout` followed at4075. Navigation began afterward, ruling it out as the interrupt. An attempted equip during eating was correctly rejected `item_use_active`. `eat-before-navigation.jsonl` preserves 1,489 observations with frames, not truncated.

Baritone `InventoryBehavior.onTick` automatically moves the best stored pickaxe into hotbar0 whenever allowInventory is enabled, including while using an item. Selecting food from inventory swaps the pick into storage, making that housekeeping immediately eligible. The stationary home control succeeded from slot6, consuming the pork; it does not prove slot0 works. Stored the spare 161-durability pick in the home barrel and withdrew its pork reserve to prepare a single-pick slot0 retry. Hunger filled during the control, so that exact repeat remains pending natural hunger loss.

Added a narrow Baritone mixin that skips automatic inventory housekeeping during active item use. It leaves ordinary housekeeping and explicit path inventory operations alone, and preserves OUT tick handling. This avoids reserving special food slots or disabling inventory access globally; eating, shield blocking and other held uses own their item for their duration. Full build passed (`item-use-guard-build.log`). A new mixin requires client startup, so a same-world restart is necessary; post-change live consumption remains pending.

While replenishing supplies, the farm pass completed at10025, harvesting seven wheat and replanting. Crafted two bread, leaving wheat2. Workshop navigation to the furnace coordinate `(202,93,469)` failed, as expected for an occupied feet position. Paused/exported `workshop-furnace-target.jsonl` (146 observations with frames, not truncated); inspection confirmed the furnace, and adjacent supported `(202,93,468)` completed at12271. Nearby visible surveys found no iron; the old ore note must not be treated as current stock.

At the forest cooking station, one four-log surface/visible-only acquisition completed at13282 (`job-824040eb-d217-4434-899d-ed8bce8b04db`), recovering from one rejected approach without planner movement. Returned home at14086 with four dark oak logs; axe durability79→75, dirt15/cobble37 unchanged. `visible-wood.jsonl` has380 observations with frames, not truncated. This extends visible acquisition proof to one wood batch. Before restart: health20/food20, bread2/pork1, torch16, coal3, shield94, carried pick75 and stored pick161.

Restart/rejoin succeeded with persisted inventory and the new mixin. Restored travel restrictions and lighting. The session was already hosting LAN; an extra `open-lan` call returned an internal error wrapping `LAN hosting requires SINGLEPLAYER_LOCAL`, so no retry or networking patch was needed. Converted two logs into eight dark oak planks, retained two logs, equipped the sword and verified the shelter door closed. Updated the world coal bookmark to remove its now-mined remaining faces. Paused at2055; `item-use-guard-restarted.jsonl` contains234 observations with frames, not truncated, and companion status/inventory files establish startup/persistence only. Hunger20/saturation8.8 remains too high for the ordinary-food regression. Keep that live acceptance pending; next outing should test pork from selected slot0 with allowInventory=true once hungry, then restore false. The 161-durability spare pick remains in the barrel so the 75-durability carried pick can be selected unambiguously.


D069 live acceptance: during the next cave trip hunger fell to18. Paused at11875, canceled navigation, selected the sole carried iron pickaxe in hotbar0, and enabled allowInventory. Pork was outside the hotbar. Resumed and called eat_food without any further mutations until the observation pause. Snapshots at11896/11916 retain pork in selected slot0; `food.eaten consumed` arrived at11924, then the pickaxe returned to0 at11937. Hunger18→20, pork1→0, position stayed `(198,36,386)`. This verifies both uninterrupted slot0 consumption and normal housekeeping afterward. `eat-slot-zero-fixed.jsonl` contains60 observations with frames, not truncated; before/after inventory and pause files preserve the setup. The earlier live acceptance gap is closed.


### D070 — Explore a cave branch, verify eating, and retrieve remembered iron

Followed the established short descent with no path digging/placement, then surveyed an east chamber through `(225,64,401)`, `(235,63,400)`, `(241,63,400)`, `(246,63,396)` and `(263,61,395)`. Tried the north bend to `(265,63,384)` and the south floor through `(265,60,405)` to `(273,59,400)`. Surveys found coal/copper, no iron. These are bounded observations, not proof of absence. Saved the route and tried branches in world bookmark `east cave chamber`. Small visible steps around bends required many planner calls; this remains a traversal-efficiency concern, not a demonstrated pathfinding defect.

Switched to the historically observed deep iron location using D063's recorded return stances. Hunger dropped during travel; the stopped, same-slot eating test verified D069 as recorded above. Crafted twelve torches from three coal and newly crafted sticks, taking eight carried torches to twenty.

A mixed encounter interrupted travel toward `(276,14,371)` at13001. Two skeletons and four zombies were detected; twenty sword attacks and eight four-durability shield blocks occurred before `threats_clear` at14009. Fourteen damage events: thirteen attributed to zombies and one unattributed; no confirmed arrow damage. Health ended10.883333, hunger17, shield62 durability. Shield holds separated several melee attacks by roughly50 ticks while nearby zombies kept hitting; preserve this as a mixed-threat efficiency issue, not proof that shooter-facing arrow protection failed. Paused/exported `deep-iron-combat.jsonl` (1,297 observations with frames, not truncated). Ate both bread to recover full health, resumed the exact safety hold, and reached `(276,14,371)`. Food reserve was exhausted. Future trips need substantially more food and torches; the driver underestimated supply demand.

Continued through `(293,12,360)` → `(306,18,355)` → `(320,21,346)` → `(322,13,351)` → `(324,-2,342)` → `(318,-5,328)` → `(317,-5,313)` → `(320,-6,296)`. A brief additional encounter cleared at full health and its exact hold was resumed. No path digging or placement was enabled. Reduced lighting threshold to fully dark positions with spacing8 to conserve remaining torches; nevertheless none remained on arrival.

Fresh survey confirmed the three remembered deepslate iron faces. A single visibleOnly three-raw-iron acquisition, radius6/verticalRadius4, completed as `job-2c6b44a2-d347-4f22-9d14-fce244be4505` at19795, starting19649 (about7.3 simulation seconds). All three pickups were recorded; pick durability75→72, dirt15/cobble37 unchanged. One displaced torch was recovered. Local approach excavation was permitted, but only the three ore breaks were needed. Restored allowBreak=false and paused at20176. `visible-iron.jsonl` has259 observations with frames, not truncated; before/after inventory files preserve the counts. Updated the deep iron bookmark to mark these faces mined and record the return chain. Return and shield replacement remain in progress.

The no-placement return goal from `(324,-2,342)` toward `(322,13,351)` took a large detour. Paused at22514, at `(370,9,223)`, and canceled it. `iron-ascent-detour.jsonl` preserves1,747 observations with frames, not truncated. This repeats D063: the driver omitted the documented local foothold allowance and waited too long before checking actual displacement. It is not an A* correctness claim. The local route script now pauses on observation timeout or displacement beyond max(32 blocks, twice straight-line waypoint distance), preserving the still-running task for diagnosis; this guard is not a production API change. Its session copy is `run/playtest/2026-09-13/follow-local-route.py`.

With placement/inventory enabled and digging still disabled, retraced grounded recorder positions back through the iron site to `(324,-2,342)`. The previously unsuitable ascent to `(322,13,351)` completed at25952. Dirt15→4 across retrace/climb, cobble37 unchanged, raw iron3 retained, health20. Ate three dropped rotten flesh as emergency food: food6→18, flesh5→2. Continued the existing return route with local footholds allowed.

Returned through the established route with footholds allowed. A skeleton chase briefly pulled the player back down from the upper landing; it cleared at full health, and resuming its exact safety hold returned to the interrupted destination. `upper-skeleton.jsonl` preserves908 observations with frames, not truncated. Home navigation completed at35686 with all3 raw iron, health20, cobble37 and no dirt remaining. `iron-retrace-current.jsonl` preserves5,919 observations through most of the return. The route helper now rejects a tool-level navigation error before waiting for a new job; a `reflex_active` rejection had otherwise caused an unnecessary timeout pause.

Withdrew the final stored bread and spare pick, deposited two bones (barrel total4), and smelted all3 raw iron using6 leaf litter. The automatic preview preferred2 dark oak logs despite available planks/debris; record a fuel-value inefficiency, bypassed with explicit fuel. Collection completed39372. Leaving the shelter reopened the barrel again (D060); the first crop pass timed out with its GUI open. Paused/exported `iron-home-barrel-reopen.jsonl` (118 observations with frames, not truncated), closed the barrel and retried. The same plot completed38674, harvesting8 wheat and replanting; crafted3 bread. Crafted a replacement shield from the returned iron, leaving2 ingots. The full trip recovered resources but was inefficient: food/light preparation, long detours and combat costs dominate the7.3-second ore acquisition.

### D071 — Equip the healthier carried shield

Reproduced `equip_item shield` returning `already_equipped` with a57/336 shield in offhand and the newly crafted336/336 shield in hotbar5. Paused at43053 and exported `shield-replacement-before.jsonl` (295 observations with frames, not truncated). The shield branch returned before inspecting carried candidates. It now selects the carried/offhand shield with greatest remaining durability, retaining the current shield on ties and preserving the replaced item.

Build and existing item-use tests passed (`shield-replacement-build.log`); those tests cover eating, so replacement acceptance comes from the live retry. HotSwap reloaded only PlayerItemUseController. The identical request returned accepted; settled inventory has336 in offhand and57 in slot5, still two shields. A repeat returns already_equipped without swapping back. Paused43235, exported `shield-replacement-fixed.jsonl` (27 observations with frames, not truncated). Before/after inventories are `new-shield-equipped-before.txt` and `shield-replacement-after.txt`. No client restart or shield aiming change.

### D072 — Expand food production and preserve two new observations

After D071, one eat_food bread request accepted hunger15. Recorder snapshots show bread3 at43277, bread1 and hunger20 at43297; one food.eaten event arrived43285, with no intervening navigation or crafting. Paused43611 and exported `bread-use-count.jsonl` (57 observations with frames, not truncated). Reopening the chest also showed one carried bread. The double decrease is observed; its cause and a repeat with server-consumption instrumentation remain unverified. No speculative eating patch.

The automatic logbook located a stored stone hoe and25 dirt; reopening the original chest confirmed both. Withdrew them and verified settled stock (`farm-expansion-stock-settled.txt`). First sixteen-target placement batch placed eight dirt at x265,y62,z478..485, then failed target_not_visible on the first x266 block. Paused44945/exported `farm-side-placement.jsonl` (208 observations with frames, not truncated). A reordered dry-land retry also failed its first target. Navigated into water east of the platform; the drowning reflex restored breathing. The next eight-target batch completed45948, placing all of x266,y62,z478..485, but finished underwater. Prompt navigation onto `(265,63,483)` completed46189 with full health/air. `farm-water-approach.jsonl` preserves489 observations with frames, not truncated. This establishes a costly water-side workaround, not a safe automatic construction approach. No block-placement patch; preserve the dry-side stance failure for future work.

Tilled all16 new cells; fresh inspection confirmed farmland at each. The first expanded crop pass failed `crop_approach_failed` targeting262,63,483 with work position264,63,483. Paused47243/exported `expanded-farm-approach.jsonl` (163 observations with frames, not truncated). Direct navigation to that work position completed47296; identical crop retry completed47622 (`job-35b93630-e03e-4f27-b588-7f80390c04b6`), harvesting2, planting18, leaving13 growing. Fresh scan confirms31 wheat plants, and `expanded-farm.png` visually shows the planted extension. Saved `shore wheat farm` with the enlarged preserved area and updated deep-iron bookmark with the verified ascent/return. Farm capacity more than doubled; mature yield/long-term food sufficiency remain to test.

Returned home48748, crafted one more bread (two carried), equipped the sword and verified the door closed. Paused49546 with full health/food, fresh336-durability shield, two iron ingots, nine dirt, twenty-three seeds and no carried torches/fuel. `expanded-farm-complete.jsonl` contains547 observations with frames, not truncated; final inventory/pause artifacts and planted-cell scan accompany it. Next: build a larger food/light reserve, retest the bread-count discrepancy with server-side instrumentation after natural hunger loss, then pursue cave exploration or animal husbandry. Broader playtest goal remains active.

### D073 — Resupply interruption and crossbow guard timing

Revalidated the paused runtime and existing completed work. Inventory/debug-world reads are client-side; a read-only watch on InteractionLogbookRecorder.opened confirmed the server also held2 bread before this outing. The earlier two-loaf decrease still needs an instrumented repeat. Deposited the two spare iron ingots into the original chest and verified slot6 count2 (`spare-iron-stored.txt`). Reached the old workshop202,93,468 at51373. A surfaceOnly wood request failed immediately without movement/mining because that stance is below the surface layer. Paused52100 and exported `forest-scope.jsonl` (190 observations with frames, not truncated); moved to the reported forest stance198,100,461. A pillager patrol interrupted before the retry could start.

Three arrows hit at52953/53014/53065, each one tick after shield raise. Paused53163/exported `pillager-shield.jsonl` (411 observations with frames, not truncated). Vanilla CrossbowAttackGoal stops item use when loaded and waits20–39 ticks before firing. Our guard watched isUsingItem and could expire during that loaded delay. A bounded read-only live watch confirmed the client observes charged_projectiles containing an arrow while isUsingItem=false (`pillager-held-crossbow-probe.txt`). Guard loaded crossbows in either hand, keep drawn-bow handling and horizontal shooter-facing unchanged.

The first HotSwap protected the next volley without further damage, but holding a shield stopped navigation and prolonged the fight. Paused54334/exported `pillager-loaded-guard.jsonl` (595 observations with frames, not truncated). Allow existing combat A* approach while guarding arrows, stopping at melee range; active creeper guard still stops movement. Live approach reached melee range and counterattacked. The generic20-tick post-shot hold still consumed much of the reload opportunity; paused55440/exported `pillager-advance.jsonl` (1,068 observations with frames, not truncated). End that hold early only when visible crossbow threats are confirmed reloading, with no incoming arrow, drawn bow, loaded crossbow or active creeper taking priority. Visible idle bows/unconfirmed crossbow states retain the ordinary hold.

The final HotSwap resumed the same fight at55440 and both threats cleared55652, about10.6 simulation seconds later. Counterattacks occurred during reloads; no player damage occurred after the first loaded-crossbow patch at53631. Health stayed18.703333 through all post-patch phases; shield316→168 across them. `pillager-reload-window-fixed.jsonl` has243 observations with frames, not truncated; before/after inventories and pause files preserve the checkpoints. Focused reflex tests passed after each change and an unfiltered full build passed (`pillager-final-build.log`). No restart. This verifies this natural two-pillager encounter, not every mixed-threat or terrain case.

A subsequent bread request at hunger17 consumed exactly one loaf (2→1), restoring food20/health20/saturation5. `bread-single-retry.jsonl` contains30 observations with frames, not truncated. The bounded ConsumableComponent watch enhanced the class but captured no consumption calls, so it supplies no server-consumption trace; do not treat this retry as explaining or fixing D072. Paused55886 while preparing to resume the wood request from a verified surface stance.

### D074 — Finish wood and lighting resupply

After the patrol, a surface-only feature query supplied `(217,108,471)`, reached56009. A single visibleOnly/surfaceOnly twelve-log request completed57026 (`job-944c3509-07f4-4b61-9654-cd13f8f9d8ad`), starting56306:36 simulation seconds. Carried logs2→14, full health, no manual moves during acquisition. `wood-supply-batch.jsonl` contains1,234 observations with frames, not truncated. Returned home57938.

Converted one log into four planks, then smelted eight logs with six planks in the home furnace. Insertion completed58517; background process `smelt-process-98c887a8-4789-4a80-8b00-c9f2372d03c4`. While it cooked, reached the farm, confirmed no open container, and completed a31-cell pass at59516: harvested/replanted1, growing30, no missing seeds. A nearby drowned then aggroed and was defeated by four sword attacks, threats clear59951 with no damage; returned from water into the shelter60369.

Collected all8 charcoal at60683 (`job-71a48678-1cd9-47dd-8312-29c095cc95b8`). Settled inventory and the server logbook agree on8 withdrawn from255,63,479 (`charcoal-supply-inventory.txt`, `charcoal-supply-logbook.txt`). Converted one further log into planks/sticks and crafted16 torches, retaining4 charcoal,4 logs,3 planks and2 sticks. Food remains scarce: bread1, wheat1;24 seeds support future farming. Two iron ingots remain stored in original chest slot6. Full health/food, shield168, dirt9/cobble37. Verified the shelter door closed and paused63043. `wood-resupply-home.jsonl` contains784 observations with frames, not truncated; final inventory/pause artifacts accompany it.

Next priority is food/husbandry. The expanded farm works but this pass yielded only one mature crop; it is not yet a sufficient travel food reserve. Earlier loaded-entity observations found pigs, sheep and chickens around the forest hills; refresh positions before interacting. Keep the bread-count discrepancy open until its cause is established. The broader playtest goal remains active.

### D075 — Reach an occluded animal and breed chickens

Reached observed ground `(196,103,487)` beside two chickens. First use_entity consumed one seed and completed64383. The second stayed RUNNING64710→65146 while the target was about2.1 blocks away. Paused/exported `chicken-approach.jsonl` (92 observations with frames, not truncated). A bounded read-only watch confirmed distance2.1146, lineOfSight=false, chase target194,103,486, and Baritone processActive=false. The three-block near goal was already satisfied on the wrong side of the obstruction, so the interaction kept retrying from the same spot.

Use an exact feet approach while the target is occluded; retain the near goal for visible targets. Apply a small feet-height offset for shortened supports. Existing entity interaction tests and build passed. One-class HotSwap resumed the same stalled task, which completed65176, twelve ticks after resumption. The first chicken's love timer had expired during diagnosis, so fed it again; that interaction completed65511. A third chicken then appeared. `chicken-approach-fixed.jsonl` has177 observations with frames, not truncated; `chicken-breeding-after.txt` shows both original UUIDs and newcomer6adfb5c1. Paused65989 at the hill. No new entity action or manual position workaround was needed after the fix. The query currently lacks an age flag; add that before relying on it to choose breeders versus offspring.

### D076 — Expose visible baby/adult state

The breeding query showed three identical-health chickens but could not distinguish breeders from offspring. Added optional baby to NearbyEntitySnapshot and its compact descriptions, sourced from LivingEntity.isBaby on the client. Nonliving entities omit it; no unsynchronized breeding cooldown or love timer is inferred. Contract tests cover adult, baby and omitted nonliving age; full build passed (`entity-age-build.log`).

Changing the shared record shape required one saved-world client restart. The original process exited and the same `another world` save rejoined in Codex-driver/LAN mode. Restored no-break/no-place/no-inventory travel and underground lighting. Live query confirms original0e4c17cc/eebd5397 are baby=false and new6adfb5c1 is baby=true. All three and inventory persisted. `entity-age-live.txt`, `entity-age-restarted-inventory.txt` and `entity-age-live.jsonl` (115 observations with frames, not truncated) preserve the result. Paused1512 in the restarted session. World bookmark `hill chickens` records the uncontained herd and requires fresh positions before interaction. Containment and sustainable food yield remain open.

### D077 — Hunt, cook and store a food reserve

Hunted two freshly observed adult pigs with the iron sword. Attack jobs completed2454 and3820. The kill action leaves drops on the ground; fresh item queries and short exact navigation collected3 then2 raw porkchops, all5 confirmed in `hunt-pork-collected.txt`. This is an efficiency gap for a future hunting/food action, not false completion of attack_entity. Returned via the known hill stance to home7793, health20, food16. No digging or placement during travel.

Cooked all5 porkchops using1 charcoal in the home furnace, process `smelt-process-4a286cf8-2880-4396-935e-95a732d49e62`. While it ran, a31-cell crop pass completed8854: harvested4, replanted4, growing27. Returned9375; collected all5 cooked porkchops9651. `pork-cooking-collected.txt` and `pork-cooking-logbook.txt` agree on output and server-observed withdrawal.

Crafted1 bread from the harvest, then ate1: bread2→1, food16→20, saturation6. This normal retry does not establish the cause of D072. Deposited2 cooked porkchops in barrel256,64,482; settled slot3 count2 and the automatic logbook agree, with3 carried. Evidence: `pork-reserve-settled.txt`, `pork-reserve-logbook.txt`. Closed the barrel and shelter door, paused11368 at home. `hunt-food-home.jsonl` preserves1,859 observations with frames, not truncated. Carried reserve now3 cooked porkchops,1 bread; stored reserve2 cooked porkchops. Chicken containment and sustained food production remain next; broader goal stays active.

### D078 — Build a chicken pen and test paced luring

Collected three additional visible surface logs, completed12406, then crafted a nearby workbench,12 fences and a gate. Built at x196..199,z488..491,Y103 after removing one raised grass block. Eleven-fence placement completed15748; gate faces west/east for entry through196,103,489. Added an interior torch and world bookmark `hill chicken pen` with preservation. `pen-built-blocks.txt` and visually inspected `chicken-pen.png` verify construction.

Holding seeds and moving in short steps drew adult0e4c17cc and chick6adfb5c1 about20 blocks toward the pen. Combat interrupted: first skeleton and creeper cleared without damage; a later skeleton/zombie pair caused one1.125 zombie hit, then cleared21934 at full health. The shield absorbed the skeleton arrows while facing its horizontal direction. Another zombie hit for1.125 during the subsequent return, resolved22593. `pen-build-and-lure.jsonl` contains2,537 observations; `pen-combat-followup.jsonl` contains821, both with frames and not truncated. The flock scattered while combat owned movement; containment must not be inferred from player arrival.

Reacquired the adult through use_entity with seeds (completed24527). Added an ignored, bounded playtest helper `follow-lure-route.py`: use observed waypoints, wait for selected chickens within3.1 blocks before continuing, verify seeds remain equipped, stop on combat, and pause on lost followers/catch-up timeout. The sixteen-leg route completed27497 with the adult staying about2.2 blocks behind. It stopped at196.3,103,489.4, just outside the open gate: the two-by-two interior was too shallow to draw it fully inside. Paused28010/exported `pen-gate-standoff.jsonl` (764 observations with frames, not truncated). This is a layout failure, not a navigation success criterion for containment.

Extended the pen one row south through z492: moved two fences, recovered their drops from the slope, filled two floor cells with dirt and used a two-block-high cobblestone corner at199,103..104,492. Remaining perimeter uses all12 crafted fences and the original gate; interior is now x197..198,z489..491. Reacquisition fed the adult again30785. Two short observed-ground routes failed CALC_FAILED:212,104,494 and213,105,493. Paused/exported `lure-short-route-failed.jsonl` (166 observations) and `lure-bypass-failed.jsonl` (136), both with frames and not truncated. A placement site is not proof of a traversable route. Previously traveled positions bypassed both failures.

The known-route retry reached the deeper interior34467 with adult0e4c17cc at197.3,103,489.6, fully through the gate. Closed the gate and put away seeds. Settled gate open=false, subsequent entity queries, and visually inspected `contained-chicken.png` confirm the healthy adult remained inside; player also remains inside at198,103,491. `pen-contained.jsonl` has1,446 observations with frames, not truncated. Updated the world bookmark with the new footprint, single occupant and exit caution. The other adult and offspring remain uncontained; no sustainable husbandry yield yet.

Ate one cooked porkchop at hunger12: carried3→2, hunger20, health20, saturation12.8. Two cooked porkchops remain stored at home and one bread is carried. Paused36302 (`pen-fed-pause.txt`), `pen-fed-checkpoint.jsonl` contains61 observations with frames, not truncated. Shield144 remaining, cobble35, dirt8, charcoal3, no carried torches. Next: make the demonstrated pacing into a reusable System1 herding action that reacquires moving animals and resumes after combat, then retrieve the remaining breeders. Keep placement-site reachability and the original bread-count observation separate; no speculative fixes. Broader playtest goal remains active.


### D079 — Native luring, recovery limits, and a breeding pen

Implemented `lure_entities`: select 1..8 observed UUID tokens and a held food item, then supply an inclusive destination box (maximum16x8x16, within64 blocks). System1 resolves loaded animals within32 blocks, approaches moving followers, waits for laggers, chooses standing positions, and completes only when every selected animal's whole bounding box is inside. No feeding, gate operation or server AI manipulation. Temporary walking-only settings disable breaking, placing, sprinting, parkour, inventory housekeeping and bucket falls; release restores prior settings. The normal job, task-update, interruption and recorder paths own execution. Acquisition and leading each have1200-tick phase budgets, with6000 active ticks per attempt. Current global reflex release calls onWorldLeave on all executors, so a combat resume starts a fresh bounded attempt using the retained job selectors; the budget is not cumulative across those resets.

Fourteen focused executor tests cover moving/occluded targets, catch-up, every-animal arrival, player-arrival insufficiency, alternative standing positions, session gating, release, timeout and argument validation. ActiveJobRuntime tests preserve lure payload through pause/resume/cancel. Full build passed before launch and again after live corrections (`lure-final-build.log`). New task enum/records required one client restart; subsequent method changes used HotSwap on JDWP5005. World another world, Easy, driver active; no new fixture entities or inventory were spawned.

At restart, contained adult0e4c17cc was still inside; the already-arrived action completed666 without movement. After opening the gate and exiting, that bird was later observed outside. Paused1877/exported `pen-escape.jsonl` (493 observations with frames, not truncated). Frames show it inside before opening; the gate was open across the exit and follow-up interval, but no frame captures the crossing. Do not claim a wall gap or a fixed escape bug. Gate was verified closed afterward; improve exit handling before relying on long-term containment.

The previous chick6adfb5c1 had grown. Navigated to227,110,458 and called native luring for that observed animal near242,104..105,452 into the entrance area192..195,102..104,486..489. Job a10ffbe4 completed2723, about42 simulation seconds after starting. Recorder shows acquisition, waiting/reacquisition and leading through the forest; player ended192,102,489 with the chicken192.9,103,487.2. Health20, seeds31 unchanged. This is native retrieval proof, not the earlier Python waypoint helper. Evidence is included in `native-lure-stall.jsonl`.

A two-bird pen attempt selected6adfb5c1 and escaped0e4c17cc. A phantom interrupted3484: one0.88 damage hit, counterattacks, brief re-engagement, clear3710; health regenerated to20 and a membrane was later collected. Resumed the same job3848bc25 after safety hold fa8b3b0f. Both selectors and seeds were restored. Six-setting queries verify normal values during the hold and walking-only values after resume (`lure-six-settings-interrupted.txt`, `lure-six-settings-resumed.txt`).

The resumed attempt stalled beside the tree/fence with both birds2.1..2.4 blocks away, one occluded. Paused5117/exported `native-lure-stall.jsonl` (2597 observations, frames, not truncated). Read-only probe showed an active exact path from199,103,488 through199,104,487 to200,104,487. The new controller incorrectly required visibility for nearby acquisition. Local Minecraft1.21.8 TemptGoal bytecode explicitly uses TargetPredicate.ignoreVisibility. Removed the unnecessary visibility condition for close acquisition/catch-up, retaining exact approach for distant occluded targets. Focused test passed and HotSwap advanced the same attempt to LEAD with2/2 acquired.

A separate fence-edge movement stall remained. Paused6160/exported `native-lure-fence.jsonl` (1070 observations with frames, not truncated). Added/tested the leading phase budget and HotSwapped; the same live attempt failed6377 with lure_navigation_timeout, releasing ownership. Baritone's underlying fence collision route was not fixed. Explicitly removed our fence199,103,488; retry3647d999 moved freely and brought both birds into the enclosure. One body still straddled the strict destination boundary z489; after trying all six standing cells, the action correctly failed destination_too_small_or_unreachable7122. Paused7515/exported `native-lure-arrival.jsonl` (510 observations with frames, not truncated). Player block arrival does not guarantee enough draw distance for a small pen.

Closed the gate and restored the removed fence. Expanded the interior one row to x197..198,z489..492,Y103: placed three dirt supports at196..198,102,493 and a double-height cobble wall x196..199,z493,Y103..104, then removed/recovered the two inner fences197/198,103,492. Full wall checked before removing the inner barrier. The expanded-area two-animal check completed9121; both were already inside by then, so this does not establish a fresh two-bird entrance traversal into the larger pen. Feeding the adults completed9913 and10087, consumed exactly two seeds (31→29), and produced baby0413304a. Subsequent entity reads confirm three healthy chickens inside with seeds put away and gate closed. `native-lure-family.png` was visually inspected; `native-lure-family.jsonl` has496 observations with frames, not truncated.

Updated per-world hill chicken pen bookmark/preserved bounds through z494. Final inventory: full health/food, saturation9.8, two cooked porkchops and one bread carried, two cooked porkchops stored at home,29 seeds,27 cobble,5 dirt,2 recovered fences,3 charcoal, no carried torches; shield144 remaining. Player remains inside around198,103,491. Paused11456, serverTick11170, debugSession130c20bf-a18d-47c9-9845-2a9fafde9bdd epoch1. Next: safe exit/longer containment, continue food/light reserve and cave progression; original adult eebd5397 remains uncontained. Broader playtest goal remains active.


### D080 — Gate passage and a scattered-herd distance bug

Revalidated D079's paused live world and classified that turn as progress. Tried a pale moss carpet crossing over fence196,103,490; placement succeeded but navigation to196,104,490 failed CALC_FAILED11822. Paused12257/exported pen-carpet-exit.jsonl (127 observations, frames, not truncated), then removed the carpet; it was later recovered. Do not claim a general carpet-crossing implementation or a vanilla collision defect.

Built a two-gate entrance west of the pen. Outer gate193,103,489 and existing inner gate196,103,489 surround corridor194..195,103,489. Six cobble floor supports and four cobble gatepost blocks, two recovered fences on the south side, and existing tree trunks194/195,103..104,488 on the north enclose it. Preserve those trunks. Foundation placement from inside the pen stalled while Baritone opened the inner gate to seek an outside work position; paused14306/exported airlock-inside-build.jsonl (86 observations). Two birds were outside afterward. Outside placement completed14634. Side-fence placement from the wrong side failed target_not_visible; paused16146/exported airlock-side-placement.jsonl (214 observations). Moving into the corridor made the same placement succeed. These work-position failures were bypassed, not patched. A one-log collector completed16228, then normal crafting produced the outer gate; gate placement succeeded and both closed states were read back.

Retrieved the escaped adult/chick from the forest. A tightly chosen exterior staging box did not fit their final bodies; cancelled that attempt once both were at the entrance rather than claiming tool completion. With both gates open, the subsequent three-animal return e9333ae0 completed21841. Both adults later escaped before the closure/exit sequence finished. Paused24335/exported airlock-family-return.jsonl (997 observations with frames, not truncated). The camera faced the wall after arrival, so it did not record their crossings. Observation gaps and delayed gate closure remained unresolved explanations; do not infer a broken wall.

A prompt-closure retry exposed a real luring defect: all three selectors passed initial32-block validation, but walking toward one put another33.4 blocks away. The executor failed follower_out_of_range24489 while that follower was still loaded. The wrapper helper paused24506; lure-distance-cutoff.jsonl contains90 observations with frames, not truncated. Removed the duplicate mid-task32-block cutoff. Initial range validation, pinned loaded identities, missing/dead detection, catch-up pacing and1200/6000-tick budgets remain. A focused regression starts two animals25 blocks on opposite sides, then approaches one while retaining the other. All15 executor tests and full build passed (lure-distance-full-build.log); HotSwap loaded the change without restarting.

Live retry325a409d retained the distant adult through33.57 blocks (42 traced observations beyond32), reacquired all three and brought them into the pen. It then failed the overly tight integer arrival box: one adult at x196.8 was physically inside the fence yet its body lay outside requested x>=197. This is a goal-region limitation, not a fixed arrival claim. All eight candidate standing cells were tried, and the bounded failure paused25909. The 1200-record airlock-closure-entity-trace.jsonl shows both the distance regression and arrival geometry. Fractional usable space inside fence blocks needs consideration before treating integer box arrival as proof of enclosure containment.

Closed the inner gate promptly and put away seeds. A separate1200-tick entities+gate trace (airlock-closed-containment-trace.jsonl) recorded no bird leaving the physical enclosure and no inner-gate opening. A creeper approached outside the north fence during this trace: two counterattacks, shield raised at fuse0.3214 on27261, then explosion27282. The chick/adult position trace, reflex events and visibly inspected explosion frame27291 explain one adult's health4→3 at the explosion tick. Player health stayed20; a fresh block scan found the pen perimeter intact. Evidence: pen-creeper-chicken-damage.jsonl (155 observations, frames, not truncated) and pen-after-creeper-blocks.txt. This is collateral blast damage, not a gate or feeding bug.

Closed the outer gate from inside without opening the inner gate. Then used an ignored playtest helper for the verified exit sequence: outer remains closed; open inner; navigate into corridor; close inner; check all three birds; open outer; exit; close outer. Native tools and settled task results perform every action. The 1200-record airlock-exit-trace.jsonl has zero ticks with both gates open: inner open28697, closed28816; outer open28881, closed28978. The chick briefly approached the open inner threshold (five samples extended slightly past the conservative fence-face inset), then returned; no bird escaped. All three were still inside after the exit. airlock-finished.png was visually inspected.

Updated the world bookmark and preservation to x192..200,y101..105,z485..494. Final outside player position about191.8,102,489.5, both gates closed. Adults0e4c17cc health4,6adfb5c1 health3 and baby0413304a health4 remain in the pen. Inventory: health20/food20/saturation9.8,17 cobble,5 dirt,29 seeds,4 eggs,1 dark oak log,3 planks,2 sticks,3 charcoal,2 cooked porkchops,1 bread; shield144 remaining. Paused30172 with debugSession d59a9c6a-1b98-41a1-b3d7-5475ac9afe84 epoch1. Recorder checkpoint airlock-exit-checkpoint.jsonl has227 observations with frames, not truncated. Next: food/torch reserve and cave progression; long-term husbandry yield is still unproven. Broader goal remains active.

### D081 — Resupply, stored shield replacement and farm interruptions

Resumed the pen-exit checkpoint. Crafted eight torches and reached the observed forest stance217,108,471. A bounded visible/surface wood request gained eight logs (one carried initially); combat displaced the player beyond its16-block acquisition area. Resuming after the resolved reflex failed33987 with `acquisition_scope_left itemCount=9`, as constrained. Kept the useful partial yield and returned home257,63,480 at35306. Initial torches were all spent by automatic lighting en route. Recorder shield events32788–32956 retain the shooter UUID, shield damage192→196 and health20; threats cleared32969. Source still faces the shooter horizontally, with incoming-direction fallback for unknown owners, rather than aiming at a projectile. No shield code change or new combat reliability claim this turn.

Used the readonly logbook to locate the stored iron, then verified the actual chest: two ingots in slot6. Withdrew one, confirmed settled chest/inventory counts, crafted a shield and equipped it at336 durability. One iron remains stored. Turned two logs into planks for the shield/sticks; smelted the other seven with one charcoal in home furnace255,63,479, process `smelt-process-9099cc38-72b7-4960-b957-d3ed347f9588`. Output collection completed40007. Crafted24 torches, retaining one charcoal/two sticks. Existing item-history filters intentionally include later empty observations for containers that once held the item, preventing stale-stock conclusions; no filter bug.

First farm pass `job-93ea80b8-200b-46f0-9568-1808c21ab64e` timed out36955 at its first mature crop. Paused37282 and exported `resupply-crop-stall.jsonl` (115 observations, frames, not truncated). Snapshot36538 had no screen;36720 had `GenericContainerScreen` at259.2804,63,480.5009. Paused inspection identified the home chest, syncId6, empty cursor. The harvest method refuses any non-player screen handler. Closing the chest and retrying from the same location immediately harvested/replanted crops. Accidental opening mechanism remains unproven; retained as a bypassable issue, with no code patch.

Retry `job-2d5084d0-5dd7-4b1a-94e5-1ad3c4e0ebc8` harvested eight/replanted seven, then failed37548 routing to drops at264,61,481 in irrigation water. Paused38113/exported `resupply-water-drops.jsonl` (177 observations). Wheat/seeds were still present, later floating aroundY62.5. A short route toward264,62,481 collected both, raising wheat9→10/seeds44→48. The player subsequently sank while idle; the existing air reflex recovered with health20. Returned to264,63,483 at38625. This is not proof that an exact-Y water arrival stays at that elevation after movement releases.

Next pass `job-6d9d1c56-3ee3-4a6d-a782-dcc91be0a762` harvested/replanted eight more, then failed39048 approaching266,63,485 from selected work position264,63,483. Paused39362/exported `resupply-farm-approach.jsonl` (343 observations). Kept the partial yield rather than expanding this turn into a general pathfinder rewrite. A one-cell pass at264,63,483 replanted the pickup-interrupted crop, completed39383. Final structured scan confirmed31 wheat plants: ten age0, six age1, three age4, two age5, three age6, seven mature. All16 harvested plants were replanted; neither broad pass completed. Crafted six bread from18 wheat, leaving seven bread total plus two cooked porkchops carried.

Restocked cobble17→64 from the chest. Deposited45 seeds (chest now76), four eggs (now5), seven rotten flesh (now17). Stored both worn shields plus moss/carpet, membrane, saplings, gravel, arrows and bones in the barrel; settled slot reads and logbook retained. Barrel also has eight coal, two charcoal and two cooked porkchops, observed during this visit, available for later resupply. Artifacts `resupply-chest-transfers.txt`, `resupply-barrel-transfers.txt`, `resupply-shield-logbook.txt` and final inventory verify changes. No source changes or build required this turn.

Final checkpoint41942/server41654: inside home257,63,480, door verified closed, health20/food20/saturation9.8, fresh336 shield, seven bread, two cooked porkchops,24 torches, one charcoal,64 cobble, five dirt,16 seeds,20 free storage slots. Paused with debugSession `77c7276f-af35-4a52-9e2a-dc1579cd619e`, epoch1. `resupply-checkpoint.jsonl` contains2474 observations with frames, not truncated. Next: resume cave exploration with this reserve, using recorded return stances and visible ore; longer-term reliable single-pass farm routing remains open. Broader playtest/fix goal remains active.


### D082 — Cave iron and acquisition memory

Resumed D081 and reached185,86,392 at42810,204,83,411 at43348,214,77,405 at43527. Passage settings remained allowBreak=false, allowPlace=false, allowInventory=false, allowWaterBucketFall=false. Descended via215,73,402,216,68,401,215,67,400. The old216,66,400 landing failed CALC_FAILED44266; paused44798/exported cave-next-landing-failure.jsonl (166 observations), verified its local footing and bypassed through freshly surveyed218,66,401. One bread consumed normally, seven→six, food18→20/saturation6. No navigation patch or explanation of the underlying CALC_FAILED is claimed.

Surveyed the north/east upper chamber using first-hit rays. A bounded eight-coal request around232,66,388 gathered four and failed46774 when no further reachable source remained in scope. Continued245,63,393→263,61,395→265,60,405→273,59,400→275,60,400→277,61,400. Copper/coal were visible but no iron or lower exit from these stances. Saved east copper alcove bookmark with surveyed limits, instead of declaring a proven dead end. Five separate fusing-creeper guard sequences44356–46362 cleared at health20; shield336→315. cave-next-upper-exploration.jsonl contains3039 observations with frames, not truncated. The snapshots show the effects of exploration, not proof that every past waypoint remains safe after explosions.

Returned via265,60,405→220,63,398→200,61,390→199,59,391. Selecting visible ledge197,42,396 led fromY59 to water198,36,392, then CALC_FAILED50364. Paused50742/exported cave-next-mid-descent-failure.jsonl (170 observations); health20/air300 throughout recorded descent. Navigated to lower dry196,37,393 and tested return to199,59,391, completed51113 without enabling placement/digging. Saved west cave water descent bookmark distinguishing the failed ledge from the verified landing/return. A second direct descent to196,37,393 completed51843. Then193,35,380→180,33,381→177,22,374 reached53413. Saved western lush branch with its approach and then-unverified lower return.

The lower survey found two visible iron blocks178,21,375 and178,21,376. Eight-item visibleOnly collection job mined the first53894, collected its drop53930, and failed53951 with itemCount1/rejectedTargets0. After noticing remaining ore, paused54678 and exported cave-next-iron-missed.jsonl (204 observations). A fresh survey still saw the second original block plus three newly exposed faces; exact read confirmed original second block still iron. The production candidate generator sampled only the current eye at each SELECT and forgot sources from prior views. Pickup movement changed visibility, so a known vein was prematurely treated as exhausted.

Added attempt-local observed-source positions in TargetAcquisitionTaskExecutor. Each visible-only SELECT adds eligible first-hit sources; MinecraftAcquisitionEnvironment regenerates work positions from those known sources and rechecks loaded state, matching/ready block, scope and preservation. This caches ore observations rather than stale routes. Source order has a coordinate tie-break. Scope/active-tick bounds still constrain the attempt; new task identity/world leave clear observations. Ordinary withdrawal of the same request retains them, tested at the executor boundary. Existing outer-runtime reflex resets can still start a fresh attempt; this turn does not establish persistence across every combat interruption. Recorder task detail now includes observedSources count, and the tool description states the discovery/retention behavior.

Eighteen acquisition tests passed, including observed-but-now-hidden continuation, no discovery of an unseen third block, disappeared sources, withdrawal, task replacement and world isolation. Existing ray sampler tests and an unfiltered full build passed (cave-seen-sources-tests.log and cave-seen-sources-build.log). One restart required by the new executor field/interface: stopped verified PID60557, relaunched through scripts/codex-driver, rejoined the same save, restored no-break/no-place/no-inventory/no-bucket-fall and lighting. The saved one raw iron persisted. A nearby zombie cleared at health20; no cheats or world replacement.

Live retry requested seven additional iron within the original center177,22,374/radius6/vertical6/visibleOnly scope. It retained three initial observations, added two newly exposed sources, and mined all five remaining observed ore blocks, inventory1→6. It finally failed1445 below the requested eight-total threshold with no sources remaining; one initial approach_CANCELED rejection was bypassed by another candidate. Returned to original ledge177,22,374 via179,21,373. Its survey showed no ore; exact eight-cell read178..179,20..21,375..376 showed air throughout. cave-seen-sources-fixed.jsonl contains653 observations with frames, not truncated. This proves the remaining observed vein was acquired and preserves the correct partial-result status; it is not proof that no hidden ore exists nearby.

Crafted eight torches from two coal/two sticks, carrying twelve torches, six raw iron, two coal, one charcoal, six bread and two cooked porkchops. Paused new-client2481 with debugSession73728433-7ce5-4599-b453-3509be76e7e1 epoch1 at177,22,374, full health. Next: verify the lower return, bring ore home and smelt it; resume broader cave progression afterward. Broader playtest/fix goal remains active.


### D083 — Bring cave iron home; guard resumed routes too

Returned from the mined vein toward180,33,381. The32-block local driver guard paused2759 at166,29,411 while the same job0b3f1edd was still RUNNING, estimated420 ticks remaining. This was a natural uphill detour with altitude progress. Exported cave-iron-return-detour.jsonl (282 observations), continued that exact job with a bounded watcher, and it reached180,33,381. Consumed one porkchop, two→one, food17→20/saturation12.8. Then193,35,380→196,37,393→199,59,391→200,61,390 succeeded.

Combat interrupted the next upper-landing goal and displaced the player to196,35,379. It resolved at full health; resumed the same job and reached220,63,398 at5771. Settings still matched no-break/no-place/no-inventory/no-bucket-fall. A later skeleton interruption started6677 during216,68,401 navigation and cleared7085, with no counterattack/kill claim; the recorder shows ranged guards followed by threats_clear. cave-iron-upper-skeleton.jsonl preserves693 observations.

The resumed216,68,401 job then took a large downward/eastward detour: observed242,16,401 at8743, paused9210 at321,15,348. Exported cave-iron-ascent-detour.jsonl (1096 observations). This was an active route, not a terminal failure. The driver had kept ascent overconstrained and had not attached its distance guard to the resumed job. Cancelled it deliberately, enabled allowPlace/allowInventory for return while keeping allowBreak/allowWaterBucketFall false. Added an ignored reusable watcher for exact existing jobs, run/playtest/2026-09-13/watch-current-route.py (source /tmp/airicraft_watch_route.py): distance/time limits also pause resumed routes; terminal or reflex changes stop observation without falsely declaring completion.

Two further encounters cleared at health20. Resumed the320,21,346 return goal under the new watcher, completed10302. Followed306,18,355→293,12,360→276,14,371→196,37,393, completed11997. Then199,59,391→215,67,400→216,68,401→214,77,405→204,83,411→185,86,392 all completed, final13586. The upper ascent consumed one dirt (six→five); cobble remained75. Allowing carried footholds made this observed return practical; do not generalize the separately verified no-placement lower ascent to the whole cave. Updated western lush branch and lit cave descent world bookmarks with these distinctions and resume guards. cave-iron-return-first-leg.jsonl preserves5143 observations with frames, not truncated.

Restored allowPlace=false/allowInventory=false at the surface. One bread consumed normally, six→five, food16→20. Home257,63,480 reached15085 with six raw iron and full health. Closed the door and started six-iron smelting in furnace255,63,479 using one charcoal, process smelt-process-0520873b-3304-43ae-9125-682f96be5bdd. This return exposed a driver policy/observation gap; the committed source fix remains D082's task-local acquisition knowledge. No additional runtime patch this leg.

Collected all six ingots17375, verified inventory6, then deposited six into chest256,63,481. Settled slot6 and the server logbook agree on seven total ingots (the previous one plus this trip's six); logbook worldTick492809 records container_put6 and contents iron_ingot7. Proof: cave-iron-smelted-inventory.txt, cave-iron-home-chest-after.txt, cave-iron-home-logbook.txt. Final door read is closed. Paused18748/server18390, debugSession69f51f29-6099-4c7c-a3b9-9e6efdb92ac7 epoch1 at home257,63,480. Health20, food20, saturation6; shield302; five bread/one cooked porkchop carried, two porkchops previously stored; one torch/two coal,75 cobble/five dirt,16 seeds,17 free slots. cave-iron-home-complete.jsonl contains2085 observations with frames, not truncated. The seven stored ingots are confirmed current stock, not only historical logbook inference. Next: replenish sticks/torches from home fuel and wood, use the observed-vein memory fix on further cave trips, and continue progression. Broader goal remains active.


### D084 — Reuse the shore tree and home fuel

The previously replanted oak at279,63,478 has grown: exact reads show six oak logs Y63–68 and leaves above. Broad visible-only wood collection from259,63,482 instead selected dark oak253,85,483 with work position253,84,484 (three observed sources). It failed19763 with acquisition_scope_left and zero logs. Paused22131/server21773 and exported next-resupply-wood-scope.jsonl (543 observations, frames, not truncated). The player was256,78,469. Source inspection confirms the same surface predicate is applied to the player's transit position, so remaining inside the radius alone does not ensure the route survives. This is a bypassable observed acquisition failure, not proof that the shore oak was unreachable or that a general pathfinding fix is needed. Retry near the observed tree with radius5/verticalRadius8.

Driver mistake on the approach: guessed276,63,478 was above water, not supported land. Navigation CALC_FAILED22510 with the player submerged; paused22826 and exported next-resupply-shore-approach.jsonl (516 observations, frames). The drowning reflex recovered with health20/air300 and awaited the planner. Cancelled the failed task and inspected actual footing. Dry278,63,476 succeeded23563. Do not infer walkable shore positions from approximate coordinates; inspect support before issuing exact-Y navigation.

The local six-log request completed with six logs in inventory, but an exact recheck still found the original top block279,68,478. Converting two logs left four. A fresh one-log request from279,63,478 found zero observed sources and failed; paused25437/server25079 and exported next-resupply-last-log.jsonl (699 observations, frames, not truncated). The sampler omits the polar directions (elevations−75..75), which is a plausible cause of this directly overhead miss, not a verified general visibility diagnosis. Exact break initially returned target_out_of_range; inspected supported280,65,478, navigated there (completed25822), then broke the observed top block successfully. The drop raised logs four→five, and exact recheck found zero remaining logs in the six-cell trunk. Eight planks from the two converted logs made16 sticks. Record the overhead miss as bypassable; no source patch.

Returned home26959. Barrel reinspection confirmed eight stored coal, two charcoal and two cooked porkchops. Withdrew eight coal/two porkchops; deposited three oak logs, two arrows, two bones and three rotten flesh. Settled sync7 contents verified all transfers (next-resupply-barrel-after.txt): barrel now holds three oak logs/two charcoal, no coal or pork; carried ten coal/three pork. Closed the container and crafted40 torches from ten coal/ten sticks. The readonly logbook contains ten four-torch crafting events at worldTick502215..502250, totaling40 (next-resupply-torch-logbook.txt). Two logs/six sticks remain carried; no new coal expedition was needed.

Picked up one naturally dropped oak sapling on the first return, leaving the seven stored saplings untouched. Returned via verified278,63,476, replanted279,63,478 at28569 and read back oak_sapling stage0 (next-resupply-replanted.txt). Updated the persistent shore replanted oak bookmark with the verified cycle, supported approach and overhead-log workaround. One growth/harvest/replant cycle is proven; sustained yield remains unmeasured. A brief MOB_ATTACK reflex28774..28919 rejected the first return command, then cleared at health20; retried navigation after IDLE.

The return via278,63,476 was still RUNNING after hundreds of ticks, estimated2827 ticks, while the player was at279,63,479. Paused30019/server29661, exported next-resupply-return-detour.jsonl (987 observations), and deliberately cancelled it. This was not a terminal pathfinding failure. Direct navigation to the known home landing259,63,482 completed30250, then shelter257,63,480 completed30339. Amended the bookmark to distinguish outbound footing from the successful direct return.

Final checkpoint: door verified closed; paused31055/server30698, debugSession7346edf1-d33c-4bde-9ca3-019b7ca5b7d0 epoch1. At257,63,480 with health20, food20, saturation0,40 torches, five bread/three cooked porkchops, two logs/six sticks,75 cobble/five dirt, shield302, iron picks161/62,19 free slots. Seven ingots remain in the chest from D083; no chest transfer this leg. Barrel now has three logs/two charcoal and seven reserve saplings. next-resupply-complete.jsonl preserves2956 observations with frames, not truncated. No code changes or restart this leg. Next: use the replenished food/torch reserves for another bounded, visible-ore cave trip; continue husbandry and farming follow-up. Goal remains active.


### D085 — Western cave frontier and water-edge iron pickup

Verified D084 checkpoint, then followed the known cave route with allowBreak/allowPlace/allowInventory/allowWaterBucketFall false. Surface185,86,392 completed31920;204,83,411 completed32124;214,77,405 completed32266. Descended through215,73,402,216,68,401,215,67,400,218,66,401,220,63,398,200,61,390,199,59,391 to196,37,393 (33586), without a route interruption. One bread restored food15→20/saturation6, five→four bread. Continued193,35,380→180,33,381→177,22,374, completed34712.

Extended the western branch using fresh first-hit surveys and short supported landings:174,22,379→172,23,380→167,23,377→162,23,381→160,23,383→157,23,381. Saved western moss shelf world bookmark; reverse traversal remains unverified. Surveys at162/160 saw wall iron146,33,382 and146,32,382. At157,23,381, a new floor vein exposed155,22,383. No hidden-block scan selected these sources. Subsequent exact reads were for the observed drops and footing.

Lighting concern was not a confirmed bug. Paused38013/server37657 at light1 and exported cave-frontier-dark.jsonl (466 observations). Inventory showed three torches had already been placed,40→37. The next mining task placed another and raised the original stance to light13, leaving36. The runtime maintains lighting during navigation/mining, not idle survey time. No lighting patch or unresolved lighting-bug claim.

The four-iron request mined155,22,383 and newly exposed154,22,383, but failed with zero collected, three rejected pickup targets and approach_CALC_FAILED. Water flowed into the mined cells. Paused38645/server38288 and exported cave-frontier-floor-iron-failed.jsonl (284 observations). Two raw-iron entities remained at154.1,22.2,383.1 and154.1,22.2,383.9. Recorder shows exact pickup goals at155,22,383 and154,22,383. With the same pathfinding settings, manual navigation to supported adjacent154,23,384 immediately collected both. Paused39388/server39030; cave-frontier-pickup-reproduced.jsonl preserves106 observations. This reproduces a pickup-targeting gap, not failed mining or a need to permit excavation.

Changed drop candidate generation to include standable sites in the adjacent3×2×3 neighborhood, filtered through the existing scope check, while retaining the original drop cell as fallback. Candidates retain separate keys, existing rejection behavior and active-tick limits; no automatic terrain-permission change. Added AcquisitionPickupSites as a small geometry helper. Its three tests cover the live water/drop landing, distinct alternatives and unsupported/out-of-range exclusion. All18 existing acquisition tests plus the three new tests passed; the full build also passed (cave-pickup-sites-tests.log and cave-pickup-sites-build.log). HotSwap redefined only MinecraftAcquisitionEnvironment on JDWP5005; the new helper loaded normally. No client restart.

Live retry from156,23,383 surveyed the remaining visible pair154,22,384 and155,22,384, then requested two additional iron. It completed with inventory2→4. The recorder shows adjacent pickup targets155,23,384 and155,23,385; both saw transient approach_CANCELED, followed by recovery and collection without another planner movement command. Preserve this limitation: the change supplies alternatives, not a claim that every route event or water pickup is fixed. cave-pickup-sites-fixed.jsonl has232 observations with frames, not truncated; fixed inventory is saved separately.

Final paused40125/server39767, debugSession4000b137-a3d6-4eab-9f68-b4ac7b26c524 epoch1, at155,23,385. Health20, food20/saturation6, four raw iron,36 torches, four bread/three porkchops, two logs/six sticks,75 cobble/five dirt; shield302, iron picks157/62,18 free slots. Updated western moss shelf bookmark with the mined floor cells, observed unmined wall iron, lighting resolution and outbound path. Next: continue the visible western frontier or approach the higher observed wall vein, verify the reverse branch and eventually bring ore home. Broader goal remains active.


### D086 — Wall iron, verified western return, and cave mapping direction

Continued from D085 via153,24,386→151,25,384→150,26,386. The previously observed wall iron146,32,382/146,33,382 had no visible higher landing. Enabled allowBreak for a fixed visible-only acquisition scope, keeping placement/inventory/bucket-fall disabled. The first request collected two iron (four→six); newly exposed faces justified a second bounded request. It collected four more and ended below the eight-additional target with no remaining observed sources, no rejected targets. Carried ten raw iron. Restored allowBreak=false and returned from145,32,382 to147,31,383; its survey showed no remaining iron faces. This is observed-vein exhaustion, not a hidden-ore absence claim.

Explored the rising water channel via147,28,391→146,29,393→144,29,395. More upward/waterside candidates and overhead coal were observed, but no deeper opening established. A first-person check toward140,32,405 was occluded by clay143,30,396. Saved western wall iron ledge and western rising water channel world bookmarks with these limits; neither branch is declared a dead end.

Retraced146,29,393→147,28,391→147,31,383→150,26,386→151,25,384→153,24,386→155,23,385→156,23,383→157,23,381→160,23,383→162,23,381→167,23,377→172,23,380→174,22,379→177,22,374. All completed without enabling digging or placement; final job492de340 completed47512. This verifies the reverse western branch, which D085 had left open. cave-western-branch-return.txt records the exact jobs.

User proposed area-weighted air flooding for cave route mapping, preferring broad open caves when torches permit. Paused50607/server50247 at177,22,374, debugSession09f9e0d1-1524-4a40-bec9-a153d911cdda epoch1. Health20, food20/saturation6, ten raw iron,33 torches, four bread/three porkchops. Export cave-wall-and-return.jsonl contains2050 observations with frames, not truncated. Source inspection confirms survey_cave currently selects the farthest standing candidate in each of eight angular sectors; it does not model chambers or passage connectivity. Proposed next design: bounded connected-air geometry plus walkable-floor connectivity, chamber/open-area preference, and short locally constrained movement segments. Avoid treating vertical air connectivity as traversability or exposing hidden ore through geometry mapping. No cave-mapping implementation yet.


### D087 — Area-weighted cave mapping: implemented, tested and narrowed

Added read-only map_cave, a bounded geometry snapshot plus pure connected-air/floor graph and weighted shortest paths. It reports alternative destinations, route cells, short waypoints, bounded counts, truncation and timing. Default radius24/vertical16 sampled79,233 cells; the first live map took21ms capture/78ms total and found17,539 connected air cells,1,414 floor cells and931 reachable positions. Later warm default maps took3–18ms capture/14–47ms total; one radius32/vertical24 map sampled207,025 cells in8ms capture/24ms total. These are observations in this loaded cave, not universal latency guarantees. Computation after the client-thread snapshot uses immutable geometry asynchronously. No chunks are loaded, no ore identities are returned, and geometry does not grant acquisition observation freshness.

The first two inverse area bonuses failed the broad-detour fixture. Replaced them with a bounded narrow-space penalty, distance*(1+weight*(1-min(localArea,25)/25)), which passed. Kept System2 ownership of purpose, torch/mob checks and visit history. The second live default suggestion was an immediate return to the previous origin; chose another returned destination using our travel history rather than adding automatic exploration memory. Added a public-CLI playtest follower: no navigation digging/placement, exact job correlation, two-block local corridor, bounded observation, pause on cancellation/reflex/detour. It is not a new native movement executor or a per-tick corridor guarantee.

HotSwap loaded the factory and new provider without restarting. Normal reload reconstructed the tool registry and reset agent state/recording; it also released the previous server pause. A stale continue then returned internal_error, with the log confirming server ticks were not paused. Read fresh state after future reloads. Automatic lighting was explicitly reapplied.

Live route1:177,22,374→175,22,398,26 floor cells/12 waypoints, all completed; two torches placed33→31. cave-map-first-route.jsonl preserves415 observations, not truncated. A first-person view showed the connected lush chamber and water shelves. Live route2 to151,24,419 used44 floor cells/38 waypoints, all completed, including climbing to157,32,406 and descending into the southwest pocket. Return maps with both default and maximum bounds could reach only100 floor cells, with no route back to175,22,398. The model allowed a two-block drop156,31,406→155,29,406 that its step-up rules could not reverse. cave-map-one-way-drop.jsonl preserves961 observations. This establishes a floor-model limitation, not absence of swimming/building routes in the real cave.

Narrowed ordinary map edges to reversible one-block steps; deeper drops now require a separate contextual plan. Retraced the pocket to155,29,406 through14 mapped waypoints. A fresh exact geometry read showed a useful foothold position155,29,405. Placed one cobblestone against156,29,405, read it back, and confirmed inventory75→74. The new map connected100→783 reachable cells and supplied a return route. Traversed the foothold155,30,405→156,30,405→156,31,406 successfully.

Initially compressed continuous slopes to reduce per-step CLI overhead. A four-step descent completed, but the following three-step descent to166,24,406 cancelled6037 and left the player in shallow water167,23,406. The follower automatically paused; cave-map-return-cancelled.jsonl preserves234 observations. Recorder shows estimatedTicksToGoal0 then BARITONE_CANCELLED, no reflex ownership. Exact cancellation cause remains unresolved; do not claim the mapper fixed Baritone. Reverted downhill compression, keeping each descending landing while retaining flat/uphill merging. Direct recovery to167,24,405 completed6139. Retested the same staircase: four uphill waypoints reached159,31,406 (including two merged climbs) at6562; the revised descent and return completed all20 waypoints to175,22,398 at8072. The previously failing landing166,24,406 completed7143 and was left successfully. cave-map-stairs-fixed.jsonl preserves the final retry. This bounded retry supports keeping the conservative waypoint policy, not general cancellation immunity.

Eleven focused tests and the full build passed after the final changes; Python follower syntax and git diff checks passed. All code changes were HotSwapped; no full client restart. See cave-map-tests.log/cave-map-build.log and the cave-map-* route/map artifacts under run/playtest/2026-09-13. Saved southern cave chamber and southwest cave pocket world bookmarks, including the return foothold and cancellation caveat. Keep this mapper: it produces useful structured routes cheaply, while swimming, diagonal movement, tall drops, visit history and native route execution remain outside this first implementation.

Final paused at175,22,398: health20, food20/saturation6, ten raw iron,28 torches, four bread/three porkchops,74 cobble. DebugSessiona8699b35-dbd7-4d01-b9ae-5664732e5a2d epoch1, clientTick58740/server58397. Client remains running. The broader playtest goal is currently paused in the app; this implementation request is complete without changing that goal status.


### D088 — Chest minecart interaction, camera ownership and persistent inventory history

User identified mineshaft loot carts while the diamond search was underway. Existing use_entity plus inspect_container/transfer_container already handles the chest-style screen; no separate cart tool was needed. The tool descriptions now explain that workflow, including UUID discovery, settled transfer checks and closing without breaking the vehicle. Chest boats share this screen path but were not live tested; hopper carts and animal inventories are not claimed supported by it.

Reproduced an approach stall: use_entity for cart df0dd6c0 at207,25,409 moved from217,31,392 to196,32,388 and remained baritone_chase for over1000 ticks. Paused/exported mineshaft-cart-approach-detour.jsonl. A separate navigate_to207,25,407 completed in221 agent ticks, and close interaction opened the natural loot inventory. Avoiding periodic identical chase-goal refreshes did not fix the stall on retry (mineshaft-cart-refresh-failed.jsonl); reverted that hypothesis and its test. The actual executor was aiming at the cart every tick while Baritone needed to steer along an indirect route. Restricted target aiming to the in-range/visible interaction branch; direct chase retains its own aiming. HotSwap applied this narrow fix to the same paused job f00968a7-8eb1-4acf-a663-d84e3fd3cb16. Without resubmission, it completed at agent18520, about194 ticks later, and opened sync9. Export mineshaft-cart-camera-fixed.jsonl contains33 observations for server68590–68826. This verifies this obstruction route, not every Baritone approach.

Reproduced missing entity-container memory: opening the cart produced no automatic entry. A baseline one-torch withdrawal settled21 carried/4 remaining, but logbook type was empty and coordinates were the player's207,25,407. Server-side detection of an already-open GenericContainerScreenHandler now records its backing entity type, full UUID and current position. Transfers carry the same identity, and item-history queries follow a cart after movement rather than mixing containers at the same coordinates. Old world JSON entries lacking the field remain readable. No unopened loot inventory is inspected.

Fifteen focused tests and full build passed (minecart-tests.log/minecart-build.log). One graceful client restart loaded changed record layouts. Reopening the same cart recorded minecraft:chest_minecart, UUID df0dd6c0-d4da-4989-8139-6c7c3d4e4d76, position207,25,409 and contents before clicking. Withdrew4 torches,3 bread,2 ingots,3 glow berries and1 name tag; settled inventory confirmed25 torches,6 bread,2 ingots and the other loot. Deposited and withdrew one cobble; settled total returned71 and server history recorded both exact deltas. Cart retains7 rails. All15 new cart entries were read back from the world's interactions.jsonl. mineshaft-cart-logbook-fixed.jsonl exports218 observations without truncation; new-open/transfers-settled/transfers-logbook text files preserve CLI evidence. Moved-cart identity and same-position distinct carts have unit coverage, not a live movement test.

Diamond search before this interruption explored the eastern upper arm and returned through observed196,37,393→239,22,399→250,14,391. No diamond observed. Native navigation to266,13,381 failed CALC_FAILED at263,13,381; paused and returned through prior stances for the cart work. This bypassable route failure remains unresolved. Opening LAN after restart returned an internal error because the session was already automatically hosted on25565; no gameplay blocker or patch. Current client remains alive at207,26,405, health20,10 raw iron,25 torches,6 bread and3 porkchops. Continue the mineshaft/deeper cave search.


### D089 — Continued mineshaft loot search after the entity fix

Resumed gameplay after D088 and opened six additional natural carts:306b1a4e at186,24,440;118ee2b0 at185,25,459;2960e3ce at190,26,487;68de4000 at186,29,502;a114f5e8 at193,29,514;97d8ce89 at192,28,564. Exact opened screens and settled transfers are saved as mineshaft-cart2 through cart8 artifacts (cart7 remains unopened). Rails were left in place; useful loot was withdrawn. Seven carts total including D088, no diamonds. At the southern checkpoint inventory held7 ingots,10 raw iron,25 coal,43 torches,11 bread,3 golden apples,22 glow berries,8 melon seeds,5 pumpkin seeds,14 redstone,21 lapis,4 name tags. The three porkchops became two after confirmed consumption restored food14→20/saturation12.8. Eight free storage slots remain. This supplies farming and further cave exploration without returning home first.

Native no-break/no-place travel207,25,407→186,24,440 completed. The floor mapper could not supply that earlier exact target (including one out-of-bounds request), so those failures do not prove no real route. From186,24,456 the connected-air map supplied a24-segment route to190,26,487 via199,25,456 and199,26,467; every guarded segment completed. Then native entity approaches opened the next two carts, providing additional live coverage of the camera fix. Brief inspect_container calls made before task completion correctly returned container_not_open; subsequent terminal jobs and opened screens established success.

Cart86715f9e at214,32,529 remained inaccessible from lower tunnel215,28,529. use_entity failed target_unreachable with placement disabled and again with it enabled; digging stayed disabled. A radius24/vertical12 floor map returned target_not_reachable. Paused/exported both attempts (mineshaft-cart7-unreachable.jsonl69 observations; mineshaft-cart7-foothold-failed.jsonl94). No root cause or new fix claimed; bookmarked the unopened cart with contents unknown. Other loot carts bypass this local access limitation. Native movement to the southern cart succeeded using four cobble71→67; a local floor-only map there reaches only34cells, so its return was not presumed reversible.

Saved southern mineshaft loot route in world memory with traveled stances and the upper-cart caveat. mineshaft-loot-run.jsonl preserves2430 observations without truncation. Full UUID container history is persisted automatically in the world; external notes record route decisions rather than duplicating all stock. Started the return toward the known deeper cave, since the local mineshaft remains aroundY24–32 and no diamond appeared in inspected loot.


D089 return follow-up: southern cart→215,28,529 completed. A subsequent189,28,513 goal cancelled at191,28,513; paused/exported mineshaft-return-cancelled.jsonl (513 observations). The next goal toward189,28,501 was interrupted twice by combat, both holds cleared at health20, then cancelled near189,28,502. Saved mineshaft-return-combat-cancel.jsonl (768 observations). Continuing to the next traveled stance190,26,487 bypassed this, followed by successful186,24,456→186,24,440→207,25,407→239,22,399→250,14,391. No cancellation fix claimed.

The known deeper route then completed every leg through276,14,371→293,12,360→306,18,355→320,21,346→322,13,351→324,-2,342→318,-5,328→317,-5,313→320,-6,296. Direct276 landing bypassed the earlier failed266,13,381 waypoint. Fresh first-hit survey at320,-6,295 observed deepslate iron321,-8,296, no diamond. Started a39-segment mapped western continuation to288,-3,275. Re-read the persisted world logbook: all seven opened cart UUIDs have distinct latest stock, consisting only of the rails intentionally left; the inaccessible cart has no fabricated inventory entry.


### D090 — Stop at false navigation arrival on a cave ledge

The western continuation completed11/39 mapped segments, then the corridor guard paused segment12. Its target was318,-10,280 from318,-9,281; actual settled position became318,-12,279, corridor distance2.236. Health20, food20, reflex IDLE throughout, no digging or placement enabled. The navigation job dff9a260-2c06-46a8-8e4d-cb26e9a887a9 nevertheless reported COMPLETED at agent14888. Saved diamond-west-corridor-drop.jsonl,316 observations with frames, no truncation, plus map/status/survey artifacts.

Recorder snapshot14865 shows grounded318.706,-9,281.537. At14885 the player is airborne at318.579,-9.0784,280.288. At14887 task.completed says Goal reached with terminationCause BARITONE_CANCELLED; task decision resets at14888. At14906 the player is grounded318.506,-12,279.560. This reproduces arrival being reported before a stable landing, followed by departure from the intended ledge. Source corroborates the inadequate completion predicate: BaritoneTaskExecutor.cancelledStateFor converts cancellation to COMPLETED whenever LiveBaritoneFacade.navigationGoalReached matches playerFeet block coordinates. The predicate does not inspect grounded/settled state. Do not claim that this alone caused the physical overshoot, or that merely adding onGround would fix movement.

The landing is not demonstrated to be a trap: a fresh floor map supplies a four-waypoint return via320,-12,279→320,-10,281→319,-9,281→318,-9,281, still untested. Current first-hit survey reports no diamond; deeper standing candidates320,-20,274 and320,-19,275 are visible but not validated routes. Saved western deep cave arrival failure as a world bookmark. Stopped the search at this navigation completion/safe-landing boundary, per the instruction to pause serious progression problems for recorder diagnosis. No new navigation patch or successful return claimed.

Checkpoint: client session38713 remains alive, paused debugSession4aa85a97-a101-43d0-a3ed-382e48fa9483 epoch1, client14930/server14691, position318,-12,279. Health20/food20/saturation12.8,28 torches,25 coal,11 bread,2 porkchops,3 golden apples,7 ingots,10 raw iron; no diamonds. The chest-minecart feature remains live verified and committed independently; paused broader app goal status unchanged.

### D091 — Report physical episodes and actual navigation outcomes

Implemented the requested observation seam: System1 reports bounded `player.physical` episodes for significant falls, movement without directional input, burning and low air. Payloads preserve origin/current position, velocity, contact facts and active/recent task target/status. Existing damage observations now include position/task context. The existing semantic pipeline and flight recorder carry these observations; System2 decides recovery. No corrective locomotion or goal retry layer was added. See docs/physical-observations.md for thresholds, lifecycle resets and inference limits.

Navigation completion now requires supported arrival rather than just a matching airborne feet block. The initial immediate check was too strict: the live return's ordinary step to319,-9,281 cancelled at agent235 just before landing correctly. Paused/exported physical-immediate-arrival-regression.jsonl before changing it. The replacement observes up to10 ticks after path end, issuing no movement, so a normal step can land before classification. Automated coverage asserts one navigation dispatch, preserves cancellation/failure variants, and rejects an unsupported AT_GOAL event. Two later ascents completed the same step and returned to318,-9,281; the final four-waypoint ascent completed at agent349.

Repeated original318,-9,281→318,-10,280 descent twice after the patch. Both attempts cancelled at actual318.52,-10,279.80 instead of falsely completing; terminal notices included target, actual position, velocity and grounded state. These retries did not reproduce the original full three-block overshoot: do not claim its movement cause is fixed. Saved physical-original-descent.jsonl and physical-final-descent.jsonl. A deliberate ordinary navigation to the previously observed lower landing318,-12,279 then produced a two-block fall start/end with health20 and completed after landing.

The first HotSwap exposed a validation trap: raw `player.physical` events appeared, but the preloaded static routing map lacked the new profile. Config reload alone did not rebuild that static map. Made routing profiles instance-owned and repeated HotSwap plus runtime reload; no client restart. Strengthened the test to exercise the actual event pipeline and check semantic delivery plus reflex-owned trigger suppression. Final physical-planner-fall.jsonl records fall start at agent598 and landed end at599, each with event_pipeline route emitSemantic=true, emitTrigger=true and triggerType=SYSTEM. The target task completed afterward. Export has210 observations, frames included, no truncation. Codex driver disables embedded model inference, so this proves delivery/wake-up routing, not an autonomous LLM recovery decision.

Validation: seven observer tests cover ordinary steps/jumps, fall context across task changes, slow and ordinary water displacement, episode coalescing, hazard escalation/clearing and lifecycle resets. Runtime pipeline, arrival and existing damage tests passed; full Gradle build passed. Fall sampling, supported arrival and routing are live verified. Water displacement, burning and low-air episodes have automated coverage only. No water was placed in the survival cave; inspection showed the immediate test site borders a lower drop. No environmental recovery or universal involuntary-motion classifier is claimed.

Checkpoint: same client PID28673 remains paused at318,-12,279, health20/food20, debugSession a0da5d23-3647-426e-9978-541df57e9873 epoch1, client16694. Export before any reload. Broader paused goal remains unchanged; no diamond found. The four-waypoint return via320,-12,279→320,-10,281→319,-9,281→318,-9,281 is now live verified.

D091 validation correction during the next streaming feature: the last added reflex assertion used the shared DROWNING fixture in AWAITING_PLANNER, which correctly still owns actuation. Its focused test had failed, but a subsequent evidence-print command masked the shell exit status. The earlier full build and live fall/routing evidence stand; the final added assertion was not passing as reported. Corrected the fixture to IDLE for a released reflex, and keep build exit status separate from evidence commands.

### D092 — Stream the configured planner and run the actual planner

Implemented SSE for the OpenAI-compatible planner with incremental reasoning/content/tool-argument previews. Complete envelopes still pass the existing parser and execution boundary; partial tokens never execute actions or enter accepted history. Added an active-generation preview to the in-game conversation projection and the browser LLM transcript. The HTTP body is covered by the configured timeout; EOF without a finish reason fails. Previews are bounded to16,384 characters and recorder updates stay on the existing five-server-tick cadence.

The current launch had codexDriver=true, so one normal runClient relaunch was necessary to enable the actual embedded planner. Current normal client PID70606, launcher26729, same bridge /tmp/airicraft-survival-playtest-0910.json and same saved world. Config was left unchanged: model qwen3-8-27b, existing provider and30-second timeout. No embedded planner mock was used. Initial world-triggered calls reproduced a parser failure on optional null streamed tool_calls; paused/exported planner-stream-null-fields.jsonl, added that provider shape to the transport test, and HotSwapped OpenAiChatStream without another restart.

Live assessment: Qwen discovered and called inspect_inventory, inspect_world and inspect_nearby_entities, stayed at318,-12,279, and produced an assessment. Browser DOM and a rendered screenshot showed STREAMING with growing reasoning before completion. Native Minecraft UI access was unavailable, so the in-game overlay is source/projection-tested rather than visually verified here. The initial150-word assessment needed a later split chat response; no chat-policy change was made.

Live action: Qwen was asked to return along the previously validated stances without breaking or placement. It discovered navigation, issued each leg and reached320,-12,279→320,-10,281→319,-9,281→318,-9,281. Four distinct jobs completed at agent8429,8607,8683,8926, each with grounded actual position and matching target. Recorder planner-stream-live-success.jsonl retains965 observations and all four completions. Health20/food20, no block or inventory loss; no new diamond search progress is claimed.

Recording inefficiency found during this run: each preview update initially copied the full prompt, evicting older history under the64MiB cap. Paused/exported before changing it. Reused existing shared-context storage for one llm_request envelope per call; updates reference it and the dashboard resolves the reference. After HotSwap, calls24/25 retained59KB/62KB request bodies once each, with9/19 lifecycle samples whose largest payloads were5.4KB/6.6KB. The provider completed inventory verification and a final report; the browser still rendered the shared request correctly. planner-stream-compact.jsonl contains159 observations from server9300 onward. Both success exports report truncated=true because earlier observations had already been evicted before this optimization; do not claim a complete10-minute recording or that lost data was recovered.

Automated checks cover tokens arriving before completion, Unicode reasoning, fragmented executable arguments, optional null fields, final usage, body timeout after headers, incomplete-stream rejection, request correlation across HTTP threads, bounded previews, late-token isolation after detach/reset, and shared-envelope reuse. Existing planner/orchestrator/overlay tests and full Gradle build pass. See docs/planner-streaming.md for the contract.

Checkpoint: actual planner enabled, codexDriverActive=false. Game paused at318.644,-9,281.501, health20/food20, debugSession b1202fe1-e309-4a90-8fcc-dc01b6ca5cef epoch1, client11361/server10432. Chrome dashboard left open on LLM transcript. Broad paused app goal unchanged. No further client restart required after the initial mode switch; provider compatibility and recorder changes were HotSwapped.

### D093 — Persistent planner objectives and live Qwen self-play

Added a planner-owned goal lifecycle separate from action graph and navigation jobs: set, change, inspect, and finish with success/give_up. One bounded current record persists in `airicraft/planner-goal.json` in each local world; changing the objective creates a new identity so stale completions cannot finish it. Active objectives schedule through the existing trigger pipeline after a quiet idle interval, independently of ambient ideas or human chat. Busy jobs/graphs, reflexes, pending planner/tool calls, external control, disabled/degraded planners and unavailable actuation suppress that continuation. Current goal state is projected into every planner prompt and survives compaction. See `docs/planner-goals.md`.

Baseline reproduction: a deterministic DialogueRuntime test supplies an unfinished-work plaintext reply and polls 200 more ticks without events. Exactly one model call occurs and the planner remains idle. The old prompt also explicitly discouraged autonomous survival and repeated useful tool follow-ups. These instructions were changed alongside the scheduler. The live baseline instead exposed repeated `inspect_action_goal` requests while torch preparation was REPLANNING: execution action-graph-857b4ae4-7ae2-4cb5-963d-4ceb69e53589, traceEventCount 41908. This is not evidence that the live run reached the exact reply-to-idle bug. Paused and exported `run/playtest/2026-09-13/planner-goal-before.jsonl` before edits; action-graph replanning requires separate diagnosis.

The active save was `planner playtest`, not the previous cave expedition's `another world`. Fresh inventory/context was used. Enhanced HotSwap plus config reload applied the feature without restarting the client; codexDriverActive remained false. Full Gradle build passed, plus focused goal/dialogue tests. `c343d760` contains implementation, tests and interface documentation.

Live request timeout: at the configured 30 seconds, three Qwen attempts streamed partial reasoning toward set_planner_goal but timed out before a complete callable result. Paused/exported `planner-goal-timeouts.jsonl`. Temporarily changed only local `requestTimeoutMillis` from 30000 to 120000 and reloaded. The next resource/memory run completed requests in roughly 1.9–5.7 seconds; later requests were much slower again. This is variable live request latency, not proof that changing the goal scheduler fixed provider performance.

Live bounded success: Qwen created goal 2bfdd265-9294-4d9f-9dc5-1bc4285243be, remembered playtest-start at -4,138,6, collected two spruce logs, yielded while collection ran, recalled the bookmark, and finished SUCCEEDED with inventory/place evidence. Server ticks: set 22149; resource action 22337; plain status 22456; recall 22622; finish 22662; final reply 22703. `planner-goal-success.jsonl` and `planner-goal-success.png` preserve the result, with no recorder truncation/drops. The return turn came from the existing terminal TASK UPDATE, so this proves live lifecycle and multi-stage completion, not the new no-event continuation path. That distinction corrects an initial progress-update inference.

Isolated continuation proof: changed the active torch/shelter objective to verifying the existing bookmark, with an explicit diagnostic instruction to yield immediately. Qwen called change_planner_goal at server tick 29222, returned the exact plain reply `Goal recorded.` at 29273, then received the scheduler's `GOAL CONTINUATION` at 29291 with no new human message or task completion. It called recall_place at 29341, finish_planner_goal at 29384, and replied `Verified and finished.` at 29434. The replacement goal c52355eb-eb70-4bb4-94db-ce9562758ccb is SUCCEEDED. This directly verifies the new no-event loop, goal change, and explicit termination with the real configured model. Saved `planner-goal-continuation.jsonl` and `.png`. This longer recording reached its 64 MiB cap: its manifest correctly reports dropped history/truncation, although the above continuation sequence is retained. The earlier complete bounded run is separately preserved.

During the long model wait, a skeleton attacked. System 1 defended with the held spruce log, killed the skeleton (Monster Hunter and bone pickup), and released the reflex at tick 5912; health was about 13/20 afterward. This verifies existing reflex behavior in this run, not shield usage. Torch/shelter preparation did not advance far enough for a completion claim: the planner set its objective but long requests and the interruption preceded the diagnostic goal change. The earlier REPLANNING observation is a candidate to investigate, not a proven permanent hang or a fixed crafting defect.

Final state: restored local requestTimeoutMillis to its original 30000, reloaded without restarting the client, and verified the completed replacement goal appears in the fresh runtime prompt. Game paused at client tick 31433, debug session 20ae5a71-b1a3-423d-bd64-9f5abb6e080c, epoch 1. No active planner objective remains. Outstanding playtest issues: variable model latency/timeouts, redundant action-status inspection in the baseline, recorder capacity pressure during longer sessions, and unresolved torch-graph progression. No diamond/shelter/torch completion is claimed.

### D094 — Break the torch/coal illumination recovery loop

Resumed the real configured planner in `planner playtest` with the existing survival objective. The first torch graph crafted a wooden pickaxe, then failed nine dispatch preflights on the same coal-mining illumination condition: `action-graph-70028060-1554-4bf9-9db3-b8e512ca2a2e`, `replanCount=8`, `traceEventCount=60214`, terminal `missing_fact`, typed recovery `missing_item`, `loaded_target_unilluminated torchCount=0`. Paused at client33512 and exported `torch-light-cycle.jsonl`. A local assertion over the inspected recovery history reproduced nine repeated denials. This establishes a repeated-recovery defect; it does not establish that ordinary REPLANNING is a permanent hang.

Changed only the graph dispatch classification of an illumination-denied mining method from MISSING_ITEM to ENVIRONMENT_CHANGED. The existing resolver then excludes the failed method for this execution and considers other routes instead of immediately retrying the same unmet lighting condition. It does not override the illumination policy or remove acquisition knowledge globally. HotSwapped; focused action-graph/coordinator/runtime tests and full Gradle build passed. Commit `14161b06`.

The post-fix execution `action-graph-392b6283-e5ce-4fad-b37d-6f53d58125b6` completed the eight-torch goal. Its route mined cobblestone, made a stone pickaxe and furnace, gathered more spruce logs, smelted charcoal, and crafted the torches. It still tried coal with another tool before eventually choosing charcoal, so three method-level illumination rejections remained; the initial assertion expecting only one denial of any kind was too broad for distinct method alternatives. The improvement is a successful alternative route, not elimination of all redundant work. Final `agent evidence` confirmed `minecraft:torch: 8`, graph SUCCEEDED with `goal_satisfied` and `replanCount=3`. `torch-alternative-progress.jsonl` preserves an intermediate checkpoint; `torch-goal-success.jsonl` and `.png` preserve completion. The final recording reached its capacity and reports truncation; early incidents were exported separately.

Corrected an intermediate inference: the first eight cobblestone initially supported a stone-pickaxe detour, not directly a furnace. A later actual crafting event verified the furnace, and actual inventory verified final torches. The graph also briefly planned an acacia-wood charcoal recipe despite collecting generic local logs; replanning after real spruce-log acquisition corrected it, but extra acquisition/planning is an efficiency issue.

Repeated 30-second LLM deadlines again interrupted the planner before it could submit a retry. Saved `torch-retry-timeout.jsonl`, changed the local playtest `requestTimeoutMillis` to120000, and reloaded. Unlike D093's temporary setting, 120000 remains in effect for the ongoing autonomous playtest. Model/provider unchanged, codexDriverActive=false, client not restarted. The saved active objective resumed without another human prompt after this reload.

Health fell from13.17 to5.67 before the post-fix mining run: the earlier export records three2.5-damage hits from two zombies. The later two-block acquisition fall caused no damage. At the inspected checkpoint hunger16/20 and three rotten flesh were available. Steered Qwen to address food/recovery. It attempted `eat_food`, but the shared graph mutation gate denied it in both REPLANNING and WATCHING, even without an active primitive. This is the next confirmed blocker to diagnose: distinguish graph ownership from actual hand use, while ensuring eating and primitive dispatch cannot interfere. Do not simply allow simultaneous actuation.

Game paused after torch completion at client40505/server39239, debug session ba1e321d-e9eb-4ac8-9a12-6c885d432c1a, epoch1. The planner goal c4cca467-3094-40a2-aaea-c6fa3558372a remains active for a remembered suitable shelter site. Next: fix food admission/actuation coordination and continue actual-planner survival. Recorder pressure, premature progress narration, method/tool detours and unrelated wood-species assumptions remain observed weaknesses; no shelter completion is claimed.

### D095 — Eat between action graph primitives

Confirmed the D094 denial came from treating eat_food as a graph mutation even when no primitive held the player. Changed it to task mutation admission and held graph advancement while the existing item-use controller consumes food. Active tasks, reflexes, death and existing item use still deny admission; the furnace and server continue ticking. No additional scheduler or actuator lock. Two regression tests failed before the fix; focused tests and full build passed. HotSwap applied without restarting. Implementation commit e8999dd3.

Actual Qwen started charcoal reserve graph action-graph-bbf2d40f-8783-403b-852d-2eb6955fd6b8, received the expected active-task rejection while mining, then successfully ate between primitives. food.eaten event183 occurred at agent7084; next task.submitted event184 at7111. The same graph continued without cancellation. Saved food-graph-coordination.jsonl, food-graph-events.txt and food-graph-planner-state.txt under run/playtest/2026-09-13. Health later reached10.07/20, compared with5.67 before eating. This verifies coordination, not completion of the charcoal graph or automatic emergency preemption.

### D096 — Remove repeated recipe data from recorder snapshots

Measured the live recording: planner.baseRequest.missionExecution.evidence copied about1MB of recipe knowledge into each runtime snapshot, despite the root mission already referencing shared context. Projected that nested mission through the existing recipe catalog representation and moved canonical/projected conversation sources into shared context. Snapshot schema4 preserves original request facts and uses exact sequence references. Current and historical request recipe catalogs differ in this live incident, so they use separate bounded context keys to prevent alternating versions from being re-appended every snapshot.

Regression coverage checks compact payload size, preserved inventory/safety/tool-result facts, unchanged original requests, alternating current/request catalogs and seek reconstruction. Full Gradle build passed. HotSwapped one collector class without reload/restart. In charcoal-option-failure.jsonl,26 retained schema3 samples averaged1,137,813 bytes;15 new schema4 samples averaged31,493 bytes. All90 context references from new snapshots resolve in the export. Earlier evicted history remains lost; this does not prove a complete10-minute retention window under every workload.

Resuming exposed the charcoal reserve graph's separate terminal failure: eight replans, smelting_option_not_found for inferred spruce-log charcoal, while holding two spruce logs, one coal and a furnace. Paused at client43803/server42546, debug session33d7de24-aa8d-446c-addb-0403f65963c6 epoch1, exported charcoal-option-failure.jsonl. Player5.70,125,4.50, health10.07/food16. Diagnosis pending; no charcoal reserve or shelter completion claimed. Also observed ambiguous prompt wording: “There is no active goal right now” describes direct navigation state even while a planner objective/action graph exists; Qwen explicitly struggled to reconcile this with completed-job context.

### D097 — Reachable furnace ledge and explicit job context

Paused-world scan at player5,125,4 found only occupied feet/head air within the previous furnace placement height range. A supported open ledge at4,127,4 was omitted because candidate search was limited to y offsets0,-1,+1. Expanded the existing shared planner/executor candidate list to include +2/-2; normal support, collision, preservation and interaction checks remain. A regression for the captured ledge failed before the edit; full build passed. HotSwapped SmeltingPlannerService and asked actual Qwen to retry the two-charcoal goal from the same position. No successful live placement or charcoal completion has been observed yet: model requests remained slow, and the user requested a new two-role System2 design before this retry finished.

Separately reproduced misleading ambient context in the actual prompt: completed collection jobs were labelled Active job, and absence of direct movement was labelled no active goal despite a persistent planner objective. Added explicit job state and direct-action-goal labels, removed the duplicate active-job prefix, and renamed the evidence label. Regression and existing context/orchestrator/goal tests plus full build pass. Labels are HotSwapped, with fresh model-request verification pending.

The user then requested lower thinking, followed by a stronger replacement: fixed non-thinking controller plus a separate thinking planner that takes over gameplay tools for a delegated task and returns control. Both retain independent prefix-stable histories. Copy relevant context on entry and actual actions/results/outcomes back to the controller. The interim plannerReasoningEffort config support is built and HotSwapped but no runtime config reload has applied low. Game paused at client47133, debug session11523ca4-c99e-4818-9d45-0bb088e26fc5 epoch1; saved planner-before-low-thinking.jsonl before any reload. Continue with two-role implementation; do not rotate effort on one history.

### D098 — Independent controller/thinking sessions with evidence handoff

Implemented the requested two-role System2 mode. Controller profile is fixed at reasoning_effort=none; the delegated profile is independently configured (local Qwen medium). Each owns its registry, fixed prompt/tool schema, accepted wire history, compaction and cache key. Shared generation IDs and recording sequences prevent cross-role debug collisions. The existing action runtime remains shared, with one decision owner. delegate_task waits while the thinker operates; return_control requires idle gameplay work and ends that thinker turn. Failure returns control with evidence. Context transfer includes recent visible messages/tool exchanges and fresh facts; return includes observed tool exchanges/events, incoming user guidance, final inventory/location/task facts, and a separately labelled planner outcome. Bounded omissions are explicit. Goal data moved outside the system prompt; stable histories retain accepted notices and tool exchanges instead of reconstructing them each turn.

Full Gradle build passed (1,192 root tests, two skipped), including real-orchestrator deterministic tests of two handoffs, plaintext continuation without a global goal, exact prefix preservation, safety-epoch changes, reset and late-return rejection. Local HTTP tests verify fixed distinct effort/cache identity. HotSwap of38classes succeeded, but config reload failed because the compiler-generated enum-switch helper changed identity; the existing recorder then accessed its newly typed, uninitialized sequence field. The prior incident was already exported. Restarted this structural migration once; normal compatible changes still prefer HotSwap.

Live actual Qwen, codexDriverActive=false, same planner playtest save: controller delegated charcoal production (e10b7b28-f21a-4eb4-9eca-c3896af10391). Thinker navigated from5,125,4 to5,129,11, placed furnace4,128,12 and started smelt-process-5f732c72-75cb-494a-8985-9a6c8bc07b44. It repeatedly inspected instead of explicitly collecting, incorrectly assuming automatic output collection. Paused/exported two-role-smelting-wait.jsonl, clarified the collection contract through planner chat, and resumed. Thinker exhausted the existing per-turn tool budget; the controller received a failed report containing42observed entries, including the clarification, then collected and independently observed2charcoal with8torches intact. Its reply accurately distinguished the thinker's placement/smelting from its own collection. This proves live failure return and controller understanding, not yet explicit successful return_control.

A second delegation inspected inventory but repeatedly encoded remember_place.position as a JSON string rather than its required object. Both roles encountered that parse failure. Recorded two-role-memory-schema.jsonl; provider/schema interaction cause is not established. Second thinking history retained earlier work; provider call50 reported53,632cached input tokens of54,105. Effort/cache keys remained none/controller and medium/thinking. Larger initial full-tool prefixes are a cost tradeoff; these measured hits do not guarantee retention.

Follow-up blocker reproduced: a new operator instruction arrived while paused after a model response had completed. Routing ALLOW/emitTrigger=true and queuedTriggerCount=1 proved it entered the system, but the ready-response shortcut let the old turn continue issuing tools without the new instruction. Exported two-role-guidance-routing.jsonl and paused. Fixing operator priority before the final explicit-return check. Furnace placement here used explicit navigation/place_block; D097's +2 automatic placement candidate still lacks isolated live proof.

### D099 — Operator guidance supersedes unapplied model output

The D098 paused-world reproduction showed a direct operator trigger queued behind a completed model response. PlannerOrchestrator.submit deliberately returned early when that response was ready; its ensuing tool chain could keep acting without the newer instruction. Removed that shortcut: newer direct guidance supersedes replaceable, unapplied output using the existing coalescing path. Pending side-effect tools still finish and journal their results before handoff. Autonomous events retain their non-superseding behavior. Updated the old completed-reply-wins contract test to assert fresh guidance priority for both ready replies and ready tool calls; both cases failed before the fix. Full build passed. HotSwapped the orchestrator without resetting role histories and submitted a fresh cancellation/verification instruction.

### D100 — Reserve thinking for spatial design and constrained planning

Updated the controller prompt per user steering: routine resource gathering, known-location navigation, crafting, furnace operation, inventory inspection and known transfers stay with the non-thinking controller. Delegate substantial spatial reasoning or planning across constraints: shelter design, farm site/layout selection and inventory/storage organisation. Task length or one tool failure alone is insufficient. Explicit diagnostic delegation remains allowed. Compatible HotSwap plus config reload applied the changed fixed prompt without a client restart. Exported two-role-before-spatial-policy.jsonl before resetting histories; normal handoffs still preserve both histories.

Live spatial-policy-success.jsonl verifies the new prompt in controller wire requests, none/controller vs medium/thinking profiles, and an explicit successful sequence: controller delegate_task call2 → thinker inspect_inventory call3 → thinker return_control call4 → controller inspect_inventory call5. The report included actual tool evidence, success outcome and no omitted entries. No further thinker call occurred for that completed delegation. Controller handled recall_place, navigate_to and collect_resource directly afterward, then delegated actual shelter design/execution at call17 with spatial dimensions, fixture-preservation and doorway requirements. This is an observed example of the intended routing, not proof of policy compliance on all future tasks.

Inventory was now empty, not the earlier2charcoal/8torches: before the prompt reload, recorder events show creeper damage at agent7065, zombie damage7565, fatal zombie damage7707 and respawn7708. Preserve this distinction: earlier charcoal collection succeeded, but the later survival run lost the carried items. The planner's updated shelter objective still contained stale inventory text after death; the final diagnostic and controller inspection correctly observed zero charcoal/torches. Shelter completion and item recovery are not claimed.

Handoff audit also corrected two state-reporting details: refresh the idle gate every client tick even while a graph suppresses idle scheduling, and pass freshly sampled WorldEvidence into handoff start/finish even when the mission record is idle. Idle-only return and actual tool evidence remain enforced. Full Gradle build passed after these changes; compatible HotSwap applied them without resetting the newly tested prompt/history. The observed empty inventory exposed the missing idle-mission final-facts fields; the final fresh-facts change is build-verified, with the next real return providing its live check.

Remaining observed weaknesses: unnecessary smelting polling before collection; JSON-string coordinates in remember_place (source schema is a string-current/object union, provider/model interaction not isolated); oversized visible chat repair detours; full growing conversation context can still shorten recorder retention under the64MiB cap. Preserve exports and report omissions. Resume the actual controller/thinker playtest; pause and diagnose the next substantive failure.

### D101 — Make background smelting collection explicit

D098's live thinker repeatedly polled inspect_smelting/inspect_inventory and exhausted its tool budget while assuming output would be collected automatically. Actual collection by the controller succeeded, so this was a tool-contract comprehension failure, not proof of a stalled furnace. Clarified the returned smelt_items acceptance, inspect_smelting summary and output-ready trigger: the loading job starts background cooking; output remains in the furnace until collect_smelted_items, followed by inventory verification. No tool schema, fixed prefix, process lifecycle or collection behavior changed. Existing exact-output tests updated; full Gradle build passed. Paused/exported shelter-before-smelting-guidance.jsonl and HotSwapped compatible classes without config reload or history reset. A new autonomous smelting run is still needed to verify that this reduces polling.

Meanwhile actual Qwen completed the20-log collection, decided the shelter required3more, gathered those too and reached23spruce logs. It is now crafting/building under the existing shelter delegation. Health8.53 at this checkpoint, no further death observed during these collections. Both the collection completion and the retained shelter history are live evidence; shelter completion remains unproven.

### D102 — Trial incremental construction before adding tools

User observed excessive shelter reasoning. Paused at client23616/server23218, debug session4f8e69ea-ac24-4c24-babb-d99f031becc5 epoch1, and exported shelter-overthinking-review.jsonl. Call37 spent120seconds repeatedly reconsidering footprint, wall layers, doorway, material count and reach, then timed out without an action; call38 began revisiting the same dimensions. The pause snapshot still held92spruce planks at player-5,134,4. The controller's delegation contributed ambiguity: it called the footprint3x3 while explicitly specifying a5x5 exterior around a3x3 interior. These are observed planning inefficiencies; no placement-executor failure is established by this incident.

Resumed with live guidance to build a small inspected/reachable section, inspect the result and adapt, preserving fixtures and an exit. Clarified interior versus exterior dimensions and allowed a workable shelter height. Reversible changes to newly placed blocks are preferable to repeatedly enumerating the entire structure. This is a prompt-only experiment through the actual thinker's incoming guidance; no schema, reasoning profile or runtime reset. The next request entered normal conversation compaction, so construction improvement remains unverified pending actual placement results.

After compaction, the thinker submitted a four-block batch. Actual task.completed event149 at agent18357 verified all four placements, job-c9a8cd4d-4459-43b0-8356-6220fcfa7070. The next five-block job placed two, then failed on target0,134,2 with target_not_visible/safe_stand_position_not_found. Inventory86planks and nearby6planks corroborated partial progress. The thinker correctly reported partial completion and navigated closer instead of reconsidering the entire design. Saved incremental-shelter-first-blocks.jsonl. This is practical evidence that the combined guidance, dimension clarification and compaction allowed incremental construction; it does not isolate each contribution or prove shelter completion. A subsequent sparse client frame at server29162 visually shows the first plank wall section beside camp equipment.

Promoted the policy into both role prompts: controller delegates purpose/real constraints with unambiguous dimensions; thinker builds, inspects and adjusts reversible sections. Full build passed and provider HotSwap applied. Existing frozen role prompts intentionally remain unchanged until the next normal session rebuild; this live run already has the equivalent incoming instruction, so no config reload interrupted construction.

### D103 — Non-thinking conversation compaction

The compaction request inherited medium reasoning from the thinker and timed out after120seconds before immediately retrying, delaying the new building guidance. Paused/exported shelter-compaction-timeout.jsonl before editing. Made compaction requests use fixed reasoning_effort=none; existing separate compaction client/cache identity and gameplay role profiles remain. A local HTTP regression failed before the change and now verifies none, the separate key, JSON response format and omitted tools. Existing repeated-role tests verify gameplay profile/cache stability; full Gradle build passed. HotSwapped one chat client class without restarting or resetting histories.

Important causal limit: the original medium-effort retry completed in46seconds at18:22:47, before the18:25:36 HotSwap. Recovery from that timeout therefore does not validate the patch. A future compaction request is needed for live performance evidence. The thinker then processed the incremental-building instruction, navigated to the northwest corner and began inspecting; construction is not yet verified.

Fresh live verification: at18:33:08 a103-message compaction request used none with the existing separate :thinking:compaction key. It completed successfully at18:33:30, then the gameplay thinker resumed with medium. Saved nonthinking-compaction-live.jsonl. This22-second sample is faster than the earlier46-second successful medium retry and120-second timeout, but workloads/provider conditions differ; it is not a controlled speed comparison. Construction meanwhile reached14placed planks, including successful support and wall work at the south edge.

### D104 — Center bounded inspection on the requested area

Actual thinker calls90/91 requested inspect_area centers0,134,4 and-2,133,6, both with horizontal/vertical radius5 and default32results. Their bounds differed (-5,129,-1..5,139,9 versus-7,128,1..3,138,11), but both returned exactly the same32positions in the same order. Source sorted all records by distance from the player before truncating, so shifting a broad inspection toward a construction section could still omit that section. The model then repeatedly inspected and inferred around incomplete terrain information.

Paused at client38494/server38158, debug session8ba56757-192c-460b-b60b-ec142f7316d6 epoch1, and exported shelter-query-focus.jsonl. Bounded inspect_area results now prioritize the requested bounds' center (self scope remains centered on the player), while each block's distance still means player distance. Results identify the selection center, mark truncation and recommend narrowing the query; only returned positions count as inspected. No new tool/schema or gameplay-controller behavior. Regression using both captured neighborhoods failed before the ordering change; full build passed. HotSwapped query classes without resetting histories, submitted the same two queries through actual Qwen as a live check, and resumed.

Live query-focus-live.jsonl verifies both responses: first returned position matches the requested center in each; both returned32blocks and their sets now overlap by only4positions rather than being identical. The thinker then used radius2 inspections centered on the southwest construction section and navigated there. This verifies query selection and useful local follow-up; it does not yet prove the remaining wall/support work succeeds.

### D105 — Inspect all unread modification targets in one response

Upper-wall placement reached27planks: task.completed event221 verified seven targets in job-46fda54f-e15a-471f-95ee-d18fbf73b2c3. The sparse view shows a two-block-high wall section. Before that batch could run, thinker calls119/120/122/123 repeatedly hit target_not_inspected for different cells; the existing fallback read only the first missing target's neighborhood per response. This caused avoidable LLM round trips and contributed to the tool-budget exhaustion that ended delegation e9f737f0-cb62-474f-b463-0bd1111699d7. A separate over80-character narration also incurred a repair call.

The failure handoff correctly reported current position-2,134,4 and65planks, with146older evidence entries explicitly omitted. Controller resumed directly using the existing layout. This is fresh live evidence for D100's final-facts fix. It subsequently placed one more block but included the doorway head cell in a batch; support failure prevented that placement. Operator steering reaffirmed the two-high east entrance. No autonomous complete-shelter claim is justified.

Paused at client42692/server42362, session5712f01c-ba49-4c60-834c-8f82c7a3f7ba epoch1, and saved shelter-read-retries.jsonl before editing. Modification fallback now reads every missing target in one response for place_block/use_block/break_blocks. Batches read exact cells to bound output; single-target queries retain radius1context. No mutation occurs until the planner reviews the observations and retries. Read errors leave targets uninspected, replacing the previous unconditional claim that inspection succeeded. Two regressions failed before the fix; full build passed. HotSwapped runtime classes without reset/restart and resumed actual controller play. Live batch-fallback verification pending.

Live batch-inspection-live.jsonl verifies a four-target placement got all three missing cells in a single1297-character response: -2,135,6; -3,135,6; -4,135,6. It reported inspectedTargets=4/uninspectedTargets=0, including the already-known cell. Controller retried and placed three blocks, reaching31total/61remainingplanks. The final corner failed with target_not_visible for an explicitly requested up support (the block above), not an unread-target guard; controller is navigating to retry. This proves grouped inspection and subsequent mutation after model review, not full success of that batch or the shelter.

### D106 — Tool budget misclassified as a repairable format error (diagnosed)

Controller crafted three spruce doors, then use_block at0,134,4 failed with support_not_found while explicitly requesting west/up supports. Later apparent placement retries did not execute: calls162/163 received TOOL CALL FORMAT REMINDER saying too many tools for one goal, despite each response containing exactly one valid tool. Source PlannerOrchestrator caps a tool plan at20calls, returns PARSE_ERROR on overflow, and schedules two format-repair generations that cannot change the exhausted count. The existing boundedToolPlanRejectsExcessiveToolRequests test explicitly expects attempt3/PARSE_ERROR. This is a confirmed control-loop inefficiency/failure, separate from terrain placement.

Paused at client46488, sessionb814612d-cb67-4e26-976c-ae2cc20883ee epoch1, saved shelter-door-support.jsonl. Paused authoritative block reads show0,133,4 is minecraft:snow layers1 and0,132,4 is minecraft:snow_block. The doorway will need an appropriate solid threshold or adjusted placement height; arbitrary viewpoint/face changes do not resolve that terrain requirement.

Next implementation: make exhausted tool turns yield normally while retaining completed tool evidence, without accepting the extra unexecuted call or generating false chat. Keep the20-tool bound; let the existing persistent-goal/delegation continuation resume in a fresh turn instead of classifying a runtime limit as malformed model output. Verify both continued ownership/history and the absence of format-repair retries. Game remains paused for this fix; no budget-handling change or doorway success claimed yet.

Implemented the turn checkpoint. Two regressions failed before the change and now pass: bounded turns yield silently on attempt1; real controller/thinker orchestrators execute20tools, discard the21st proposal, continue under the same delegated owner without new user input, preserve the exact accepted prefix/results, execute subsequent work and return evidence. No format-repair requests, phantom execution or degraded mode. Full build passed and compatible HotSwap applied.

The live controller had already entered degraded mode from the old parse failures. Saved tool-budget-before-recovery.jsonl, used the supported @agent reset command to recover (this clears role histories), then supplied a verified construction checkpoint and resumed the same world/client. This recovery reset is separate from normal budget yields, which preserve histories. Live confirmation of a new20-tool boundary is pending.

Live verification in tool-budget-recovery-progress.jsonl: generation15 yielded at19:02:31 with completedTools=20; its proposed13-block roof action (call185) did not run. At19:02:34 the active goal continued automatically, and call186 explicitly rechecked fresh shelter state after the checkpoint. Same controller cache identity/none effort, no format repair, and degraded=false. The controller also placed a solid doorway threshold and a spruce door; paused-world read later confirmed the lower door at0,134,4, facingwest, closed.

### D107 — Controller confused interior headroom with roof gaps

After the budget checkpoint the controller queried air across y135..136 and proposed filling interior y135 cells, despite floor y134. That would leave only one block of headroom. An11-target attempt failed at its first cell (-3,135,3 above the furnace), so that batch made no confirmed progress. Paused at client49565/server49256, sessionc0993209-5a0e-42ee-a862-1a2de923c0f2 epoch1, exported shelter-headroom-mistake.jsonl and inspected the client frame. The doorway is installed; the enclosure/roof and usable clearance remain incomplete/unverified.

Steered the controller to delegate this spatial repair/completion to the thinker. Requirements: inspect real floor/ceiling heights, remove our misplaced planks if they obstruct walking clearance, preserve fixtures/camp log and installed door, finish incrementally, and verify entry/exit plus lighting. Resume after the pause. This is a planning correction and role-routing experiment, not an executor code fix or proof of shelter completion.


### D108 — Separate walking height from supporting block height

The new shelter delegation did start at19:05:25, but calls195 and196 each timed out after120seconds before another building action. Paused client57967/server57658, session7776bc99-40bc-4efb-bd7f-ddcd0f130908, and saved shelter-repair-deliberation.jsonl (780 observations, truncated). The trace repeatedly struggled with the ambiguous phrase floor at134: feet were actually at134, supported by blocks at133. Inventory remained50planks/2doors/2charcoal; health8.53 and food12.

Latency is a separate contributor: call196 accumulated only1528response characters over120seconds, and successful call197 used43.2seconds for209completion tokens (55reasoning tokens), despite38208of38239prompt tokens being cached. These observations do not locate the delay in provider versus client, but do not support attributing every timeout to lengthy reasoning. No provider or gameplay reasoning profile changed.

Clarified feet134, supporting blocks133, free body cells134/135 and roof-block underside136 through live guidance. Asked the actual thinker to perform one concrete reversible correction before further design: remove its three misplaced interior planks atx=-1,y=134,z=3..5. Resumed without reset/restart. Job81e4d453-4b6f-4c8c-a9bd-22cda1ad591d completed50163. In shelter-local-correction.jsonl, call204's follow-up query verifies all six cells atx=-1,y134..135,z3..5 are air. The same medium/thinking cache identity executed the correction. This verifies removal and restored local clearance, not an autonomous finished shelter. Narration length caused two repair requests, and an inspect_action_goal call returned execution_not_found for this direct job before the thinker inspected the blocks correctly.

Added a general full-block floor/feet/roof coordinate convention to the shared system prompt, including inspection for partial blocks. Full Gradle build passed. The running fixed prompt is retained until normal role/config reconstruction; equivalent live guidance supplied the tested correction, so this is not evidence that the newly packaged prompt was loaded. No additional construction tool yet. Roof completion, usable entry/exit and lighting remain outstanding; actual planner play continues.


### D109 — Trial compact horizontal world layers

After the clearance repair, call205 again timed out while reconstructing roof geometry from previous per-block lists. Saved shelter-after-clearance.jsonl, then paused client63840 (b58fb788-99f6-468d-bab0-02fb0a92dd18) and exported shelter-before-layer-view.jsonl (883 observations, truncated). The trace correctly recognized the removed planks but resumed listing roof cells and reconsidering the tree. No new placement failure was demonstrated.

For small inspect_area boxes, replaced repeated per-block prose with horizontal Y layers, explicit ascending X columns/Z rows, and a numbered material/state legend. Omitted cells are ?, distinct from loaded air and an unloaded legend entry. Existing maxResults, selection order and observedPositions authorization are unchanged. Individual or broad queries keep the original representation; no tool schema, extra planner or blueprint executor was added. Fixtures verify orientation across multiple Y layers, snow layers, door properties, unloaded versus omitted cells and unchanged observation limits. A repeated25-cell layer is less than half the prior per-record text length. Full Gradle build passed; live interpretation and construction benefit remain to be verified.


Live D109 follow-up: HotSwapped7query classes without reset/restart. Actual thinker call207 requested the complete25-cell roof atY136; call208 received the new layer representation and correctly decoded its rows into air, snow, leaves and the retained log. Saved shelter-layer-live.jsonl. This establishes live output and interpretation. However call208 still timed out after120seconds, and209 began interpreting the same layer again (shelter-layer-followup.jsonl). The view has not yet improved construction throughput. Asked the thinker to return give_up with its partial evidence and a next small roof section, allowing the controller to execute the settled design; no false shelter success. Handoff and subsequent controller progress pending.


### D110 — Avoid regenerating actions for cosmetic narration

D108 recorded an otherwise valid break_blocks request being regenerated because its narration exceeded80characters. This adds an LLM round trip and permits an unnecessary new gameplay decision. Paused client70470, session2688ae66-abcd-4de3-ad74-4f9dd70e04ec, exported shelter-before-narration-fix.jsonl (896 observations, truncated). The current thinker was streaming return_control for delegationc2d62e02-09fa-431a-bd6e-ad88fbfa3f43; no new roof placement yet.

Use existing local narration contraction immediately when visible reply validation passes and only tool narration is invalid. Tool names/IDs, non-narration arguments and subsequent action validation remain unchanged; normal invalid chat replies retain their existing repair behavior. Deep-copy arguments before contraction so the received tool call remains immutable. A regression first failed because no action executed before the unnecessary repair. Full build now passes, verifying a single execution with identical break targets, bounded narration, an unchanged original request and no format-reminder round trip. Live cosmetic-repair avoidance remains pending; no model effort/cache or history reset.


D110 live state: HotSwapped5orchestrator classes and resumed without reset. The thinker exhausted its handoff attempts, so the existing failure path returned delegationc2d62e02-09fa-431a-bd6e-ad88fbfa3f43 to the controller at19:31:24. Saved shelter-return-recovery.jsonl: failed outcome LLM request timed out,17observed evidence entries, workIdle=true, fresh position-2,134,4 and52planks/2doors/2charcoal. Controller call214 used its original controller/none profile, acknowledged the clearance repair and incomplete roof, and requested a fresh roof read at19:32:47. This verifies failure handoff with preserved evidence; explicit return_control did not complete. A thread snapshot during the delay showed the planner waiting on HTTP and idle HTTP workers, not recorder-lock contention; one sample does not prove the service/network is the sole cause.


### D111 — Controller builds roof, then recovers around an edge

Actual controller progressed from the settled layer: job66fc7433-b309-48aa-af24-ce2de82d93f0 placed8roof blocks and completed68707. The next batch rejected a leaf target; its retry jobc925cbc2-b48b-4fbd-abba-a13551c6faa2 placed4before stalling at(0,136,5), then the controller cancelled it69567. Material mismatch also prevented a mistaken leaf-removal call from breaking an existing plank. The controller corrected the leaf targets, cleared them, and jobafc474bf-30f5-4594-bd25-cff280a195fe placed5more before failing at the same east-edge target after5attempted stand positions. Total17new roof planks since the handoff. Saved shelter-controller-roof.jsonl and shelter-roof-edge-failure.jsonl; both partial retained histories. This demonstrates construction progress with the layer view and controller, not a controlled speed comparison or finished shelter.

Paused client81089, session10c16a19-f52a-433d-b1a4-588ecd65fe51, with screenshot shelter-roof-edge.png in/tmp. The player was down the slope at roughly3,131,8. Navigation event344 confirms a previous exactY=false goal(0,134,7) completed at0.68,131,7.70. The schema already explicitly says false ignoresY: this is planner misuse, not a demonstrated exact-height navigation failure. It also confused the east-wall door facingwest with a west-wall door. Sent the actual location, clarified wall versus facing direction, requested inspection and exact-height recovery, then resumed. An aggro spider triggered defence but resolved as sealed_shelter without health loss (8.53); safety epoch advanced3 and a stale response was rejected. The controller requested new terrain observations. Remaining roof-edge access, enclosure, entry/exit and lighting still require verification.


### D112 — Unsupported footing and confirmed provider saturation

Controller selected air at(1,134,5) as a standing cell even though its inspection showed air below at(1,133,5). Exact navigation job3eefdedf-cbb3-438d-9e32-a12befd6ed41 failed CALCULATION_FAILED at1.3,132,5.7 (event381); this does not show exactY being ignored. It then attempted another4roof blocks: jobd152201c-35f4-43bd-8e32-e7c1d6d9cf8d placed the first before the same east-edge visibility failure. Saved shelter-unsupported-stand.jsonl after pause86004. Suggested using existing find_placement_sites to query air feet cells with solid support and headroom. Correction to operator guidance: requireAirAbove is boolean true, not integer1; the suggested numeric form was erroneous and must not be reused. That trial has not executed.

Actual provider requests248/250/252 returnedHTTP429 with codehosted_shared_model_concurrency_limit and a stated shared limit16. Synthetic failure records249/251/253 repeat those failures. The controller entered degraded mode after these service failures, displaying an inaccurate understanding-error message. This confirms service saturation for this incident, not the cause of every earlier slow request. Paused client87884, session2fa5f1c1-7ef9-4170-bf03-4c26516f5bbb, and saved shelter-footing-recovery-failure.jsonl (1522 observations, truncated). Keep paused while restoring provider availability/recovery; avoid repeated manual resets against saturation. No complete shelter claim. Next: inspect Retry-After handling and preserve the active goal/evidence across transient provider recovery, then retry the correctly typed footing query through the actual planner.


### D113 — Preserve planner sessions across HTTP429 cooldown

The live D112 failure was routed as generic PROVIDER_ERROR, which is not retryable inside PlannerSessionCoordinator; subsequent queued turns consumed the dialogue degradation threshold. HTTP429 now carries Retry-After milliseconds through LlmBackendException and PlannerExecutionResult to the scheduler. Delta seconds and HTTP dates are supported; absent/invalid headers wait30seconds, with a1second minimum. The coordinator waits without adding a dialogue failure or modifying the frozen conversation, then retries. New guidance can replace the pending request but cannot bypass the coordinator's provider deadline. Existing non-rate-limit error behavior remains. No new reasoning profile or tool/API schema.

Local HTTP tests verify429 delay propagation and401 remaining terminal even with a Retry-After header. Deterministic-clock orchestrator tests verify four successive rate limits without a failed result, exact preserved conversations, scheduled resumption and new-guidance cooldown. Full Gradle build passed. This covers gameplay planner sessions; compaction has its separate recovery path and is not newly covered by this scheduler change. Live recovery remains to be verified; the existing degraded flag requires one supported reset before retrying.


D113 live follow-up: enhanced HotSwap reloaded7classes, including retry metadata records, without a client restart. Used one supported @agent reset to clear the pre-existing degraded flag, restored the shelter checkpoint and corrected footing query (requireAirAbove=true), then resumed paused session2fa5f1c1-7ef9-4170-bf03-4c26516f5bbb. The original controller/none identity received successful responses: inspect_inventory at19:52:30 and take_a_look at19:52:55, degraded=false. Saved provider-cooldown-live-resume.jsonl. Provider availability recovered; no new live429 occurred yet, so timed cooldown behavior is supported by automated HTTP/scheduler tests, not a live429-to200 cycle. Shelter work remains active.

### D114 — Navigation cancellation recovered on a planner retry

Paused client90823/server90543 and saved shelter-nav-cancel.jsonl. Controller job881584ba-fee5-4229-928c-3cf1e2f4777e targeted(-2,134,1), exactY=true, but terminated BARITONE_CANCELLED after17ticks at(-0.747,134,1.533). No intervening planner cancel_task appears in the retained calls. It subsequently narrated that it was moving and queried inspect_planner_goal rather than confirming the direct job's terminal state. The footing query executed, but its north-of-shelter target was incorrectly described as inside. Health6.53/food11;34planks and2charcoal remain. No full shelter verification.

Asked the actual controller to retry that same destination once and inspect completion, then resumed without edits/reset/restart. Jobd1c1fe08-19f0-4bc3-9494-2180e6324379 completed in4ticks at(-1.179,134,1.513), supported and at the requested height; event404 records the accepted arrival with BARITONE_CANCELLED. The controller then acknowledged completion and inspected shelter geometry. Saved shelter-nav-retry.jsonl; both exports are truncated retained histories. The original cancellation cause remains unconfirmed: this is a recoverable incident, not evidence requiring a navigation patch. Preserve the distinction between target coordinates and the actual terminal position. Continue construction and watch whether cancellation or waiting on terminal jobs recurs.


### D115 — Sample visible support faces for roof placement

The next controller retry of(0,136,5), job194e68f4-af2f-425c-b23c-6c7199019f18, again failed visibility. It navigated inside successfully, then job1416199a-2c3e-40a4-965c-548aa55990d3 requested west support. Recorder decision states show an approach to roof-level stance(-1,137,3), interrupted by sneak preparation; physical events423/424 record a two-block fall through the east doorway before visibility failure. Paused client92524/server92244, session1b128058-081e-48d0-a32c-7de90eee77bc, and exported shelter-placement-fall.jsonl before edits. The export is truncated; an authoritative80-cell scan captured the roof/wall neighborhood. This does not establish that one cause explains all navigation, stance, or falling behavior.

Found a concrete targeting limitation: only the center of a full block face was considered. A geometry regression using the observed neighboring roof blocks shows the center occluded while a lower point on the intended side remains visible. A second regression shows that hitting the underside previously counted as visibility of a requested side on the same block. Both tests failed before their respective fixes. Placement selection and candidate stance visibility now try at most9points per support face and require the actual raycast face to match. Normal direct block interaction keeps its prior visibility behavior. No new planner tool or construction controller; no support creation or route guarantee. The fixture proves local face geometry, not a complete replay of world/player movement.

Full Gradle build passed; live retry pending. Unsupported interior floor cells and remaining roof/wall gaps may still require planner construction decisions.


D115 live verification: HotSwapped8interaction classes without restarting/resetting. Actual controller used auto support selection for job24dc3800-70ec-47fe-8818-02da976b95b1; event433 verifies(0,136,5) changed from air to spruce_planks against support(0,136,4), facesouth, after4ticks. Decision states show a brief direct approach then sneak/placement, without another roof-stance navigation loop. Saved roof-face-live-success.jsonl (truncated). It then crafted8torches and returned inside. This is live success at the repeatedly failing target; remaining gaps, entrance and lighting are still unverified.

### D116 — Clarify placement support direction

Controller calls298/299 used use_block with facePreference=up for torches intended on the floor at(-2,134,3) and(-3,134,4). Both failed support_not_found because up selects the adjacent support above an air target; it does not mean the upward face of the floor below. Subsequent attempts tried an unavailable snow_block and a plank with the same support error. Paused client95351 and saved shelter-torch-support.jsonl before editing. This is a recoverable planner/schema misunderstanding, separate from the roof targeting fix.

Clarified root and per-target descriptions: placement directions run from target to support, down selects the floor and clicks its top, and auto is preferred. use_block retains its existing alternate meaning for a solid target: the clicked face. No argument/schema structure or executor behavior changed. Existing cached tool definitions are not rebuilt mid-history; equivalent live guidance is needed for this session. Lighting recovery pending.


D116 live verification: after the support-direction guidance, controller jobbd48a8e5-7d02-4629-acb6-bcd8af0fba12 placed a torch at(-2,134,3); job6615d338-845b-4d97-9c9a-198e61de9eb7 placed another at(-2,134,4), both using the floor below. Full build passed for the documentation change. Paused authoritative reads subsequently verify both minecraft:torch cells with light14 and the repaired roof cell(0,136,5) as spruce_planks. A fresh client screenshot visually confirms the two interior torches. This validates live guidance and execution, not loading the new cached schema descriptions.

### D117 — Provider security checkpoint interrupts the lit-shelter run

After the two torch successes, events480/482 returned HTTP403 HTML titled Vercel Security Checkpoint; event483 entered degraded mode. This differs from the earlier429 Retry-After incident. Paused client96634/server96359, sessionf1e65071-3292-49a9-8ec9-dfdaf10746fa epoch1, and saved torch-live-provider-checkpoint.jsonl (truncated), with screenshot in/tmp/shelter-torches-provider-pause.png. Keep this incident separate from gameplay failure; no bypass, automatic403 retry policy, or repeated reset. The actual planner needs normal provider access restored before play can resume. Shelter entry/exit, remaining enclosure gaps and floor holes still need verification; no completed shelter claim. The goal remains active.

### D118 — Paused-world shelter checkpoint while provider remains unavailable

Revalidated the live bridge: same paused sessionf1e65071-3292-49a9-8ec9-dfdaf10746fa, client96634/server96359, actual controller degraded. A normal unauthenticated GET to the configured provider's /v1/models again returned403/Vercel Security Checkpoint. No reset, alternate route or gameplay mutation. This is the second consecutive goal turn encountering this provider condition; the previous turn made verified gameplay/code progress.

Saved a complete125-cell authoritative scan to run/playtest/2026-09-13/shelter-checkpoint-blocks.txt (x=-4..0,y133..137,z2..6). All9roof cells over the3x3interior atY136 are spruce_planks. The remaining outer roof row atz6 is air/snow, but that row lies over the perimeter wall; completing it is lower priority than these actual gaps:

- Missing floor support at(-2,133,5) and(-1,133,5). Both are air; restore usable footing atY134.
- Upper east-wall cells(0,135,5) and(0,135,6) are thin snow, not full wall blocks. Replace with full blocks while preserving the door at(0,134..135,4).
- The east-wall door has both halves present, facingwest, hingedright and open. Closing it and actually traversing the entrance remain required verification.

The furnace occupies(-3,134,3); remaining interior body/head cells have no full-block headroom obstruction. Two torches at(-2,134,3) and(-2,134,4) each read light14; clear interior feet cells read12..14 (the solid furnace cell reads0). The scan verifies local lighting and geometry, not navigation or enemy exclusion. Exterior approach/support and safe entry/exit must still be tested through the actual planner.

Frozen player facts: feet(-1.580,134,4.501), health6.53/20, food11, no armor;29spruceplanks,6torches,6sticks,1dirt,2doors and2rottenflesh. No pickaxe or charcoal in the snapshot inventory. Once normal provider access returns, recover the existing degraded planner once, provide this fresh checkpoint and the placement-direction clarification, then resume the same paused world. Prioritize the four floor/wall repairs and usable entrance over redesign or a decorative roof rim. The actual planner should inspect these cells before modification because external debug reads do not authorize its tools.

### D119 — Authenticated planner still blocked after probe changed

On the third consecutive goal turn with this provider condition, /v1/models changed from403/security-checkpoint to401. That unauthenticated response was insufficient to establish gameplay service recovery. Cleared the existing degraded state once, supplied D118's exact construction checkpoint and resumed the same client. Actual configured qwen3-8-27b controller requests still returned403/Vercel Security Checkpoint (events488/489/490), and event491 re-entered degraded mode. No gameplay action completed in this retry. Do not use a normal models endpoint response alone as proof that authenticated chat requests work.

Paused again: debugSessionId1ebfe7c7-6f84-4d07-ba82-8bb053fa3981, epoch1, client97709/server97434. Saved provider-authenticated-still-blocked.jsonl (1210observations, truncated) and/tmp/provider-blocked-final.png. The client is alive, gameplay idle, and the recorder/world are preserved. Further actual-planner play requires restoration of normal provider access; no bypass or repeated reset. Mark the persistent playtest goal blocked after this three-turn audit. D118 remains the construction recovery checkpoint; use this newer pause session for continuation after provider recovery.

### D120 — Rotated key restores actual planner gameplay

User rotated the provider key and requested another attempt. Used the supported airicraft reload to load credentials without restarting Minecraft. Reload reset agent/planner state and resumed tick-debug automatically; agent tick numbering restarted, with controller/none under a new fixed cache identity. Supplied D118's construction checkpoint and the support-direction clarification. No credential values were printed or copied into this note.

Actual configured qwen3-8-27b requests now return HTTP200. After a normal visible-chat repair, the controller inspected the target neighborhood and submitted a two-target floor placement. Jobaa5f88de-d2c7-431b-b43c-224d2ef526e1 completed at agent3024 (event5), confirming both missing supports(-2,133,5),(-1,133,5) placed as spruce_planks. Saved rotated-key-resume.jsonl and rotated-key-planner-progress.jsonl (both truncated retained histories). This establishes authenticated provider recovery and successful gameplay, beyond the unauthenticated probe that misled the earlier retry. The game is running, degraded=false at the latest status, and the controller is inspecting upper east-wall gaps for the next repair. Door traversal/closure and complete enclosure remain unverified.

### D121 — Last wall corner and actual provider cooldown recovery

Controller job936ea84e-241a-487b-a48d-84b83b6d21c8 placed(0,135,5), replacing thin snow, then failed the final corner(0,135,6): target_not_visible supportPos=0,134,6 safe_stand_position_not_found attemptedStandPositions=0. Saved shelter-last-corner.jsonl. The controller correctly noted the first placement but misread the second error as lacking support. Supplied the distinction: existing support does not imply a visible approach. Prioritize an exterior approach during doorway testing; do not add unnecessary interior supports. No new executor bug established from this corner alone.

Fresh D113 rate-limit proof: calls11..16 returned shared-model concurrency-limit errors; call17 completed successfully. All seven serialized request bodies had identical SHA256prefix445d9d507148. Recorder runtime snapshots expose successive retryReadyAtMs deadlines, and each following request started after its deadline. No manual reset or degradation occurred. Saved shelter-door-understanding.jsonl and door-nav-cancel.jsonl. This is a live cooldown-to-success cycle preserving the request, complementing the earlier deterministic tests.

### D122 — Door question, crossing observation and skeleton interruption

Source inspection confirms use_block on an existing wooden door uses ordinary interactBlock, while inspect_world includes open/facing/hinge/half properties in its block descriptions and layer legends. There is no demonstrated idempotent desired-open-state action. User highlighted the difficulty of interpreting orientation from structural snapshots. Proposed clearer passage/approach observations and a desired-state interaction as possible improvements; neither has been implemented. Raw facing is not the wall's location, and operating a door should not require deriving the shelter layout.

Navigation jobe24073dc-4359-4086-908b-a22abdbe58cf requested(1,134,4), exactY=false, and ended CANCELLED26ticks later at(2.222,133,4.500), outside the east doorway. Paused/exported door-nav-cancel.jsonl. The actual endpoint establishes outward progress despite the cancellation; no exact target arrival or completed door interaction claimed. Asked the actual planner to inspect open state, close/reopen and verify, then return and close behind it.

The test was interrupted before any door toggle: aggro skeleton58e40129-fed9-45ab-9e2a-08757df6c3dc triggered DEFEND at10542, while the player had6.53health and no armor/shield. Reflex chased and attacked with spruce_planks. Arrow hits reduced health to3.03,0.03, then death at10803 near(5.256,126,23.500); automatic respawn at10804. Physical events show reflex-owned movement continued after respawn until threats_clear at10911. This continuation needs investigation before attributing it to stale ownership versus fresh danger. The planner subsequently requested health/location images; no reliable close/open sequence demonstrated.

Paused at client109978 (debugSessionIdef3c23ad-27d3-455c-b490-d8c053d467e8, epoch1), saved door-skeleton-interruption.jsonl and/tmp/shelter-door-skeleton.png. Respawned feet(7.493,128,12.996). Paused authoritative door read still shows open=true, facingwest, hingedright. Keep the game paused for incident diagnosis. Floor repairs and(0,135,5) remain prior confirmed construction; corner(0,135,6), safe entrance and door operations still require practical verification. Do not carry the pre-death inventory or location forward without fresh checks.


### D123 — Explain door passage geometry independently of open state

User requested a local heuristic because a door placed across the wall can leave the doorway clear when nominally closed, then obstruct it when nominally open. Added bounded door summaries to inspect_world inspect_area/find_blocks results. For each observed door (deduplicated halves, at most8), read actual collision shapes of both halves and their hypothetical toggled states, then sweep a standing player's body along east-west and north-south crossings. Surrounding clearance determines a preferred passage axis only when unambiguous. Report current/toggled panel blockage, separate neighboring obstruction, approach coordinates, floor support, hand-toggle availability and powered state. Incomplete/inconsistent doors or unloaded neighbors yield unknown. No world mutation, new action tool, locomotion controller, or change to observed-position authorization.

The check is local level clearance, not a route guarantee. Partial floor support, drops, stairs, other entities and powered-door timing still need navigation/context decisions. Seven focused geometry tests cover ordinary and reversed panels, both axes, ambiguous surroundings, neighboring obstruction, upper-half collision and a fence protruding from below. Full Gradle build passed (/tmp/door-passage-build.log). An experimental vanilla-registry JUnit test could not initialize Minecraft due to an existing SimpleRegistry/RegistryEntry access error; removed the experiment rather than changing the test runtime. Real shape-adapter verification is performed in the running client below.

HotSwapped the query implementation without restarting/resetting the client. D122's game remained paused throughout edits; its incident export was already saved. Supplied a read-only exact-door query to the actual configured controller and resumed for validation. The pre-death inventory is not current.

D123 live verification: actual qwen3-8-27b call30 completed HTTP200 with the requested two-cell inspect_world query. Call31's request contains the new output from the real Minecraft shape adapter: door(0,134,4), stateOpen=true, handToggleAvailable=true, powered=false; east_west surroundingsClear=true, doorBlocksNow=false, doorBlocksAfterToggle=true; westFeet(-1,134,4) supported and eastFeet(1,134,4) not_full_support. North-south is blocked by both surroundings and the current panel. The controller's streamed answer correctly identified the clear east-west crossing, the effect of toggling, and missing exterior support. It loosely described north-south as surrounding obstruction rather than noting both obstructions; the tool provides both facts. No new image was requested for this check (older image content remains in history). Saved door-passage-live-result.jsonl, truncated retained history. This verifies real shape observation and planner uptake, not physical toggling, safe traversal or a reversed-placement world fixture.

D122 diagnosis clarification: recorder shows reset ran on death, then a new DEFEND epoch2 began at10807 against the same skeleton24.65blocks away with no new damage and no line of sight. Source proximity scanning compares mob target UUID to player UUID. A dead old player object retained as a target could explain this, but that hypothesis is unconfirmed; no reflex patch made. Resume ordinary play with fresh inventory/location and recovery guidance after the door observation check.


### D124 — Recovery succeeds, then stale evidence derails resource planning

Following D123, actual-controller navigation jobc6c08458-acaf-41d8-a444-1727b72244a0 reached the death area in50ticks, ending near(5.834,126,23.204). Events56..60 confirm1bone,2spruce doors,1dirt,6sticks and16snowballs recovered. Job66507ca9-5486-4c0d-8cc4-f3b335f57cc4 then returned inside at(-1.292,134,4.500); both jobs completed, despite BARITONE_CANCELLED being the normal underlying termination cause. Fresh inspect_inventory confirmed those five item types, health20/food20, no planks, charcoal, pickaxe, shield or armor. No proof that every death drop was recovered. A new image was used to search for remaining drops; inspect_nearby_entities already accepts minecraft:item, but currently lacks exact stack IDs/counts. This is an observation improvement candidate, not implemented here.

Calls45/47/49 repeatedly inspected shelter geometry; call48 claimed inventory empty and searched for54planks supposedly at camp. Its request repeatedly carried the persistent objective's old "Current verified supplies: 54 spruce planks..." checkpoint. It also appended an obsolete World evidence snapshot at(-5,135,-1) with empty inventory after fresher inventory/position observations at the death site and inside shelter. Authoritative pause at client117894, sessiondaa0f5c9-7ef5-49e2-9e5f-725af694f131 epoch1, confirms feet(-1.470,134,4.498) and the five recovered item types. Saved planner-stale-goal-inventory.jsonl before edits (461observations, truncated), plus earlier post-door-recovery-1/2/3.jsonl.

Source cause for stale automatic evidence: EmbodiedAgentRuntime sampled current WorldEvidence and passed a temporary refreshed mission snapshot only to dialogueRuntime.poll. The retained missionExecutionSnapshot was refreshed only for a projected semantic task. Idle/primitive navigation paths could therefore re-inject an old inventory/position in goal-continuation triggers. Retain the fresh evidence every tick before planner polling, preserving job history; no extra world scan or context reset. Regression idlePlannerEvidenceRefreshesWithoutAProjectedSemanticTask exercises the runtime tick with stale supplies and no projected semantic task: failed before, passed after, and verifies terminal result history is retained. Separately clarified planner-goal guidance and dynamic context labeling: objectives hold desired outcomes/durable constraints; newer observations override historical supply/health/progress claims. The actual planner should clean the existing objective using its own change tool.

D124 validation: focused regression and full build passed (/tmp/planner-evidence-green.log, /tmp/planner-evidence-build.log); HotSwap reloaded6classes, no restart or history reset. The first provider attempt returned403/security-checkpoint, but actual calls59/60 subsequently completed200 without credential change or manual reset. Briefly paused at client119967 (session8cbf8744-0056-4a81-b5e9-c81c3b3e3eb7) and exported planner-evidence-provider-interruption.jsonl, then resumed after confirming successful requests. This was transient, not a persistent provider blocker.

Live request observations31187/31238 carry the corrected current position(-2,134,4), all five recovered item counts and the new stored-intent label; no new stale(-5,135,-1)/empty-inventory context is appended. Call59 invoked change_planner_goal, removing obsolete supplies and correcting floor/feet/roof heights while preserving the shelter objective. Call60 then used inspect_nearby_entities filtered to minecraft:item instead of requesting another drop-search image. This establishes request construction and planner uptake; longer survival/resource progress remains pending.

Separate pre-patch job26dc9493-2adf-4373-bca0-e3c6aada9f45 failed acquisition_scope_left after1tick. Its radius12/verticalRadius10 centered(-3,134,4) contains the player. surfaceOnly=true also applies to the starting player stance, so standing below the shelter roof can fail the surface-height gate. This is distinct from a spatial radius violation; the current failure text does not distinguish them. No acquisition behavior change made in D124; an outdoor starting position is a planner recovery path to verify.


### D125 — Pillager approach stalls while holding planner control

After D124, the planner's structured item query found no recoverable drops. It chose a remembered forest destination(-42,149,-111), job26dc95d1-1f66-42c5-8d24-8cdee23a30d7, rather than retrying surface-only collection inside the shelter. During travel, pillager51130d12-6b86-41fc-a58c-14e66fafc6ae became an aggro target at22786, distance18.876, lineOfSight=true. DEFEND epoch3 interrupted navigation, hold40136883-c4de-4e86-a907-4a74e990fad5. Two3-block descents followed without damage. Player then stayed near(-34.511,124,-38.573), pillager near(-21.740,123,-41.500), distance13.14068. Recorder through24476 shows no damage, no melee engagement and unchanged separation for over80seconds while the planner remains excluded by the active reflex.

Paused initially at client122822 (sessiona41da34a-4be4-46a2-bc25-6d9293d62fc2) and exported pillager-stalled-combat.jsonl; earlier sample pillager-approach-observation.jsonl. Both retained exports are truncated. Screenshot/tmp/pillager-stalled-combat.png shows opposing ledges at a steep cave opening. Authoritative entity read confirms pillager alive health17/24 with a crossbow and ominous banner; player health20/20 and no shield. Reflex mob-route evidence is PARTIAL/pathLength13 with line of sight. No physical arrival, hit or resolved encounter claimed.

Attached Arthas to existing JVM11034 for bounded read-only watches. The scripts/arthas HTTP default returned before asynchronous watch completion, and rich Optional inspection failed module access; direct HTTP exec with execTimeout25000 and a scalar string expression produced usable results (/tmp/pillager-watch-http.json, /tmp/pillager-state-http.json). Live probe: custom GoalNear(-22,123,-42,radius2), customActive=true, state=EXECUTING, pathing=false, calculating=false, executor=null, paused=false at the sampled instant. This alone does not prove a dead process: run/logs/latest.log records repeated A* searches considering millions of movements between probes. Do not patch processActive solely from that instantaneous sample. No world/player mutations or navigation recovery were performed through Arthas.

Latest pause after bounded resume/probe cycles: sessionaabac3ee-a79b-49f3-8f55-6b58af5b7c4c, epoch1, client123779, saved/tmp/pillager-state-probe-pause.txt. Keep paused for the next diagnosis/fix. A likely useful improvement is a bounded report to System2 when combat approach makes no progress, allowing a tactical choice while preserving the user's fight preference. That policy is not implemented yet; verify the existing safety-hold/handoff contract before changing it. Repeated A* work and missing planner handoff are established observations; a specific pathfinding root cause is still unconfirmed.


### D126 — Hand a stationary combat approach back to the planner

D125 established the live failure: player/pillager stayed13.14blocks apart while repeated pursuit held System2 out indefinitely. Added a small CombatStalemate policy around the existing safety hold. During distant combat,20seconds without either side moving1block triggers AWAITING_PLANNER with reason=combat_approach_stalled. Small coordinate jitter does not reset the interval. Movement, changed threats, close range, observed mob damage, active item use or an incoming persistent projectile prevent the handoff or reactivate defense. The event explicitly says combat remains unresolved, includes current position, remaining threats, no-progress ticks and the existing hold ID, and asks for a tactical next step. This does not infer safety or introduce generic fleeing.

The unchanged distant encounter stays deferred across resume/replacement so the asynchronous aggro query cannot immediately steal control back. Retain observed threat identities for that deferral; a lifecycle reset clears them and the new state. Record the observation/deferral state in flight-recorder decision evidence. No new planner action, background process, timer thread or navigation controller. The policy measures actual positions; a Baritone active-process flag alone is insufficient evidence of progress. A close-range stuck encounter is intentionally outside this distant-approach policy.

Three policy tests use the recorded coordinates and cover the20second boundary, deferral persistence, motion/jitter, changed identities/positions and danger. Runtime tests verify resuming a safety hold retains deferred threat identity until reset, and that the actual trigger includes unresolved-combat evidence and hold ID. Focused tests and full Gradle build passed (/tmp/combat-stalemate-tests.log, /tmp/combat-stalemate-build.log). HotSwapped14loaded classes without restart/reset and resumed the same encounter; live handoff/recovery pending.

D126 live transition: event90 at agent27024 reports combat_approach_stalled with noProgressTicks=400, the same hold40136883-c4de-4e86-a907-4a74e990fad5 and nextState=AWAITING_PLANNER. Player moved slightly during initial resumed pathing, then stabilized at(-33.765,124,-38.500); pillager remained12.434blocks away. Recorder decision state confirms DEFERRED persists and the actual controller request contains the tactical hold message without a human chat trigger. Saved combat-stalemate-live-handoff.jsonl (1973observations, truncated). Initial handoff calls65/66 returned429 and entered existing provider cooldown; do not confuse this with a failed reflex handoff.

The same request exposed D124's remaining stale-evidence path: pauseNormalWorkForReflex replaced fresh evidence with ActiveJobRuntime's last pre-interruption sample(-36,133,-34). The explicit handoff position was correct, but automatic context contradicted it. Paused session05985bc4-a8f0-4a4f-ae4f-236009f338a7 at client126496 before editing. A runtime regression invoking the real pause path failed before the fix; now preserve current evidence while projecting paused-job metadata, using MissionExecutionSnapshot.withEvidence. Full build passed again. HotSwapped this follow-up without resetting the existing deferral, then resumed. Fresh ambient context in a subsequent new planner request and tactical gameplay recovery remain to verify; an already frozen429 retry intentionally retains its original request.


### D127 — Continue an active planner goal while a safety hold awaits action

After D126's handoff, controller call76 successfully inspected nearby pillagers/skeletons. Calls77..79 hit429; call80 completed200 with plaintext: "Two pillagers are nearby, so I'll resume the safety hold and deal with the closer one before continuing to the forest." No resume/replace/cancel tool followed. Event91 applied that reply at29497; through30567 the job remained BLOCKED by reflex, stateAWAITING_PLANNER, health20. Saved combat-stalemate-planner-recovery.jsonl, then paused at client129812, session4209dcc6-e84d-4cd7-b08d-77198c1704e0, and saved combat-hold-plaintext-stall.jsonl before edits.

Two gates prevented automatic continuation: the client tick returned from the safety-hold branch before running goal continuation, and DialogueRuntime required idle work even when a hold explicitly awaited a decision. Call continuation from AWAITING_PLANNER and let an inactive reflex with a hold ID request another planner turn. Preserve busy/unfinished status for delegation, including paused action graphs; do not resume actions implicitly. The reminder names the hold and requires an actual resume_task, replacement or cancellation. Active reflex, provider cooldown/in-flight work, disabled planner and external-driver gates remain intact.

Regression safetyHoldContinuesAfterPlainReplyWithoutCallingPausedWorkIdle failed before the change. It now verifies a second model request without new chat, inclusion of the hold ID, paused work remaining unfinished for delegation, and no continuation during renewed active defense. Full build passed (/tmp/safety-continuation-build.log); hot-loaded and resumed without human guidance for live verification.

D127 live verification: without another chat message, controller call81 invoked cancel_task, releasing the held forest navigation (event92). Call82 rechecked entities, and after a stale-hold rejection at its continuation boundary (event93), automatic goal continuation started call83 to assess the steep terrain. The controller is no longer stranded after plaintext; the unchanged pillager did not immediately reacquire reflex control. The new request contains correct current position(-34,124,-39) and recovered inventory, verifying D126's reflex-evidence follow-up. Saved safety-hold-autonomous-continuation.jsonl (525observations, truncated). This verifies autonomous continuation, a real task cancellation and subsequent planning, not a completed combat encounter. Added the paused-action-graph condition to the work-idle calculation so delegation cannot treat unfinished graph work as complete; full build passed and hot-loaded that final refinement.

### D128 — Repeated locomotion is not combat approach progress

After D127, actual planner attack jobs d9af6da8 and ba9a6410 each failed target_unreachable after43ticks; ecca8aee failed target_not_nearby after7ticks. It then chose home navigation. Guidance discouraged hunting unrelated observed mobs as a substitute for handling the known attacker. Later events117 and121..126 established the second pillager's aggro and real close-quarter attacks against the original pillager; the encounter was not permanently motionless. Subsequent holds were explicitly resumed by the planner, including events145..147 and153..155. Combat success was not established.

The next blocker reproduced in epoch8: home job aeaf1c25-7200-4d08-851b-2e1901f86547 stayed paused while defense repeated the same climb/fall near(-5,131..135,-7). Events165..174 include repeated three-block falls at the same ledge. Pillagers remained35..42blocks away with no line of sight at the status sample; health16.166666 and food13 at pause. Paused session664a48e2-69ed-4e8b-a99a-aa46dfe3170d at client139295 and exported reflex-ledge-cycle.jsonl (4160observations, truncated). Screenshot /tmp/reflex-ledge-cycle.png shows the snow/dirt ledge.

Recorder interval39500..41586 contained88 approach anchors without any DEFERRED state. CombatStalemate treated each one-block player displacement as progress, so climbing and falling indefinitely restarted its400tick timer. A reduced test using recorded ledge coordinates failed before the fix (:test --tests '*CombatStalemateTest', /tmp/combat-cycle-red.log). Track the best nearest-threat distance instead; only a one-block improvement restarts the approach timer. Keep existing immediate-danger and changed-identity behavior, plus moved-threat reactivation after deferral. This is a bounded local approach heuristic: a long productive detour can still require a planner decision. Updated the handoff message from 'stayed stationary' to 'no closer approach'. Full build passed (/tmp/combat-cycle-build.log). HotSwapped CombatStalemate and EmbodiedAgentRuntime without restarting or resetting the client, then resumed for live verification.

First live retry reached AWAITING_PLANNER at42136 (event183), exactly400ticks after its best approach anchor41736, despite repeated movement. However, the primary pillager jumped fromY117 to118.14 behind terrain while still35blocks away; moved-threat reactivation seized control again42ticks later (epoch9, event184). Paused session4133f632-6172-4195-8337-f8bad19c63ed, client140481, and saved combat-cycle-reactivation.jsonl (5152observations, truncated). A second regression reproduced the inappropriate reactivation (/tmp/combat-jump-red.log). Removed distant movement alone as a deferral reset: existing close-range danger, item-use attack preparation, incoming projectiles, damage or new threat identities still permit defense. No new recovery controller. Full build passed again (/tmp/combat-cycle-final-build.log); planner message now names these actual reactivation conditions.

D128 final live evidence: event187 handed back epoch9 at42625, and the actual planner resumed it at42725 (events188..190). Home navigation completed at42864 inside the shelter near(-1.402,134,4.501). A later surfaceOnly log-gather attempt again failed acquisition_scope_left when started indoors; the controller chose an outside navigation next. This confirms actual recovery from the combat loop, not combat victory. User then requested simpler distance/visibility eligibility and a System 2 reflex-policy override.

### D129 — Let System 2 configure automatic reflex eligibility

Added configure_reflex: {} reads the current policy; supplying combatEnabled, drowningEnabled, maxThreatDistance and requireLineOfSight replaces it atomically. Defaults enable combat/drowning, require line of sight, and limit threats to16blocks; melee retains its6block cap. Distance is an integer1..32, matching the existing proactive query bound. Both proactive detection and resolution of previously observed attackers use the policy. Damage history alone no longer starts combat against an ineligible target; an active combat reflex with no eligible threats resolves without chasing. Disabling a reflex stops its actuation on the next tick while preserving the explicit paused-work handoff. The tool is allowed during active reflex/task/graph work, without preempting that work. Overrides survive episode/death resets and last until the agent runtime is recreated; they are not written into world memory or config files. Policy is included in decision recording, change events and survival handoffs. Prompt guidance explains intentional overrides and restoring settings after a tactic.

Validation: tests cover recorded distant/hidden pillagers, melee bounds, override/disable behavior, policy retention across reset, strict atomic tool arguments, permission during an active reflex/graph, and read/write through the actual runtime action executor. Full build passed (/tmp/reflex-policy-final-build.log). Initial HotSwap changed the gameplay classes successfully, but refreshing the static tool catalog with an unscoped Arthas expression loaded duplicate app-loader classes. Subsequent planner requests failed IllegalAccessError involving the private BuiltInTool nested record across app/knot loaders; this was a debugging-induced deployment fault, not a provider failure. Saved reflex-policy-loader-incident.jsonl (5554observations, truncated), removed all temporary refresh code, and cleanly stopped the client using scheduleStop. Logs confirm player and all dimension chunks saved before exit. Restarted normal runClient in the same world, with the actual planner enabled, and supplied the saved task context. Fresh runtime is healthy (not degraded). Avoid unscoped OGNL references to Fabric classes in future probes; use the actual Knot loader when needed.

D129 live verification after restart: actual qwen3-8-27b calls11..13 completed HTTP200 and performed configure_reflex(false,true,12,true), read-back with {}, then configure_reflex(true,true,12,true). Tool results confirm each policy; events5/6 record the two changes at ticks1134/1243. The latest decision state records combatEnabled=true,drowningEnabled=true,maxThreatDistance=12,requireLineOfSight=true, reflexIDLE, health16.166666. Controller gameplay resumed with navigation and resource-gathering attempts. Saved reflex-policy-live-validation.jsonl (1137observations, truncated=false). This proves the actual planner can configure/read the live runtime and continue gameplay. Disabling mid-combat/drowning was not exercised in-world; active-reflex tool acceptance and policy behavior have automated coverage. No claim of successful new wood/food collection from this check. The known indoor acquisition-scope failure remains tracked separately.

### D130 — System 2 refactor: evidence before every decision

2026-09-14. User approved the complete evidence/work/ownership refactor; ADR0002 records its staged acceptance checklist. Kept the client paused at client4789/server4489, sessionc2420f69-6629-400b-8168-fe3421bb0700 epoch1. Existing untruncated reflex-policy-live-validation.jsonl preserves the incident; no live reload or gameplay change yet.

First stage introduces an immutable game-thread PlannerDecisionContext, appended before initial and tool-follow-up model requests. Each role retains its own evidence cursor and conversation; transport retries reuse the submitted conversation. TOOL_WAIT now preserves the actual conversation instead of deleting it and rebuilding from the opening snapshot. Task outcomes remain in the shared event buffer; PendingInternalTaskUpdate's copied state/message queue is replaced with identity-only wake references. A consumed outcome need not schedule another turn, and superseding a wake does not delete the event. Raw warnings also remain evidence even if planner scheduling is disabled. The initial work projection uses existing job/graph snapshots; unified work handles are the next stage.

The new consecutive-tool regression injects a terminal wood failure during model execution, then confirms delivery in the next request and one retained occurrence through another follow-up. Focused orchestrator/dialogue/session tests pass; full Gradle build passed (/tmp/system2-evidence-build.log). Old runtime tests asserting delivery through dialogue text now assert identified event evidence and its inclusion in actual decision context. Additional overflow, independent-reader and dispatch/context/collection-clock tests pass (/tmp/system2-evidence-metadata-tests.log). Recorder request entries now carry actual dispatch clocks and compact decision metadata. Broader world-change/compaction/authority acceptance and actual configured-planner live validation remain outstanding; this is the first stage, not completion of the approved plan.

### D131 — Unified work projection (2026-09-14)

Added bounded work identities over existing jobs, graphs and furnace tracking, common embedded action receipts, and inspect/list/cancel/resume/wait controls. Graph primitives retain parent identity. Terminal results survive replacement. Furnace collection is now distinguished from cancelling tracking; readiness remains explicitly estimated until observed. Waiting suppresses goal polling until work changes; user guidance clears a wait. Existing wrapper adapters remain available.

Validation: focused runtime/smelting tests and full Gradle build passed (`/tmp/system2-work-build.log`). Regression tests cover direct navigation receipt identity through failure, rejected actions, bounded retained outcomes and child identity. Live runtime remains paused on the old classes; unified work/reflex and configured-planner behavior still require live validation. Goal ownership, role authorization and prompt cleanup follow in the next stages.

### D132 — Objective and delegation ownership (2026-09-14)

Added persisted BLOCKED objectives with explicit evidence, required change and named reconsideration events; block/resume operations and bounded named planning decisions. Version 1 goal files migrate to version 2 with a backup, preserving existing identity/status/outcome. Replacement retains a bounded history of prior objectives. Constraints and completion criteria have separate fields. Only the controller advertises and may execute overall-objective mutations; the thinker has assignment-scoped decisions and return_control. Delegation uses stored intent rather than copying private controller conversation, includes fresh decision context, and references shared event identities. Request rejection also checks world and decision ownership.

Validation: migration, blocked persistence, decision replacement and fabricated thinker mutation tests passed, along with focused dialogue/orchestrator/delegation tests and full Gradle build (`/tmp/system2-goals-build.log`). No live migration or planner validation yet; client remains paused on old classes. Reflex supervision and end-to-end blocked-event scheduling are the next integration boundary.

### D133 — Reflex supervision (2026-09-14)

Reflex ownership no longer prevents a model request. Meaningful reflex transitions enqueue evidence-referencing wakes, while existing executor admission still rejects competing physical actions. Idle goal continuation remains suppressed during an active reflex; a plaintext supervisory reply does not create another model request. Obsolete responses wait for an identified new wake instead of replaying the obsolete trigger. Embedded roles now allow one outstanding model attempt. Blocked objectives suppress ordinary automatic triggers and observe their explicit reconsideration event types.

Validation: focused dialogue/runtime/admission tests and full Gradle build passed (`/tmp/system2-reflex-build.log`). Added active-reflex supervision/no-plaintext-loop regression; existing stale-epoch and policy admission tests pass. Physical policy release/resumption still awaits the planned live interruption reproduction.
