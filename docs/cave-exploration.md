# Cave exploration

`survey_cave` provides a compact observation for choosing the next short move. It casts 495 sparse first-hit rays, with a default distance of 12 blocks (allowed: 4–24). Results include up to eight visible standing candidates spread by direction, sixteen exposed ore blocks, twelve hazards, and local light levels. Returned positions count as fresh observations for exact block actions.

This is an alternative to repeated screenshots and hidden-block queries. It observes geometry around the player, including behind the current camera direction; it does not enforce darkness-limited eyesight. Sparse rays can miss small surfaces. Empty results do not prove absence, and `routeValidated: false` means a visible standing candidate may still be unreachable.

The planner prepares a shield, food, picks, torches and inventory space, remembers an entrance/return point, and disables path excavation and placement while traversing passages. It chooses short waypoints, surveys again, records useful junctions and tried branches, and restores its previous path settings afterward. Exact exposed ore coordinates can be approached and broken, then newly exposed faces surveyed. Home stock is read from dated logbook observations and confirmed by opening storage.

Use `inspect_pathfind` to read current settings before changing them and after interruptions. With no arguments it returns seven movement/terrain settings; `{"names":["allowBreak","allowPlace"]}` requests a smaller set. Values and defaults use Baritone's serialized format. Up to 32 names may be queried, with no mutation. Cached tool descriptions are not live settings snapshots.

Live acceptance so far covers surface observations, visible water, an existing underground workstation passage, and a successful return with digging/placement disabled. It does not yet establish natural-cave discovery, ore collection using only this interface, or reliable exploration of a large cave network.

Follow-up: a natural lush cave was reached and a longer exact return completed, including a mixed skeleton/zombie encounter. A later inventory/settings audit found that combat had reset path restrictions and the return included excavation; it is not proof of an entirely natural route. D050 fixes that reset and requires fresh verification. Exposed coal/copper were observed through this interface; collection remains unverified. An unrestricted horizontal goal chose a deep drop into water, so use exact observed stances around openings and establish the return route before further descent. See D048–D049 in the autonomous playtest log.

Current acceptance (D052–D056): collected two exposed coal and seven raw copper, returned and deposited them in the home chest. Automatic lighting now supports active navigation as well as mining. Configure it explicitly, for example `{"enabled":true,"mode":"darkness","maxLightLevel":7,"requireUnderground":true,"minSpacingBlocks":6}`. It keeps the shield/offhand item, temporarily borrows a carried main-hand torch when needed, restores the held slot and inventory layout, and skips combat, item use and active block breaking. Placement checks reach and visible support; block-edit pathfinding restrictions remain separate. Seven placements along the existing descending route were live verified with the shield equipped and sword restored. This improves an already established passage; it does not establish reliable exploration of arbitrary cave networks.

D062 also verified the hotbar-only torch selection path and collected one further exposed coal. Creeper blasts can invalidate a standing candidate between survey and navigation: inspect current support or resurvey after combat before retrying. Several isolated fuse guards absorbed shield durability damage without health loss, establishing live blast protection; frequent combat and blast-altered footing still make exploration inefficient.

D063 return lesson: descending passages are not necessarily climbable under the same no-placement policy. One long ascent request detoured from Y64 to Y−6. Retracing grounded recorder positions and permitting footholds on short upward legs recovered the surface without excavation. If filler is outside the hotbar, Baritone also needs temporary `allowInventory=true`; `allowPlace=true` alone was insufficient. Restore changed settings after the local climb. Use the updated world bookmark's exact waypoints, and check route progress rather than equating movement with progress toward the destination.

D067 gathered eleven coal using only surveyed ore targets and newly exposed faces. Pickup still needed manual pit approaches. The next useful acquisition extension is an observed/visible target policy on the existing System 1 executor, retaining the current buried-target policy for explicit excavation tasks. A remembered exact feet waypoint became solid dirt; fresh footing inspection and a nearby step corrected it. World route notes now use `(215,67,400)` then `(216,68,401)` for that ascent. Revalidate remembered coordinates after terrain edits.
