# Codex Driver Survival Playthrough

Latest follow-up: the [2026-09-11 autonomous capability run](autonomous-playtest-log.md#verified-checkpoint-after-the-autonomous-run) completed renewable food, maintained farming, a usable shelter/bed, full iron armor, and more than two Easy day/night cycles. Its decision log records fixes, reversions, live evidence and remaining limitations; historical observations below retain their original context.

Date: 2026-09-01  
Mode: external Codex driver (`scripts/codex-driver`)  
Objective: progress from a fresh survival world to the Ender Dragon, using only the exposed Airicraft agent tools for gameplay.

## Rules of this log

- Record non-fatal bugs with the evidence, workaround, and gameplay impact.
- Record missing but non-mission-critical harness capabilities separately.
- Do not treat a queued tool result as completed work; wait for its task state and events.
- A true blocker is a failure that prevents progress with every available exposed tool and has no safe in-game workaround.

## Playthrough log

### 2026-09-11: autonomous capability loop follow-up

Current evidence and decisions live in [the autonomous playtest log](autonomous-playtest-log.md). Live checks now cover hunting/cooking, repeated resource supply, protected shelter construction and return, mature crop harvest/collection/replant, bread consumption, a bed/spawn point, and full iron armor. The player survived 49,285 Easy simulation ticks across the fix loop and returned home at full health/hunger. Some recorder exports have gaps; harder difficulties and a clean long run without interventions remain unverified.

Minor or bypassable findings: short dropped-item approaches sometimes emit `CANCELED` before acquisition reselects and collects successfully. Entrance-side crops can be displaced by ordinary foot traffic, so keep crop rows away from the door and avoid jumping on farmland. The current `mine_blocks` description was stale about requiring exposed sources; it now advertises the live-tested buried-source excavation capability.

### 2026-09-10: buried-ore excavation

**Reproduced and fixed live:** at `(204,93,458)` in `another world`, a one-coal request with `radius=6, verticalRadius=6` failed at tick `10787` with `no_reachable_resource_in_scope itemCount=0 rejectedTargets=0 lastRejection=none`. Inspection found coal at `(206,98,455)`, `(206,98,456)` and `(207,98,455)` fully enclosed by stone/dirt/other ore; the entire surrounding `205,97,454..208,99,457` box was solid. The selector required an already-open standing position and an interaction ray reaching the ore, so it never attempted to excavate. This reproduces a specific buried-source failure independently of the earlier broad coal path failures.

System 1 now selects excavatable feet/head spaces beside a known source and lets exact A* navigation dig the approach. It can try another side when one work position fails. Surface/area constraints and exact source harvesting remain in place. After HotSwap, the identical request from the unchanged position mined the covering stone/dirt, harvested `(206,98,456)`, picked up coal at `11051` and completed at `11052`. No terrain mutation, manual digging, teleport or extra targeting call was used to prepare the success. The same inventory assertion failed with zero coal before the change and passed with one afterward. Evidence: `/tmp/buried-coal-envelope.txt`, `/tmp/buried-coal-before-status.txt`, `/tmp/buried-coal-before.jsonl`, `/tmp/buried-coal-after.jsonl`, `/tmp/buried-coal-after-events.txt`, `/tmp/assert-buried-coal.py`.

A subsequent eight-coal request exposed a second defect: the approach was rejected at 240 ticks despite continuing to dig and move. The recorder shows movement from Z≈451.3 to Z≈450.3 immediately before `approach_stalled`, and the player gained 18 cobblestone during that route. Productive excavation now uses the overall 2,400-active-tick budget instead of a separate 240-tick cutoff; the 80-tick no-progress check remains. The new continuation regression failed before this change. Evidence: `/tmp/buried-coal-eight-budget.jsonl`, `/tmp/buried-approach-red.log`.

Resumed that same eight-coal job after HotSwap; it completed at tick `12221` with eight additional coal (nine total in inventory), including a second vein around `(209..210,92..93,445..447)`. No new target or replacement collection request was submitted during the continuation. Final position `(209,92,447)`, health 20, eight torches, iron pickaxe and three raw iron retained. Evidence: `/tmp/buried-coal-eight-{1,2,3}-status.txt`, `/tmp/buried-coal-eight-events.txt`, `/tmp/buried-coal-eight-success.jsonl`. Full build passed: 1,049 root tests with zero failures/errors and two skipped; 92 wrapper tests passed. Geometry, alternative-side retry and productive long-approach regressions are included. Recorder truncation remains the previously documented limitation.

Returning to the exact saved workshop coordinate failed with `CALC_FAILED` after the first coal pickup. A controlled retry with `configure_pathfind({allowDownward:true})` also failed; restored `allowDownward=false`. The downward-setting hypothesis is not supported by this experiment, and no profile default was changed. Furnace/nearby standing positions remain available for later route diagnosis. Evidence: `/tmp/buried-return-failed.jsonl`, `/tmp/buried-return-downward-status.txt`, `/tmp/workshop-current-blocks.txt`.

### 2026-09-10: user-selected peaceful progression world

Continued in the world the user selected in the same live driver client, running `7f205ccc`; no restart or difficulty mutation. The prior world unloaded at agent tick `1959`, and this world loaded at `2307`. Starting inventory at `(207,89,460)` already contained stone/wooden pickaxes, a furnace, three raw iron, five dark-oak planks and four sticks. This is progression from that supplied state, not a fresh-world bootstrap test.

- Smelted all three raw iron using six leaf litter, collected three ingots, crafted a table, then crafted an iron pickaxe. Inventory and `crafting.item_crafted` at tick `6998`, followed by task completion at `7000`, verify the result. The recipe wrapper's 40-tick `pending_timeout` was not a failure: workbench navigation continued and crafting finished.
- Automatic workbench setup moved from `(207,89,460)` to `(206,102,449)`, consuming the carried table and some terrain filler. Paused and exported the unexpected route; crafting had already succeeded. The retained interval does not establish why the whole route was chosen, so this is an observation, not a confirmed workstation-selection defect. Evidence: `/tmp/peaceful-craft-nav.jsonl`, `/tmp/peaceful-craft-nav-pause.txt`, `/tmp/peaceful-craft-nav.png`.
- One `collect_resource(WOOD_LOGS, 6)` request with `radius=16, surfaceOnly=true` completed through dense dark-oak canopy. It rejected some breaks and pickup approaches, then recovered without new targeting instructions: inventory gains reached six at tick `8418`. Intermediate drop failures did not wedge the job. Job `job-a419e27e-9abf-4b2a-b63c-1c944942749e`; evidence `/tmp/peaceful-wood-{1,2,3}-status.txt`, `/tmp/peaceful-wood-first.jsonl`, `/tmp/peaceful-wood-events.txt`.
- Saved `iron workshop` at `(207,89,460)` in this world's initially empty place store. Navigation back to those coordinates completed and inventory inspection confirmed arrival with all six logs. This verifies the contextual return flow in the current world.
- Smelted two collected logs into charcoal using four incidental saplings, retrieved both charcoal and crafted eight torches. This supplied illumination without coal. Evidence: `/tmp/peaceful-supply-events.txt`.
- An eight-coal request from the workshop repeatedly returned `approach_CALC_FAILED`, collecting zero while the player stayed at `(207,89,460)`. Paused and exported before cancelling. Read-only JDWP inspection confirmed real target/work coordinates, `allowBreak=true`, `allowPlace=true`, `allowDownward=false`, and failed A* searches in the recorder. Reachability of these underground work positions was not proven; do not promote this to a pathfinding correctness bug. Charcoal bypasses the immediate fuel/light requirement. Evidence: `/tmp/peaceful-coal-failures.jsonl`, `/tmp/peaceful-coal-internals.txt`, `/tmp/peaceful-coal-1-status.txt`.
- Subsequent `collect_resource(RAW_IRON, 3)` with a 16-block horizontal/vertical scope succeeded without targeting assistance. It approached and mined ore at `(204,95,459)`, `(205,95,458)` and `(205,95,457)`, picked up three raw iron, and completed at tick `10744`. Job `job-3583ac3e-8322-44e1-90de-20b4c7af6f88`; evidence `/tmp/peaceful-iron-1-status.txt`, `/tmp/peaceful-iron-events.txt`. Final inventory at `(204,93,458)` includes the iron pickaxe, three raw iron, eight torches, four logs and 25 cobblestone; health remains 20. Left paused at this completed-job checkpoint.

Recorder exports still report truncation; the previously documented retention defect remains open. No new progression blocker was established by these steps.

### 2026-09-10: rolling-recorder survival playtest

Ran commit `4cb23bac` through `scripts/codex-driver`, resuming the existing `driver` survival save with empty inventory and 20 health at `(76,67,-91)`. This was not a new world or the earlier peaceful recording fixture. Goal: obtain wood, then basic tools and iron. Paused server ticks at each suspected failure and exported the retained recorder window before diagnosis. No gameplay fixes were applied during this run.

**Stopped for a fundamental diagnosis failure: tick-debug pause freezes the server and recorder, but not agent decisions.** At the final pause, snapshot client tick was `6442`, server tick `4397`, debug session `ba52cfa8-0d2f-44b4-a508-d74b421217b2`, epoch `1`. Later reads still reported server tick `4397` and the same pause identity while client ticks advanced through `7148` and `7372`. Event sequence advanced from `119` to `201` to `246`, emitting repeated `reflex.close_quarter_attack` attempts against the same Husk at the same distance `2.2503225803375244`. Recorder status remained byte-for-byte unchanged at sequence `3917`, 65,407,244 retained bytes. Thus inspection time changes reflex state/events without recording those changes. These are client attack attempts, not proof of server damage while paused. Source confirms `ClientRuntimeController.onClientTick` still calls `EmbodiedAgentRuntime.onClientTick`, including survival-reflex ticking. The earlier server-pause fix remains valid; the stronger pause-all decision-state requirement does not hold.

This blocked trustworthy continuation of the requested pause/diagnose loop. It is **not evidence of an unrecoverable gameplay state**: the player remained alive at 20 health; basic wood acquisition and the suspected first-leg flee blocker were not fully tested. The server was left paused for inspection; client decision processing was the defect. Do not treat repeated attacks during this pause as a failed live combat trial.

**Pause remediation (2026-09-10, verified live):** gate the `MinecraftClient.tick` call in the render loop so vanilla actions, Fabric callbacks, Baritone and agent decisions all stop together. Keep render-loop scheduled commands and frame capture available. On pause/step, wait for the matching completed server boundary, admit one client tick, then capture and freeze. Freeze tick interpolation as well. A restart was required to register the new mixin; prefer hot reload for subsequent changes that support it. Against the same Husk UUID after restart, the repeated-read regression held server tick `169`, agent tick `352`, and event sequence `19`; one step advanced to server `170` and agent `353`, then all values froze again. The original check had failed at fixed server tick `4397` with agent `9770 -> 9875` and events `723 -> 744`. Full build passed. Evidence: `/tmp/playtest-pause-check.py`, `/tmp/playtest-fixed-pause.txt`, `/tmp/playtest-fixed-step.txt`, `/tmp/playtest-pause-build.log`.

Local evidence (temporary artifacts; export/copy before cleanup): `/tmp/playtest-final-paused.jsonl`, `/tmp/playtest-final-ticks-{1,2}.txt`, `/tmp/playtest-final-recording-{1,2}.txt`, `/tmp/playtest-final-events-{1,2}.txt`, `/tmp/playtest-wood-near-pause.txt`, `/tmp/playtest-wood-near.png`. Isolated bridge discovery: `/tmp/airicraft-survival-playtest-0910.json`. Launcher log: `/tmp/airicraft-survival-playtest-0910.log` (contains local dashboard credentials; do not publish raw).

Additional observations and bypasses:

- **`exactY:false` navigation used exact Y (fixed live 2026-09-13).** Historical request `(160,67,-100)` used `GoalBlock`, dug sand and required a known-surface workaround. Fresh reproduction requested `(215,103,416,false)` and arrived at `(215,97,416)` but failed because both path construction and completion still constrained Y. Horizontal goals now use `GoalXZ` and X/Z completion; internal workbench/exact surface-return stances retain their required height. Regression failed before the fix; full build passed. HotSwap retry completed, then a fresh request `(215,103,392,false)` completed at `(215,93,392)` with excavation disabled. Evidence and limits: D047 in `autonomous-playtest-log.md` and `run/playtest/2026-09-13/horizontal-*`.
- **Enderman proximity interrupts travel; neutrality of this encounter remains unconfirmed.** The same Enderman triggered safety epochs 1 and 2 at distances 3.78 and 7.80, without recorded incoming damage. Epoch 2 emitted a close-quarter attack. Both resolved and `resume_task` allowed travel to continue. Source's proactive `HostileEntity` scan does not check Enderman anger. The recorder lost the onset, and no anger/target evidence was captured, so this run does not prove an attack on a neutral Enderman. Evidence: `/tmp/playtest-enderman.jsonl`, `/tmp/playtest-enderman-attack.jsonl`; both truncated. No progression blocker demonstrated.
- **Wood collector reports `WAITING_FOR_PICKUP / target_missing` before obtaining wood.** At `(256,66,-137)`, an eight-log request blocked immediately, while an explicit read found four oak logs around `(284,68..69,-140..-138)`. Moving to `(281,64,-141)` and requesting four logs started execution, moved to `(281,64,-125)`, then blocked with zero collected. This disproves distance alone as the full explanation. Exact-break/manual pickup remained an untested possible bypass when the pause defect stopped the run. Evidence: `/tmp/playtest-wood.jsonl`, `/tmp/playtest-wood-near.jsonl`. Do not label this an all-tool progression blocker yet.
- **Task summary can retain an older semantic task after direct navigation completion.** Completion events identified the successful navigation jobs and inventory reads confirmed arrival, while `agent tasks` still displayed an older resumed `QUEUED` mission or cancelled collection mission. Later navigation was admitted and completed. Workaround: correlate the current active job, exact completion-event task ID and actual position; do not infer failure from the stale semantic summary alone.

**Recorder retention defect confirmed under real navigation load.** At server tick `1208`, only 22 records covering ticks `1128..1208` survived: about four seconds, despite no age expiry. Each full snapshot serialized approximately 1 MB of `missionExecution` twice (top-level and nested under `agent`). The 64 MiB cap evicted all earlier debug, decision and semantic evidence, including the interruption onset. Final export was also truncated. The earlier ten-minute peaceful idle validation did not cover this payload growth. Selective event-buffer reads and immediate exports preserved partial evidence, but they do not restore the advertised diagnostic window. Fix payload duplication/size and repeat retention validation with an active task before relying on ten-minute history.

### 2026-09-10: resumed after pause fix; stopped at flee confirmation cycle

Pause fix committed as `f3c7dfba`. Continuing the same Husk encounter completed combat: `reflex.resolved / threats_clear` at agent tick `469`, the Husk disappeared, experience orbs remained, and player health stayed 20. Pause, one-step, continued combat and a later pause all worked. The later repeated-read check held server tick `3947`, agent tick `4129` and event sequence `113`.

**Reproduced progression blocker: flee stops at marginal separation, lets pursuit close the gap, and restarts indefinitely over the observed interval.** Navigation toward the known wood position `(284,64,-141)` (job `job-32977e9c-d220-454e-9cca-fa1eb26cdd64`) was interrupted at tick `1543` by a Creeper, then a Husk and Skeleton. The same safety epoch `2` remained ACTIVE/FLEE through tick `4196`, more than 2,650 ticks (about 133 simulation seconds). By tick `3412` only Husk `c83c432e-5407-4d2d-bbf4-496e5589f027` remained. The player was healthy, dry, and carrying eight sand with food level six. Attempts to navigate were rejected: `reflex_active. Only read and cancel/clear controls are allowed during an active survival reflex.` No wood or tools were obtained.

The short final recorder interval captures the repeated failure directly:

| Agent tick | Husk route nodes | Security ticks | Flee navigation owned | Completed escape legs |
| --- | --- | --- | --- | --- |
| 4123 | 15 | 0 | true | 22 |
| 4128 | 16 | 2 | false | 22 |
| 4135 | 16 | 9 | false | 22 |
| 4140 | 15 | 0 | true | 22 |
| 4155 | 15 | 0 | true | 23 |
| 4170 | 16 | 4 | false | 23 |
| 4175 | 16 | 9 | false | 23 |
| 4180 | 15 | 0 | true | 23 |
| 4195 | 16 | 9 | false | 23 |

All listed routes are REACHABLE. `SurvivalReflexRuntime.tickMobAttack` stops movement immediately when `classifyThreatSecurity` labels a route of at least 16 nodes `DISTANT_PATH`, then requires 60 stable ticks before resolving. The pursuing Husk closes the gap while the player waits. Escape escalation requires a reachable path of at most eight nodes (or repeated close contacts), so the observed 15/16-node cycle neither resolves nor escalates even after 23 completed legs. Cancel/clear removes interrupted work but source confirms it preserves an ACTIVE reflex; it is not a movement override. This is a practical progression blocker in the observed encounter, not a proof that every possible terrain or future despawn can never resolve it. It is distinct from the earlier unconfirmed zero-leg target-selection hypothesis.

Stopped with the encounter paused, preserving the player alive and the current control hold `3fa8855f-0cfe-4aac-a633-0a460e18fdec`. Main evidence: `/tmp/playtest-flee-cycle.jsonl` (includes frames and the table above), `/tmp/playtest-flee-cycle-pause.txt`, `/tmp/playtest-flee-cycle.png`. Longer partial context: `/tmp/playtest-mixed-flee.jsonl`, `/tmp/playtest-flee-water.jsonl` (filename was a suspicion; snapshot proved dry ground), `/tmp/playtest-flee-final.jsonl`, `/tmp/playtest-flee-monitor-{0..5}.txt`, `/tmp/playtest-flee-final-events.txt`. Exports report truncation from the separate payload-size defect; the short cycle itself is retained. No reflex fix was attempted after reproducing this blocker.

### 2026-09-10: combat-first policy, hot-reloaded and verified

Policy changed at the user's direction: fight aggressors regardless of health, equipment, mob count or ranged/explosive type. Remove open-ground flee selection and its distance-confirmation loop. Ordinary movement enables Baritone mob avoidance (radius 16, cost multiplier 4); proximity by itself no longer authorizes an attack. Admit threats through correlated damage or a confirmed AI target matching the player. Integrated-server target reads run on the server thread and return immutable IDs; remote clients rely on damage and any available target evidence. Only an actually blocked, occluded pursuit route can confirm shelter; long/partial routes do not count. Existing terrain can shelter the player; automatic tunnel construction is not implemented. Drowning recovery keeps priority. The old `defendMinHealthRatio` remains parseable for compatibility but has no combat-policy effect.

Applied via the existing JDI HotSwap uploader while the encounter remained paused; no restart or runtime reset. One step at agent tick `4197` changed the same safety epoch from FLEE to DEFEND. A second Husk was admitted at tick `4198` with `source=aggro_target` at distance 13.77. The player fought both: one paused read showed Husk health 3.7 and 8.4, followed by `reflex.resolved / threats_clear / AWAITING_PLANNER` at tick `4645`. Both Husks disappeared; the player remained at 20 health. After final cleanup was hot-reloaded, resuming the same hold returned control to navigation. The original job `job-32977e9c-d220-454e-9cca-fa1eb26cdd64` completed at tick `5112`, and inventory inspection confirmed arrival at `(284,64,-141)` with one rotten flesh and eight sand. This resolves the reproduced flee progression blocker.

The full build passed. Regression tests cover aggro-based admission, blocked/occluded shelter classification, and default avoidance settings; cooldown/drowning tests still pass. A read-only JDI probe confirmed actual running Baritone values `avoidance=true`, `mobAvoidanceRadius=16`, `mobAvoidanceCoefficient=4.0`. Route-wide avoidance effectiveness, low-health mixed-mob survival outcomes, and an exact tunnel-sealing scenario were not separately tested. Nearby loaded cave Skeletons were not attacked during the resumed navigation interval.

Evidence: `/tmp/combat-policy-hotswap.log`, `/tmp/combat-policy-final-hotswap.log`, `/tmp/combat-policy-step.txt`, `/tmp/combat-policy-recording.jsonl`, `/tmp/combat-policy-finish-events.txt`, `/tmp/combat-policy-final-recording.jsonl`, `/tmp/combat-policy-arrival-pause.txt`, `/tmp/combat-policy-build.log`. The recorder payload-size defect still reports truncation, so event-buffer reads supplement these short retained intervals. The client was left paused at the wood destination after verification.

Minor harness issue: tool catalog descriptions retained startup `Current` values for Baritone settings after HotSwap (false/8/1.5), while direct read-only inspection confirmed true/16/4. Do not use cached schema prose as a live settings assertion. No fix included for this display issue.

### 2026-09-10: wood bootstrap continuation

Continued the same live client with `7f4498f6` hot-reloaded. No restart or gameplay code changes. Ate the dropped rotten flesh (`food.eaten`, tick 5540), raising food from 5 to 9. Kept pausing and exporting at suspected failures.

- **Overhead exact-break reach is unnecessarily restrictive; scaffold bypass verified.** From `(284.32,64,-140.32)`, breaking the observed log `(284,68,-140)` failed `target_out_of_range`. `BlockBreakTaskExecutor` compares feet-to-block-center distance against 4.5 blocks rather than eye/interaction reach. Placed one carried sand at `(284,64,-140)`, navigated onto it at Y=65, and the same log broke successfully. Evidence: `/tmp/playtest-wood-reach.jsonl`, `/tmp/playtest-wood-reach.png`.
- **Exact-break delayed completion is missing its promised task update in this run.** The log break returned `pending_timeout` after 40 ticks, explicitly instructing the driver to wait for TASK UPDATE. At the later pause, its exact active job `job-fbf03e64-cd97-41b8-9c5d-2698f8f13b84` was COMPLETED, updated tick 6521; the event buffer still ended at seq 209 (`task.started`, tick 6459). A world read confirmed the log was gone. Workaround: poll exact active-job state and verify the block change instead of relying solely on an event. This is not an executor timeout or failed break. Evidence: `/tmp/playtest-break-stalled.jsonl`, `/tmp/playtest-break-stalled.png`.
- **Manual wood pickup bypass now verified.** The broken log rested on azalea leaves at `(284.1,68,-139.5)`, three blocks above the player. Navigation to its block failed; the log at Y=69 also occupied headroom, so that failure alone is not a pathfinding defect. Exact-breaking the freshly observed supporting leaf `(284,67,-140)` dropped the item into pickup range. Inventory confirmed one oak log and `pickup.item_picked_up` at tick 7292. Crafted it into four oak planks at tick 7778. This establishes an exposed-tool workaround for the earlier zero-wood collection failure. Evidence: `/tmp/playtest-drop-route.jsonl` and subsequent event/inventory reads.
- **Surface tree mining hits the illumination guard.** Direct `mine_blocks` for the remaining oak logs returned `insufficient_illumination / loaded_target_unilluminated`. Retried with the exposed `allowUnilluminated=true` override after inspecting the surface tree. One more log was collected at tick 8179, then mining began a long surface detour. The guard is bypassable; this observation does not identify which candidate triggered it or prove a lighting calculation defect. Detour evidence: `/tmp/playtest-mine-detour.jsonl`.

Recorder exports continue to report truncation; the full requested ten-minute history is still unavailable under task payload load. Supplemented the exports with exact job status, fresh world reads and the event buffer.

**Stopped at a serious surface-recovery defect: an open ravine floor is selected as the surface.** The direct mining run collected only one of three requested logs, travelled from the tree through `(224,71,-140)` and `(227,70,20)`, then descended into a mineshaft. Paused at tick 9746, `(205.61,41.23,37.53)`, still with health 20, one log, four planks and eight sand. Cancelled mining. The deliberately supplied illumination override allowed underground work, so this detour is not by itself proof of an illumination-policy violation or failed mining executor.

From that paused state, `return_to_surface {"useTowering":true}` selected `nearest_surface=(200,30,29)`, eleven blocks LOWER than the player, rather than an exit to the surrounding desert. After continuation, a Cave Spider acquired the player at tick 9842 and interrupted recovery; a Zombie joined at tick 9969. The next pause at tick 9980 was `(199.73,30,29.47)`, health 20, with `RETURN_TO_SURFACE` job `job-1ddd171c-1d1b-42c6-ae0e-9856d065c86e` BLOCKED by ACTIVE/DEFEND safety epoch 3. The interrupted job still records the Y=30 target. Reflex movement overlaps this interval, so do not attribute every block of descent solely to the return executor.

A fresh vertical world read at `(200,29..90,29)` found stone support at Y=29 and air above; looking upward visibly showed a deep ravine opening to the night sky. `SurfaceMemory.isSurfaceStandingPosition` accepts sky visibility, solid support/headroom and two adjacent qualifying standing positions. `findNearestSurface` ranks qualifying positions by Euclidean distance and `bestTarget` always prefers that candidate over remembered surface ground. Thus an open ravine bottom qualifies even though reaching it does not recover the player to the surrounding terrain. `ReturnToSurfaceTaskExecutor.isSurfaceReached` uses the same predicate, so its completion criterion also permits this terrain; completion was not observed because the reflex interrupted it.

This is a reproduced recovery-target/meaning defect with practical progression impact: wood bootstrap remains unfinished and the recovery task is now held by combat in the ravine. It is **not proof of an unrecoverable save or that every manual navigation/towering escape fails**. Stopped to diagnose this serious issue rather than run unprepared cave combat or assert all workarounds are exhausted. No gameplay fix applied.

Paused identity: debug session `95d7d6ed-d2b4-41be-8c47-3131e1e2807a`, epoch 1, server tick 9797, agent tick 9980; hold `043e9f14-f060-428b-8065-353a59f5dc13`. Repeated-read validation held server tick, agent tick and event seq 233 unchanged. Main evidence: `/tmp/playtest-wood-underground.jsonl`, `/tmp/playtest-wood-detour-end-pause.txt`, `/tmp/playtest-surface-target.jsonl`, `/tmp/playtest-surface-target-status.txt`, `/tmp/playtest-surface-target-events.txt`, `/tmp/playtest-surface-target-pause.txt`, `/tmp/playtest-ravine-sky.png`. The export retains ticks 9886–9980 for decision states; the command results and event buffer preserve the earlier target selection and aggression onset.

### 2026-09-10: explicit place memory added while paused

Implemented the first part of the agent-owned destination design: remember/recall/list/forget named coordinates, persisted in the world save. See [place memory](place-memory.md) for the tool contract and validation. HotSwap and a targeted tool-registry update preserved the same paused client, combat hold and tick identities. Saved `ravine incident` at `(199,30,29)` and `wood tree approach` at `(284,64,-141)` with notes explaining their context. Both survived provider recreation and were verified on disk. The ravine incident is not labelled a safe destination; no movement or escape was attempted. The legacy surface selector remains for existing callers in this first pass.

### 2026-09-10: place-memory playtest stopped at stalled combat ownership

Continued the same paused client with `cf14c4aa` and the combat-first policy hot-loaded. `recall_place("wood tree approach")` correctly returned the persisted overworld destination `(284,64,-141)`. Cancelled the obsolete `RETURN_TO_SURFACE` task at tick 9980 before continuing server ticks. Events 234/235 confirm its hold was released and its exact job cancelled; ACTIVE combat safety epoch 3 remained in control, as cancellation is not a reflex override.

**Reproduced practical progression blocker: active combat makes no combat progress and prevents explicit destination navigation.** Continued from agent tick 9980 to 10858 (878 ticks, approximately 44 simulation seconds). The player moved a short distance from `(199,30,29)` onto `(197,29,29)`, then remained at that block in the subsequent inventory samples and final pause. Status samples were taken at ticks 10197 and 10553; the final exact pause was `(197.2379,29,29.0443)` at tick 10858. Health remained 20, food 6 at the endpoint; no attacks, player damage or reflex resolution appeared in the event buffer. The only new threat event was a Creeper admitted through `aggro_target` at tick 10043. Final threats were an occluded Creeper about 16 blocks away, an occluded Zombie about 19 blocks away, and an occluded Cave Spider about 28 blocks away, below/around the ravine ledge.

Paused immediately after suspecting the stalled approach and exported `/tmp/place-playtest-combat-stall.jsonl`. Its retained decision interval 10765–10858 shows ACTIVE/DEFEND throughout, `combatTarget=(208,22,20)`, and zero shelter-confirmation ticks. Creeper/Cave Spider routes were PARTIAL; the Zombie route was REACHABLE. Those are client-side mob-route estimates, not proof of server mob reachability. They keep the current shelter rule from resolving. At the pause, calling `navigate_to(284,64,-141,exactY=true)` with the recalled coordinates was rejected with `reflex_active`. Thus named-place recall worked, but movement never reached the new navigation job.

Read-only JDI inspection of the same paused client found `CustomGoalProcess.state=EXECUTING`, `GoalNear(208,22,20,rangeSq=4)`, and `SET_GOAL_AND_PATH` in control. `PathingBehavior.current`, `next`, and `inProgress` were all null. The facade's remaining path-event queue contained `CALC_STARTED`; `combatRouteTick=10838`. These are point-in-time observations, not proof of the exact earlier calculation failure. `SurvivalReflexRuntime.defend` refreshes navigation when the target changes or the process is inactive; it does not bound progress or consume path outcomes here. `processActive()` accepts an active custom-goal process. This supports investigating combat navigation progress/ownership, but the precise cause of the idle EXECUTING state remains unconfirmed. No gameplay patch was made.

Stopped with the player alive and the game paused for diagnosis. This is a practical block over the observed interval, not a claim that future mob movement/despawn or every possible terrain makes recovery impossible. The previously cancelled work is not resumed. Final pause: session `8b73b639-adbf-4801-aa33-b42b65b137f3`, epoch 1, agent tick 10858. Repeated-read validation confirmed the server, agent and event seq 236 remained frozen.

Evidence: `/tmp/place-playtest-combat-{1,2}.txt`, `/tmp/place-playtest-combat-1.png`, `/tmp/place-playtest-combat-stall-pause.txt`, `/tmp/place-playtest-combat-stall.png`, `/tmp/place-playtest-combat-stall.jsonl`, `/tmp/place-playtest-combat-final-events.txt`, `/tmp/place-playtest-combat-navigation-detail.txt`. Recorder truncation persists, so event-buffer and direct runtime reads supplement the short exported history. The verbose state dump is `/tmp/place-playtest-combat-stall-state.txt`; do not mistake its cancelled task execution snapshot for the reflex-owned Baritone process.

### 2026-09-10: ignore distant melee threats; hot-reload verified

At the user's direction, non-ranged mobs only participate in the combat reflex within six blocks (3D distance). Apply the same predicate both to proactive admission and to already-tracked mobs, so a distant previously aggressive melee mob cannot retain combat ownership. Ranged capability retains the existing policy, including Minecraft ranged/crossbow interfaces and innate projectile, beam and spell attackers. Classification is conservative by capability, not current equipment. The recent-damage cooldown still applies, and nearby melee threats remain eligible. No generic pathfinding retry policy was added.

Full build and focused reflex tests passed. Hot-swapped the changed reflex classes without resetting the client. From the exact stalled encounter at tick 10858, one debug step emitted `reflex.resolved / threats_clear / nextState=IDLE` at tick 10859. The distant Creeper, Zombie and Cave Spider no longer retained ownership. Recalled `wood tree approach` and submitted its exact coordinates `(284,64,-141)`; navigation was accepted where it had previously been rejected as `reflex_active`.

Ran a short monitored navigation interval through tick 11211. The player left the ledge and travelled through `(186,34,34)` to `(184.50,28.25,37.81)`. The reflex stayed IDLE, no new attack/damage/reflex events appeared, and health remained 20. Navigation is still RUNNING; this verifies released control and actual movement, not arrival or a completed cave escape. Paused after the validation interval: session `fe19383a-1c2b-42e3-8c9d-5e8ae8e348c2`, epoch 1. The original stalled-combat reproduction is resolved for these distant melee threats; ranged approach failures remain outside this fix.

Evidence: `/tmp/melee-range-before.jsonl`, `/tmp/melee-range-hotswap.log`, `/tmp/melee-range-step.txt`, `/tmp/melee-range-verification-events.txt`, `/tmp/melee-range-nav-{0,1,2}.txt`, `/tmp/melee-range-nav-final.txt`, `/tmp/melee-range-nav.png`, `/tmp/melee-range-after.jsonl`, `/tmp/melee-range-build.log`. Recorder truncation still limits the retained history.

### 2026-09-10: surface return, supply-route death, and skeleton combat

Continued `aa16337f` in the same hot-reloaded client. The ravine-to-tree navigation kept its exact goal `(284,64,-141)`, but descended through Y=3 and backtracked to `(236,18,43)`. At tick 12312 the estimate was about 46,231 ticks, up from roughly 5,935 at submission. Paused and exported before cancelling. This establishes an expensive, unproductive observed detour, not an incorrect goal or proof that no path existed. Evidence: `/tmp/playtest-return-deep.jsonl`, `/tmp/playtest-return-deep-pause.txt`, `/tmp/playtest-return-deep-status.txt`.

The driver's replacement supply route was a poor survival choice: navigating toward observed mineshaft planks at `(227,21,33)` brought the unarmed player into a Zombie and several Cave Spiders. Melee threat admission occurred within 5.69–5.98 blocks, consistent with the new six-block policy. Damage prompted a pause request, but the player died at tick 12758 before pause took effect and respawned at tick 12760. This is an actual combat loss, not evidence of another executor bug. Named places survived death; added `spawn camp` at `(74,67,-101)` with an explicit note that no shelter, bed or supplies existed there. Evidence: `/tmp/playtest-swarm-events.txt`, `/tmp/playtest-swarm-pause.txt`, `/tmp/playtest-swarm.jsonl`.

From spawn, exact navigation toward `wood tree approach` made surface progress. A Skeleton targeting the player was admitted at 14.36 blocks at tick 13659, correctly retaining ranged-threat handling. Paused for inspection, then continued under a health monitor. The reflex approached, attacked, killed the Skeleton, and resolved at tick 13981. The paused entity read confirmed health zero and nearby Bone/Arrow drops; player health was 6.5. Resuming hold `4cd2d59f-0d7c-4051-8ad9-5f6924e5ba2f` completed navigation job `job-5cfeafb0-e4f7-45a1-96f0-3a86efa28d52` at tick 14070, reaching `(284,64,-141)`. This is live proof of this unarmed ranged encounter and explicit return, not general ranged survivability. The known stale semantic task summary still said QUEUED after the active navigation job completed; subsequent navigation remained available.

Fresh inspection found two remaining logs at `(284,69,-139)` and `(284,69,-138)`. Moved onto the previously placed sand at `(284,65,-140)` and paused at tick 14231, session `021d63ab-cb46-4d28-af08-9eea7d3353bb`, epoch 1. No new wood or tools were acquired in this interval. Inventory contains one Arrow and two Bones. Evidence: `/tmp/surface-skeleton-onset.jsonl`, `/tmp/surface-skeleton-combat.jsonl`, `/tmp/surface-skeleton-events.txt`, `/tmp/surface-skeleton-end.png`, `/tmp/surface-tree-arrival-events.txt`, `/tmp/surface-tree-arrival.jsonl`. Exports remain truncated by the known recorder payload limit; event-buffer reads supplement them.

**Acquisition ownership direction, not implemented in this run:** Airicraft should choose observed resource coordinates and work positions, perform exact block interactions, pursue observed drops, and verify inventory gain. Baritone should handle navigation to explicit positions, returning path outcomes. Current `LiveBaritoneFacade.startMine` delegates block names and quantity to `mineByName`, giving Baritone broader acquisition ownership. Airicraft already tracks inventory deltas and pickup positions, so extend that existing owner instead of adding another orchestration layer. Failed or unproductive approaches must return control with evidence rather than trigger indefinite searching. The earlier ravine detour used an explicit navigation goal; removing `mineByName` alone would not address that navigation case.

### 2026-09-10: System 1 scoped acquisition

Implemented [scoped acquisition](target-acquisition.md) for `collect_resource`, `mine_blocks` and `ensure_blocks_in_inventory`. System 2 supplies the resource and optional area/surface constraints; System 1 selects observed targets, approaches, clears obstructing leaves, breaks and collects drops. Production mining dispatch uses `TargetAcquisitionTaskExecutor`; Baritone receives explicit navigation positions. Exact `break_blocks` remains available. The old nearby-block admission gate no longer prevents the executor from discovering targets or pursuing drops.

Hot-swapped the changed classes and refreshed the tool definitions without restarting Minecraft. The first runtime reload installed the new runtime but failed while shutting down an old executor record whose added fields were null after HotSwap. The superseded instance was subsequently collected; a second reload completed, including rebinding the recorder. This is a development reload artifact, not a cold-start constructor failure. A nearby Spider engaged during reload; the existing reflex killed it and released control. Player health was 4.5 for the acquisition trials.

The first two-log surface attempt moved near the tree and failed with `no_reachable_resource_in_scope`, zero inventory gain and zero rejected targets. Both logs remained. Paused and exported `/tmp/acquisition-first-incident.jsonl`; truncation prevents attributing the complete first attempt to one cause. Source inspection subsequently found that the work-position scan excluded ground five blocks below an overhead source even when the source was within eye reach. Widened the candidate-height range and added a geometric regression; this does not establish the entire first attempt's root cause.

**Live collection succeeded on the repeat call, before that final geometry adjustment.** A single `collect_resource` call with `WOOD_LOGS`, quantity 2, `surfaceOnly=true`, radius 16 created job `job-9a0995ce-e223-42e3-9046-cd1504000a31`. It started at agent tick 1614, selected the two observed logs at `(284,69,-138)` and `(284,69,-139)`, cleared leaves, broke both logs, and entered explicit drop pursuit. Inventory pickup events confirmed one log at tick 1708 and the second at 1831. `task.completed` at 1832 reported collected 2, remaining 0. No intervening acquisition or target-selection command was supplied; a debug pause/continue preserved the same task. Final inventory: two Oak Logs and one incidental Stick, plus Arrow, Bones and Spider Eye. Evidence: `/tmp/acquisition-repro-status.txt`, `/tmp/acquisition-two-logs-progress.jsonl`, `/tmp/acquisition-logs-success.jsonl`, `/tmp/acquisition-logs-events.txt`, `/tmp/acquisition-result.png`.

After hot-swapping the work-position adjustment, `ensure_blocks_in_inventory` for two Oak Logs correctly returned `already_satisfied`. A `mine_blocks` request for one additional Oak Log with surface-only radius 1 failed in two debug steps with `no_reachable_resource_in_scope`, zero matching breaks and unchanged position/inventory. It did not treat the two existing logs as a newly mined block or start distant searching. A Dead Bush inventory request was rejected by the existing nighttime illumination preflight; that call did not test harvesting and no override was supplied. Evidence: `/tmp/acquisition-empty-scope.jsonl`, `/tmp/acquisition-empty-terminal-status.txt`, `/tmp/acquisition-empty-events.txt`.

The final pause is session `ced76f3c-06f1-4ab1-a130-402a203064b5`, epoch 3, client tick 16649, server tick 15442, agent tick 1895. Repeated reads held the server, agent and event sequence 24 fixed. Recorder exports remain truncated by the earlier payload-size defect. Automated coverage includes scope propagation/validation, eye-reach work positions, inventory-authoritative completion, failed approaches, scope exit, executor routing, and retaining acquisition budgets through reflex withdrawal. Geometric surface classification and the final work-position adjustment still need broader terrain coverage; this run is not general acquisition reliability proof. Final `./gradlew build` passed (`/tmp/acquisition-complete-build.log`); the final environment and continuation changes were hot-swapped without another runtime reset, and the same pause identity remained frozen.

### Launch

- Client launch started with an isolated bridge-state file at `/private/tmp/airicraft-codex-driver-01a05cde.json`.
- Initial sandboxed launch could not open Gradle's user-cache lock file. Retried the same local launcher outside the sandbox; this is an environment permission boundary, not an Airicraft gameplay defect.

### Tick-debug proof

- Paused at client tick `914` using `agent debug ticks pause`; the returned snapshot and rendered `854x480` frame showed the survival player stationary on a desert coast.
- One `step` using the returned `debugSessionId` and `pauseEpoch` advanced the client tick to `915`, rotated the pause epoch to `2`, and returned a fresh frame. `ticks state` remained `PAUSED` with that exact identity.
- The snapshot's `worldTime` moved from `318` to `640` while `clientTickId` moved exactly once. This discrepancy is recorded below for investigation; the client-tick contract itself held.
- **Server-side pause remediation (2026-09-04, fixed live).** `fd7d7743` moves tick-debug authority to the integrated server: while paused, `MinecraftServer.tick` is cancelled; a step permits exactly one server tick. In a fresh integrated-server run, `serverTickId` stayed at `127` across 10 seconds paused, then advanced once to `128` after one `step`. Client `worldTime` is not used as server-simulation proof because the client continues rendering/ticking for snapshot capture.

### Remediation pass

- `70639dbc` and `4890f796` repaired the high-level diamond route. Cataloged raw resources now enter the mining provider directly; unobserved craftable blocks are not treated as speculative mining sources; tool recipes craft sticks instead of planning dozens of leaf breaks.
- `a18d2b7b` reconciles a Baritone `CALC_FAILED` result to success when the exact navigation goal is already reached. This does not make an actually unreachable destination reachable.
- `4c9c49ea` applies the existing mining-tool preflight to `break_blocks`, so an exact break selects a valid best tool before attacking. Drop pursuit remains separate work.
- `91f66673` prevents recipe automation from reusing a workstation substantially below the player when a portable crafting table is carried. The automated regression is green; the original terrain has not been replayed live.
- `db2da62d` accepts overworld planks as surface-tower filler and terminates 100 consecutive `support_unavailable` ticks instead of livelocking. Broader shore discovery remains open.
- `48055ad9` makes every `AWAITING_PLANNER` survival hold resumable by requiring and, for a safety-only drowning hold, generating a `holdId`. The status/action time-of-check race remains possible and should still be handled as a normal rejected action.

## Non-fatal bugs / workarounds

- **Client world-time comparison is historical, not an open server-pause bug.** Evidence: pause snapshot at `clientTickId=914` reported `worldTime=318`; the one-step snapshot at `clientTickId=915` reported `worldTime=640`. Current interpretation: use `serverTickId` to measure server simulation and `pauseEpoch`/snapshot IDs to identify captures. The server-owned fix below supersedes the original client-only timing workaround; client `worldTime` is not a server-step assertion.
- **Historical navigation failure near, but not at, the requested destination; current defect unconfirmed.** Evidence: `navigate_to(284,67,-141)` started at `(65,67,-86)`, moved the player to `(281,65,-144)`, then ended `CALCULATION_FAILED` / `Path calculation failed` with an estimated 25 ticks remaining. The target was visibly in a small azalea/oak forest. Workaround: once within local-resource range, switch to the typed resource collector rather than retrying the same exact navigation goal. The recorded endpoint does not satisfy the requested exact goal. This proves that run failed, not that pathfinding incorrectly rejected a reachable target. The reached-goal reporting defect is fixed separately.
- **Exact break has no drop-pursuit contract; the historical timeout needs its own reproduction.** The original run reported `pending_timeout` after lower blocks were broken. Current exact-break execution confirms block changes, not inventory pickup; it does not proactively chase drops. A wrapper timeout alone does not identify an executor failure. `4c9c49ea` now selects the best valid inventory tool before every exact break. `a18d2b7b` maps `CALC_FAILED` to goal reached only when the active exact navigation goal is in fact satisfied. Workaround for the remaining issue: navigate to each known drop coordinate.
- **Automatic workstation selection cave preference is fixed in source; live terrain replay is pending.** In the original run, a carried crafting table was ignored in favor of a table around five blocks below the player, routing a safe surface player into a natural cave. `91f66673` makes a substantially lower found table ineligible when a portable table is carried, and its policy regression passed. Explicit surface placement remains the conservative workaround until that exact terrain is replayed live.
- **Tick-debug pause did not freeze hostile damage (fixed live 2026-09-04).** Historical evidence: after `pause` returned `paused: true`, `clientTickId=3963`, and `pauseEpoch=1`, the event stream recorded an arrow hit, fall damage, four zombie hits, death, and respawn, all at tick `3963`. The client-only tick gate let the integrated server continue running. Fix: `fd7d7743` makes the server own pause/step state and gates `MinecraftServer.tick`; the client now only coordinates frame capture. Live verification held `serverTickId=127` for 10 seconds paused and allowed exactly one tick (`128`) for one `step`. This freezes server-side mob AI, damage, and world simulation while preserving client-side snapshot rendering.
- **Blocked collection retained an active mine primitive (fixed live).** Evidence: wood collection declared `WAITING_FOR_PICKUP` / `target_missing` after collecting 4 of 16 logs, but its active mining task continued, moving from Y=67 to Y=44 before a hostile reflex. Cause: a blocked collection job still exposed its mine goal and retained its desired primitive request. Fix: only `RUNNING` collection jobs may expose or retain a primitive. Focused `EmbodiedAgentRuntimeTest` passed; hot-reload validation submitted a no-target one-log goal and confirmed `taskExecution=IDLE`, no goal ownership, and no movement.
- **Task lifecycle responses amplified full world evidence (fixed live).** Evidence: plain `agent tasks resume` and `agent tasks cancel` each emitted about 306k tokens, including the full known craft/smelt catalog, for a one-line lifecycle acknowledgement. Fix: the normal wrapper view now projects only lifecycle, compact reflex, compact task execution, and compact task fields; `missionExecution` remains verbose-only. `AiricraftCliMainTest` covers the omission, `:wrapper:test` passed, and the rebuilt CLI returned the bounded cancellation response against the live client. Commit: `b18376a`.
- **Reflex admission can change between calls; the unresumable drowning wedge is fixed in source.** Evidence: `agent status` reported `reflex.state=IDLE`, then an immediate `craft_recipe` was rejected as `reflex_active`. Later it stayed `AWAITING_PLANNER` for `DROWNING` with full air and no `holdId`. `48055ad9` now requires a hold identity for `AWAITING_PLANNER` and generates one for safety-only drowning holds; focused runtime tests cover resume from that state. This is expected action-time safety validation, not proof of a stale-status defect. Treat `reflex_active` as authoritative and retry only after the new reflex epoch resolves. Live drowning was not deliberately induced during the remediation pass.
- **Terminal navigation could retain a stale mutation gate (fixed live).** Evidence: after a `navigate_to` terminal `CALC_FAILED` and explicit cancellation, `agent status` showed no runnable job but `craft_recipe` was denied `active_task_in_progress` because its old `PAUSED_BY_REFLEX` execution snapshot was consulted before the terminal task/job states. Fix: terminal semantic task + terminal/idle active job now wins over stale execution state. Focused `EmbodiedAgentRuntimeTest` passed. After a client restart, the live pickaxe call reached its true `crafting_table_place_failed ... site_not_found` result rather than the stale-task denial. Commit: `7e4a1c5`.
- **`return_to_surface` no-support livelock is fixed in source.** Evidence: the original executor stayed `RUNNING` for more than 1,400 ticks at `towering:support_unavailable`, and its defaults ignored available oak planks. `db2da62d` adds overworld planks to the default filler set and fails deterministically after 100 consecutive unsupported ticks. Focused executor/argument tests passed, and the restarted live tool schema exposed the expanded defaults. This bounds failure; it does not discover distant shore.
- **Nearest surface search is too local after reflex displacement.** The search radius was widened from 12 to 16 blocks (`f53b49e`) after it missed proven village terrain about 15 blocks away; focused `SurfaceMemoryTest` passed. The radius is confirmed in current source; `SurfaceMemoryTest` tests refresh and standing policy, not that exact 15-block terrain. A later reflex moved the player to `(-104,60,-44)`, where no forest was found in the loaded 256-block feature search and the camera showed only open ocean floor. The local scan cannot discover new land beyond 16 horizontal blocks. However, current `SurfaceMemory` also retains `lastSurface` and `lastGround` during the session; failure of all recovery options in this save has not been freshly reproduced.
- **Mob flee used blind movement and could enter water (fixed).** The reflex now selects loaded, dry, hazard-free standing targets that increase separation, prefers cover, reaches targets through short exact Baritone hops, and raises the configured water traversal cost while it owns the route. If water is already moving or submerging the player, it hands recovery to the existing water-aware safe-standing navigator before resuming ground flee. The deterministic water regression remains useful for exact terrain control, but live acceptance now runs in the untouched, naturally generated snowy-forest `pickup` world.
- **A short flee could falsely resolve and wait for the pursuer (fixed).** Threats are retained while alive and loaded rather than dropped at 12 blocks. Security uses each mob's own route toward the player, not L2 distance or player-origin reachability: a zombie below a three-block pillar and a skeleton behind a sealed wall can be inaccessible even when close, while ranged line of sight remains dangerous. Complete routes of at least 16 nodes require 60 stable ticks; null/partial routes require 200 ticks, so a transient partial path is not accepted as shelter. The first natural run exposed the old short confirmation by resolving at tick `1087` before the same zombie returned at `1192`; that criterion was removed.
- **Close single-mob combat now terminates the encounter (fixed live).** In the final natural-world run, a low-health reflex started `FLEE` at tick `478` and immediately emitted `reflex.flee_knockback` against the zombie at `2.05` blocks, before path selection. It switched to `DEFEND` when safe, continued cooldown-timed `reflex.close_quarter_attack` hits, earned `Monster Hunter` at tick `564`, and resolved `threats_clear` at tick `565` without a second safety epoch. The exact tick-`1577` post-combat snapshot was survival mode at `(16.57,80.0,5.38)`, alive with full health, on dry ground, and not touching or submerged in water.
- **High-level diamond mapping and bootstrap selection are fixed live.** The original route chose `diamond_block_to_diamond`. After `70639dbc` and `4890f796`, a restarted client resolved the same fresh-inventory `DIAMOND` goal to wood collection, crafted sticks and pickaxes, raw-iron mining/smelting, and a final `minecraft:diamond_ore` mining step. The 30-step live route contained no leaf mining, `minecraft:iron_block`, or `minecraft:diamond_block`; it was cancelled after route proof rather than left to mine unattended.

## Harness gaps / discoveries (non-mission-critical)

- **Exact block break exists but was easy to miss in discovery.** `break_blocks` is an exposed, coordinate-grounded tool; it requires a fresh `inspect_world`/feature read and direct interaction range. At `(8,70,-104)`, inspection found five oak logs at `(8,69..65,-104)` and `break_blocks` completed the top four. `inspect_inventory` then confirmed `minecraft:oak_log=5`. This is the safe workaround for the earlier `mine_blocks` divergence, not a missing API.
- **Automatic workbench setup needs a feasible-site explanation.** `craft_recipe` reported `crafting_table_place_failed`, but the driver needs the exact rejected terrain constraints and a preferred reachable target, not just `site_not_found`, to recover without trial-and-error.
- **Surface recovery now has a bounded water-aware route, not global shore discovery.** Mob flee and drowning recovery can reach a loaded safe-standing target without blind swimming, but `return_to_surface` still depends on remembered or locally discoverable land. The source already retains safe-ground memory within a session and clears it on world loss. It has no persistent cross-session safe-ground store or global shore discovery. A missing local target is a capability limit, not by itself proof that all navigation workarounds fail.

## Historical progression failures and current gaps

These runs stopped or endangered progression, but the audit below does not establish a current failure with every exposed-tool workaround exhausted. They are not all confirmed current blockers.

- **Deep-cave readiness is incomplete; historical death is not a fresh reproduction.** The 2026-09-01 forest run descended from Y=48 to Y=-6 without food, armor, a bed, or a safe-return plan, then died during a skeleton reflex. Current `MiningIlluminationPreflight.admit` allows predicted underground mining with one torch or the explicit override; it does not assess food, equipment, shelter, or a return route. The zero-torch guard and ore routing are fixed. This verifies a readiness-policy gap, not that every unarmored or bedless mining trip must fail or that requiring all those items is the right fix.
- **Entity acquisition lacks autonomous exploration; the 32-block inspection limit is confirmed.** `EntitySelectorResolver.DEFAULT_NEARBY_RADIUS_BLOCKS` is 32, used by `NearbyEntityService` and reported by `CurrentInventoryService`. The tool catalog has local inspection and entity interaction but no dedicated sheep-search-and-return tool. Failed historical sheep sweeps do not prove wool or a bed is unobtainable: navigation followed by another scan remains available. Treat this as an automation gap, not an established Dragon-progression blocker.
- **Ranged encounter survivability needs a new reproduction.** The historical creeper/skeleton encounter blocked crafting and reduced health from 20 to 17.17 to 13.53 while the skeleton stayed about 11–12 blocks away. Those observations show damage, not a death in those two windows. The old `leaf_litter` explanation is superseded: `tickMobAttack` now equips the best combat hotbar item before either FLEE or DEFEND, and a regression ranks a stone pickaxe above leaf litter. Current security checks account for ranged line of sight and the flee selector prefers shelter. The earlier single-zombie success does not validate mixed ranged encounters. The original client is not currently paused; the old assertion that pause permits server damage is superseded by `fd7d7743`.
- **Displaced-ocean recovery is a historical failed attempt, not a verified current save-wide block.** The original run reported no crafting site, no adjacent placement support, and no usable surface target after displacement. The local radius and lack of cross-session memory/global shore discovery are confirmed. The saved terrain, inventory, remembered targets, and all exposed navigation alternatives have not been replayed together in this audit; do not assert that the save remains unblocked or blocked from the old snapshot alone.

## Claim audit — 2026-09-10

Audited checkout: `dcc1b725`. Production code was not changed. `source .envrc` followed by `./gradlew test :wrapper:test` succeeded: root XML reports 1,027 tests, zero failures/errors, two skipped; wrapper reports 88 tests, zero failures/errors/skips (wrapper task was up to date). These are automated/source checks, not fresh live acceptance of the historical terrain. The cited remediation commits are ancestors of this checkout. Historical live results above remain attributed to their original runs.

The unconfirmed water-placement diagnosis was removed. On 2026-09-09 a copied save returned water at `(90,62,-76)` above sandstone, but the intended placement experiment never completed: the preparatory break was rejected by `reflex_active`, and the run suffered bridge stalls and death. Both inspection and portable-table feasibility accept replaceable water. Inspection and automatic table setup also use different search bounds. Neither observation proves a same-site query/executor defect.

| Claim | Current evidence and verdict |
| --- | --- |
| Client-time mismatch / damage during pause | `MinecraftServerTickDebugMixin` gates the actual server tick; `ServerTickDebugControllerTest` passes. Historical live server-tick proof is recorded above. Superseded as an open defect. |
| Navigation perimeter failure | Historical endpoint differs from exact target. `BaritoneTaskExecutorTest.failedCalculationAtReachedNavigateGoalCountsAsCompleted` passes. Remaining reachability claim is unconfirmed. |
| Exact-break timeout and pickup | `EmbodiedAgentRuntimeTest.blockModificationToolTimesOutWithoutTerminalEvidence` verifies timeout reporting, not the original cause. `BlockInteractionTaskExecutor` handles block modification; mining pickup has a separate owner. Tool-selection regression passes in `BaritoneTaskExecutorTest`. No fresh timeout reproduction. |
| Lower cave workstation choice | `PortableTablePlacementPolicyTest.carriedTableWinsOverAWorkstationSeveralBlocksBelowThePlayer` passes; source prefers the carried table in this case. Original terrain replay remains pending. |
| Blocked collection kept mining | Runtime/job ownership fix remains present; `EmbodiedAgentRuntimeTest` covers blocked collection and its events. Historical live proof retained; no new live run. |
| Oversized task lifecycle responses | Wrapper projection and `AiricraftCliMainTest` retain compact lifecycle output and omit verbose mission evidence. Fixed; cached wrapper test result accepted by Gradle. |
| Reflex status/action race and missing hold | Action admission rechecks ACTIVE; `SurvivalReflexSnapshot` requires a hold for AWAITING_PLANNER. Runtime/reflex hold regressions pass. Race is expected; missing-identity wedge fixed. |
| Stale terminal mutation gate | `EmbodiedAgentRuntimeTest.terminalSemanticTaskDoesNotRemainBusyFromStaleReflexExecution` passes. Fixed in source; earlier live proof retained. |
| Unsupported towering / ignored planks | `ReturnToSurfaceTaskExecutorTest.toweringTerminatesAfterAStableSupportFailureWindow` and `ReturnToSurfaceStepArgsTest` pass; source bounds unsupported ticks at 100 and includes overworld planks. No global-shore guarantee. |
| Local surface scan | `SurfaceMemory` scans 16 horizontal blocks and also remembers last safe surface/ground until cleared. Radius confirmed; total recovery failure not freshly reproduced. |
| Blind water flee | `SurvivalReflexRuntime` uses safe target selection, Baritone hops and water-aware recovery. Related reflex tests pass. Historical live proof covers its recorded terrain only. |
| Premature flee resolution | Source retains living loaded threats and uses mob routes/ranged visibility with 60/200-tick confirmation. `securityUsesMobRouteAndRangedLineOfSightRatherThanL2Distance` passes. Existing regression supports the fix, not every terrain. |
| Close single-mob combat | Close-quarter range/cooldown and combat item ranking tests pass. Original zombie kill remains historical live evidence, not mixed-mob proof. |
| Diamond storage-block / leaf bootstrap | `AiricraftProviderPlanningTest` passes ore-routing, unobserved-storage rejection and crafted-stick regressions. Recorded live route was cancelled before mining completion; it is not proof of obtaining a diamond. |
| Exact block break discovery | `PlannerToolCatalog` exposes `break_blocks`; runtime tests verify fresh-read authorization and batching. It is an existing API, not a missing capability. |
| Workbench failure detail | `CraftingTaskExecutor` emits `site_not_found` / `no_static_feasible_site` when no candidates rank. Detailed per-candidate rejection reasons are absent; diagnosis UX gap confirmed, correctness of the original rejection unproven. |
| Deep-mining readiness | `MiningIlluminationPreflightTest` passes. Admission considers illumination prediction, torches and override, not a full survival plan. Confirmed policy gap; historical failure needs current replay. |
| Bed-first entity search | 32-block constant and local tool surface confirmed. Autonomous search-and-return absent; all-tool inability to progress unproven. |
| Fatal ranged reflex / leaf litter | Historical damage is evidence of that encounter only. Weapon selection and ranged security code supersede parts of the diagnosis; current fatality unconfirmed. |
| Ocean save cannot recover | Bounded search confirmed; session memory already exists. No fresh test of this save and every recovery option. Historical claim only. |

### Next progression-focused reproduction: first-leg flee failure

Prioritize this narrow part of the ranged-reflex concern over a broad readiness redesign. In `SurvivalReflexRuntime.flee`, no selected target with `completedFleeLegs == 0` throws `safe_flee_target_unavailable`. `tickMobAttack` catches it and refreshes the snapshot without leaving ACTIVE. Escape escalation depends on completed legs or close contacts, not a deadline for this initial failure. `EmbodiedAgentRuntime.requireLivingPlayerForAction` rejects normal actuation while ACTIVE. Under persistent unsafe ranged exposure with no close contacts or viable initial target, this can keep crafting/resupply blocked while the reflex makes no escape progress.

This is a source-supported failure path, **not yet a reproduced current gameplay blocker**. Existing escalation tests cover completed legs and close contacts; they do not exercise repeated first-target selection failure in a world.

Reproduction acceptance: use a disposable controlled world, one visible skeleton outside melee range, low enough player health to select FLEE, and terrain with no eligible first flee target. Record the exact terrain, inventory and initial health; observe repeated `reflex.actuator_failed` with `safe_flee_target_unavailable`, the same ACTIVE safety epoch, zero completed legs, and a rejected crafting action. Include a control with an eligible escape target. Distinguish normal tick progress from bridge stalls, and stop before death using server-owned pause. Only then choose a bounded recovery change; do not simply release the safety gate into an unsafe situation.

## Follow-up acceptance — 2026-09-13

The first-leg flee proposal above is historical: the later user-directed policy prefers fighting. Current evidence and decisions are recorded in [the autonomous playtest log](autonomous-playtest-log.md), especially D041–D045.

- Shield preparation and reactive blocking passed a natural skeleton encounter: `Not Today, Thank You`, health remaining 20, shield durability 336→332, and sword counterattacks before the threat cleared. This does not establish mixed-mob or low-health reliability.
- Chest crafting, placement, exact deposit/withdrawal, settled stock inspection, close, and saved-client restart passed. Unloading restored 20 free slots. The automatic logbook retained crafting, drops, container transfers and dated home stock across restart.
- Confirmed open-furnace output collection failed with `station_unavailable`. Exact screen-identity handling was fixed and HotSwapped; retry recovered all three ingots and recorded their withdrawal. This was a reproduced progression defect with a successful live retry.
- Exact navigation near the furnace and inside a cramped underground workstation room failed at the final stance. Interaction from the nearby stance and return through the existing passage succeeded. These remain bypassable endpoint failures, not proven general route defects; avoid retrying the same point without new evidence.
- The new visible cave survey and return-memory procedure passed a bounded existing-passage visit with digging/placement disabled. Natural-cave discovery and exposed-ore collection using this procedure remain unverified.

### 2026-09-13: Farm pickup and stance follow-up

- Confirmed irrigation pickup bug: crop tending forced floating seed drops to crop Y=63, while navigation to their actual Y=62 succeeded and collected four seeds. Fixed pickup height with a farmland-only offset; tests/build passed and hot reloaded. Retry harvested/replanted nine crops.
- Confirmed remaining limitation: crop work-position search excludes farmland and crop-occupied feet blocks, so an irrigated farm can fail `crop_has_no_reachable_work_position`. Starting from shore bypassed the initial failure, but the outer cells failed mid-pass. Recorder evidence and exact coordinates are in autonomous playtest decision D054. Whole-plot completion remains pending.

The stance limitation above is now fixed: accept collision-free crop feet space over farmland. After hot reload, the previously failing farm pass completed, all 15 farm cells held wheat, and five bread were crafted at home. Existing crop tests/full build and live recorder `farm-complete.jsonl` passed; see D055.

### 2026-09-13: Shield combat efficiency observation

A skeleton at full health during repeated blocking initially looked stalled. Paused recorder review showed it spent most of the encounter outside melee range, followed by four sword attacks and normal `threats_clear` resolution. Permanent attack starvation is unconfirmed and must not be listed as a verified bug. Slow terrain-heavy closing remains an efficiency concern (D057, `close-skeleton-stall.jsonl`).

### 2026-09-13: Container reopened while leaving shelter

Confirmed, bypassable: navigation from the shelter reopened the new barrel after `close_container` had succeeded. A later crop pass waited in HARVEST and timed out because a container remained open. Flight recorder shows the screen transition during navigation; exact input-order cause remains unproven. Closing the empty-cursor barrel and retrying at the same position completed the crop pass (one harvested/replanted, fourteen growing). See D060 and `farm-harvest-stall.jsonl`.

### 2026-09-13: Mixed fuel requires separate cooking batches

Bypassable limitation: four raw porkchops and enough combined wood fuel were carried, but `check_smeltables` reported no automatic fuel for the full batch because selection uses one fuel type. Two spare doors cooked two porkchops; three slabs cooked the other two in a second batch. All four outputs were collected and one stored with a verified logbook entry. See D061. No progression blocker or furnace fix is claimed.

### 2026-09-13: Surface swimming rejected by resource scope

Confirmed and fixed: collecting shore logs with `surfaceOnly=true` failed when feet entered the top water block at Y62. The scope check required Y63 as though water were solid ground. Preserve the dry-ground boundary, but admit feet and floating drops in the top water layer while still excluding deeper positions. The identical one-log request failed immediately before the patch and completed after HotSwap, raising oak logs two to three. Focused regression/acquisition tests and full build passed. See D064 and `surface-water-fixed.jsonl`.

### 2026-09-13: Charcoal smelting double-counts input logs

Confirmed and fixed: the preview counted input logs again as fuel. A live one-log automatic batch then moved its sole log to the fuel slot and failed `insufficient_input`, despite six available planks. Reserve required inputs before automatic/manual fuel selection and in the preview. After HotSwap, the same automatic batch used one plank and produced one charcoal; an impossible manual shared-log request failed before moving items. Smelting regression tests and full build passed. See D065–D066, `fuel-overlap.jsonl` and `fuel-reservation-fixed.jsonl`.

### 2026-09-13: Exact cave waypoints can become occupied

Bypassable stale memory: return target `(216,67,401)` failed with digging disabled because its feet space now contains dirt. Paused inspection established the obstruction. The adjacent step `(215,67,400)` and updated feet position `(216,68,401)` completed, followed by the short surface route. World bookmarks were corrected. This is not evidence of an A* defect; resample footing when a remembered exact waypoint fails. See D067 and `coal-survey-trip.jsonl`.

Efficiency follow-up: survey-led mining collected eleven coal without selecting hidden ores, but repeated manual approaches and drop-pit navigation were expensive. Reuse System 1 acquisition with an observed/visible target constraint instead of inventing a separate cave mining executor. Fully buried mining should remain available separately.


### 2026-09-13: Baritone replaces food in hotbar slot 0

Confirmed: with inventory access enabled, eating from slot0 timed out while stationary. Recorder snapshots show food replaced by the iron pickaxe before the timeout. Baritone's periodic inventory housekeeping always restores its best pick to slot0 and did not respect active item use. Eating from slot6 succeeded. A narrow mixin now defers that housekeeping during item use; full build passed, but the post-change slot0 live retry remains pending hunger loss. Until verified, disabling allowInventory or eating from another slot is a bypass. See D069 and `eat-before-navigation.jsonl`.
